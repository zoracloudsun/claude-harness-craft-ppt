
### **图片 1：章节封面页**
**布局与样式**  
- 背景：深灰黑网格（#0A0A0C），带微弱同心圆光晕，科技感底层纹理。  
- 标题层级清晰：主标题（英文）→ 副标题（中文）→ 章节标识 → 英文小字注解，呈垂直居中对齐。  
- 字体：无衬线体（类似 Inter / SF Pro），主标题为浅米白（#F5F0E6），副标题为金黄色（#FFD700），强调“核心要点”四字加粗。  
- 视觉动线：顶部「CHAPTER · 04」胶囊按钮（青蓝渐变 #2A9D8F → #1D70B8）引导章节定位；中部标题区用细黄线分隔；底部三颗半透明青蓝圆点构成隐喻“子智能体”的视觉符号。

**飞书风文字内容提取**  
```
CHAPTER · 04

Claude Code Harness  
核心要点解析指南  

第 4 章：子智能体与任务委派实践  
SubAgents & Task Delegation
```

> ✅ 飞书特征：标题无冗余修饰，中英混排规范；「核心要点」作为价值锚点前置；章节编号独立封装，非正文一部分。

---

### **图片 2：双栏回顾 vs 重点**
**布局与样式**  
- 左右分栏：左为“第三章回顾”（绿色标题栏），右为“本章重点”（青蓝色标题栏），形成对比认知闭环。  
- 左侧模块：4个深绿半透明卡片，圆角矩形 + 黄点项目符号，强调“上一章掌握的核心”为行动导向句式。  
- 右侧模块：深蓝主卡 + 浅蓝标题「子智能体 任务委派」，采用大字号粗体+留白呼吸感；下方4条要点用圆点+工程术语（如“受限工具白名单”“Token 经济学”）；底部金句收尾：“让 Claude 像 CEO 一样委派任务”。

**飞书风文字内容提取**  
```
【第三章回顾】  
上一章掌握的核心  
• Skills 触发即注入机制  
• 渐进式披露与 Token 经济学  
• description 精准触发策略  
• 权限最小化与参数注入  

【本章重点】  
子智能体 任务委派  
• 独立上下文 + 受限工具白名单  
• 5 种模式 + 委派式思考  
• 流水线 + Skills 协作模式  
• Token 经济学 + 工程原则映射  

→ 让 Claude 像 CEO 一样委派任务
```

> ✅ 飞书特征：模块标题用【】封装；要点动词前置（“触发”“披露”“协作”）；结尾金句独立成行，无标点，强化传播力。

---

### **图片 3：Bug 排查现场案例**
**布局与样式**  
- 双面板对比设计：左绿（INPUT STACK）→ 右红（CLAUDE FAILURE），模拟真实调试界面。  
- 左侧数据量化：关键指标用红字突出（500 行、200 行、7 个、1h+），体现可观测性。  
- 右侧错误列表：红色 × 符号 + 疾病式描述（“遗忘日志中发现的关键错误码”），直击问题本质。  
- 底部警示框：引号语录 + 括号注解（Context Pollution），将技术现象人格化（“它—疯了”）。

**飞书风文字内容提取**  
```
支付模块 Bug 排查现场  
CONTEXT OVERFLOW · 一小时之后  

[ INPUT STACK ]  
错误日志          500 行  
回归测试输出      200 行  
源文件读取        7 个  
对话历史          1h+  

[ CLAUDE FAILURE ]  
× 遗忘日志中发现的关键错误码  
× 将无关失败项误判为核心问题  
× 引用根本不存在的函数名  

“它 — 疯了”  
这正是上下文污染（Context Pollution）
```

> ✅ 飞书特征：用方括号 `[ ]` 模拟终端/日志上下文；错误项用 × 替代 ❌ 更符合工程师习惯；引号语录保留原始语气，括号补充术语定义。

---

### **图片 4：办公桌隐喻场景**
**布局与样式**  
- 主标题“想象一张办公桌”为超大字号白字，右上角轻量标注“大佬的白板课桌”，建立讲师IP。  
- 三任务卡片横向排列，编号①②③用彩色圆环（青/黄/紫）区分优先级与类型；每张卡含任务名 + 关键挑战描述（如“数字密集”“堆栈错综”）。  
- 底部红色警示框：带感叹号图标，用破折号连接现象与归因（“桌面乱成一团 — 根本找不到需要的东西”），并点明“这正是 Claude 一小时后的处境”。

**飞书风文字内容提取**  
```
想象一张办公桌  
大佬的白板课桌  

桌面只能容纳约 20 份文件，你却要同时处理 3 个任务 ——  

① 审查财务报告  
   20 页 · 数字密集  

② 分析系统日志  
   一沓 · 堆栈错综  

③ 撰写技术方案  
   深度思考 · 高聚焦  

❗ 桌面乱成一团 — 根本找不到需要的东西  
这正是 Claude 一小时后的处境
```

> ✅ 飞书特征：用“——”替代冒号分隔前提与任务；任务描述用名词短语+修饰词（非完整句）；警示框用 ❗ 替代⚠️，更现代。

---

### **图片 5：信噪比分析图**
**布局与样式**  
- 标题“上下文窗口的信噪比”直指核心矛盾；副标题点明关键洞察：“‘大’不等于‘好用’”。  
- 左红右绿双面板：左侧“原始噪声”用密集点阵可视化“200 行”无效信息；右侧“真正的信号”仅1行，但含精确数据（3/47）。  
- 底部总结框：三关键词并列（噪声稀释信息 · 注意力被分散 · Claude 失焦），用间隔点 `·` 连接，无逗号。

**飞书风文字内容提取**  
```
上下文窗口的信噪比  

Claude 的上下文约 20 万 Token，但 “大” 不等于 “好用” — 关键看信噪比。  

【原始噪声】        200 行  
堆栈、警告、堆栈、警告 …… 几百行测试输出全量倒入主对话  

【真正的信号】      1 行  
3 个测试失败 / 47 个通过  
真正需要的，往往只是一句结论 — 剩下的全是噪声  

噪声稀释信息 · 注意力被分散 · Claude 失焦
```

> ✅ 飞书特征：数据具象化（200行 vs 1行）；用 `/` 表达比例关系；结尾三短语无主语，形成节奏感强的诊断结论。

---

### **图片 6：软件工程类比演进**
**布局与样式**  
- 主标题“这个问题，软件工程并不陌生”为设问式破题；副标题点出本质：“如何防止不同任务互相干扰？”。  
- 三时代卡片横向排布（1980s / 2010s / NOW），时间轴隐含演进逻辑；每张卡内用“时代名称”+“核心问题”+“解决方案”三层结构。  
- 底部四个圆形标签：从左到右为工程原则递进（关注点分离 → 进程隔离 → 故障舱壁 → 委派模型），最右“→ 委派模型”高亮金黄，指向本章答案。

**飞书风文字内容提取**  
```
这个问题，软件工程并不陌生  

同一个问题，反复以不同形态出现 — 如何防止不同任务互相干扰？  

[ ERA · 1980s ]  
MS-DOS 时代  
没有内存隔离，一个野指针就能让整个系统崩溃  

[ ERA · 2010s ]  
微服务架构  
取代巨石应用 — 防止单点故障拖垮全局  

[ ERA · NOW ]  
大模型时代  
上下文污染 = AI 时代的内存泄漏  

● 关注点分离  
● 进程隔离  
● 故障舱壁  
→ 委派模型
```

> ✅ 飞书特征：用 `[ ERA · XXXX ]` 统一时代标记；等号 `=` 直接建立类比（“= AI 时代的内存泄漏”）；箭头 `→` 表示最终解法跃迁。

---

### **图片 7：CEO 委派模型架构图**
**布局与样式**  
- 主标题“CEO 委派模型”+ 副标题“现代办公室 · 各司其职”，建立组织隐喻。  
- 中央主智能体（MAIN AGENT）为金色胶囊框，标注“主智能体 = CEO”；虚线向下分支至三个子智能体卡片。  
- 子智能体命名规范：`sub-A` / `sub-B` / `sub-C` + 功能名（code-reviewer / test-runner / log-analyzer）+ 角色说明 + 输出物承诺（“输出审查报告”等），体现职责契约。

**飞书风文字内容提取**  
```
CEO 委派模型  
现代办公室 · 各司其职  

主智能体不再亲吞所有原始数据，而是将任务拆解、委派给专门的“员工”。  

[ MAIN AGENT ]  
主智能体 = CEO  

SUB · A  
code-reviewer  
代码审计员  
独立办公室 · 输出审查报告  

SUB · B  
test-runner  
测试工程师  
独立办公室 · 输出测试摘要  

SUB · C  
log-analyzer  
SRE 值班员  
独立办公室 · 输出故障定位
```

> ✅ 飞书特征：用 `SUB · X` 作为子模块标准前缀；角色名用英文小写连字符（code-reviewer），符合工程惯例；“独立办公室”重复出现，强化隔离概念。

---

### **图片 8：子智能体本质定义**
**布局与样式**  
- 主标题“子智能体的本质”居中加粗；上方定义框用 `[ DEFINITION ]` 标签，三要素用不同颜色高亮（青/黄/绿）。  
- 下方三列卡片：左（独立上下文）→ 中（受限权限）→ 右（明确范围），图标分别为 ▶️ / 🔒 / ➕，视觉引导清晰。  
- 每张卡内描述严格遵循“原则 + 实现机制”结构，无废话。

**飞书风文字内容提取**  
```
子智能体的本质  

[ DEFINITION ]  
一个具备 独立上下文窗口、受限工具权限 与 明确任务范围 的 Claude 实例。  

▶ 独立上下文  
中间过程严格保留在自身上下文，绝不回流主对话  

🔒 受限工具权限  
工具白名单从物理层面禁止越权操作  

➕ 明确任务范围  
仅向主智能体返回结论，而非过程中的所有细节
```

> ✅ 飞书特征：定义框用 `[ ]` 封装；三要素关键词染色（独立上下文→青，受限权限→黄，任务范围→绿）；图标语义精准（▶流程隔离，🔒权限控制，➕范围收敛）。

---

### **图片 9：三大核心价值**
**布局与样式**  
- 标题“三大核心价值”+ 副标题“对应三条软件工程原则”，建立理论根基。  
- 三列卡片编号 1/2/3，颜色依次为青/黄/绿，与图片8呼应；每张卡含：价值名 + 详细解释 + 底部工程术语锚点（如 `Bulkhead` / `Least Privilege` / `Componentization`）。  
- 解释部分用破折号 `—` 连接抽象原则与具体实现，逻辑严密。

**飞书风文字内容提取**  
```
三大核心价值  
对应三条软件工程原则  

1 隔离  
独立上下文窗口 — 文件内容、命令输出、中间推理严格保留在自身空间  
｜ Bulkhead 舱壁模式  

2 约束  
工具权限白名单 — 物理边界，而不是 Prompt 中的 “请不要” 建议  
｜ Least Privilege 最小权限  

3 复用  
Markdown 定义 — 团队共享、项目迁移，新员工入职即可直接使用  
｜ Componentization 组件化
```

> ✅ 飞书特征：价值编号用大号数字+单字标题；破折号后为技术实现路径；底部用 `｜` 引出专业术语，形成“业务价值 → 工程原理”映射链。

---

### **图片 10：技术实现 · 子进程独立窗口**
**布局与样式**  
- 主标题“技术实现 · 子进程独立窗口”点明落地层；左右分栏：左为「CHILD PROCESS · CONTEXT」配置项，右为「EXECUTION FLOW」执行流。  
- 左侧4步配置：编号+功能名（System Prompt / 项目级 CLAUDE.md / ...），第4项高亮金黄，强调“主智能体传递的任务描述”为输入源。  
- 右侧4步执行流：动词开头（启动 / 独立执行 / 仅回传 / 终止），关键动作加粗（“结果摘要”）；底部附带两个根因定位案例（路径+错误类型），体现 Debug 思维。

**飞书风文字内容提取**  
```
技术实现 · 子进程独立窗口  

[ CHILD PROCESS · CONTEXT ]  
1 System Prompt  
2 项目级 CLAUDE.md  
3 子智能体定义指令  
4 主智能体传递的任务描述  

[ EXECUTION FLOW ]  
① 启动全新子进程  
② 子进程内独立执行：读文件、运行命令、分析结果  
③ 仅回传 结果摘要 至主进程  
④ 子进程终止 — 所有中间交互彻底释放  

根因定位：  
src/payment/charge.ts:89  
缺少空值检查 → NullPointerException
```

> ✅ 飞书特征：配置项用数字序号+名词短语；执行流用中文序号①②③④（非1.2.3.）更符合中文文档习惯；“→”用于错误归因，简洁有力；无任何“我们”“应该”等主观表述，纯客观事实陈述。

---

### **图片 11：子智能体的目录结构**
**布局与样式**  
- 主标题“子智能体的目录结构”居中大字，白色无衬线体，强视觉锚点。  
- 左侧为 `[ DIRECTORY TREE ]` 代码树形图：采用标准终端缩进风格（`├─` `└─`），`.claudef/agents/` 下四个 `.md` 文件用金黄色高亮，体现“定义即代码”理念。  
- 右侧三模块垂直堆叠：顶部黄底标签 `YAML 前置元数据` → 中部绿底 `Markdown 正文指令` → 底部青蓝半透明条，形成“配置层 → 行为层 → 元规则”三层认知流。  
- 关键结论置于底部独立条：**“每个 .md 文件 = 一个完整的子智能体定义”**，用等号强化契约关系。

**飞书风文字内容提取**  
```
子智能体的目录结构  

[ DIRECTORY TREE ]  
your-project/  
├─ .claudef/  
│  └─ agents/  
│     ├─ code-reviewer.md  
│     ├─ test-runner.md  
│     ├─ log-analyzer.md  
│     └─ bug-locator.md  
└─ src/  

YAML 前置元数据  
定义子智能体 是什么 — 身份标识 / 能力边界  

Markdown 正文指令  
规定子智能体 怎么做 — 执行流程 / 输出格式 / 专业知识  

每个 .md 文件 = 一个完整的子智能体定义
```

> ✅ 飞书特征：目录树严格遵循 Unix 风格；`.md` 文件名全小写连字符，符合工程惯例；等号 `=` 用于定义本质，非修辞。

---

### **图片 12：YAML 前置元数据三维度**
**布局与样式**  
- 标题“YAML 前置元数据三维度”直指设计哲学；中央 YAML 片段用深灰卡封装，字段带注释 `# 身份维度` 等，模拟真实配置文件。  
- 下方三列卡片横向排列，标题色与图片11呼应（青/黄/紫），每张卡含：维度名 + 关键字段标签（如 `name`, `tools`）+ 一句话原则。  
- “权限维度”卡片内突出 `permissionMode: plan`，强调物理层约束；“执行维度”用 `model` + `hooks` 双标签，体现可扩展性。

**飞书风文字内容提取**  
```
YAML 前置元数据三维度  

---  
name: code-reviewer        # 身份维度  
description: 审查代码质量、安全漏洞与性能问题   # 身份维度  
tools: [Read, Grep, Glob]   # 权限维度  
model: sonnet               # 执行维度  
---  

身份维度  
name  description  
它是谁、何时被触发，决定主智能体的路由匹配  

权限维度  
tools  permissionMode  
工具白名单 — 最小权限原则，物理层禁止越权  

执行维度  
model  hooks  
在哪里运行 / 模型选择 / 期间发生什么
```

> ✅ 飞书特征：YAML 注释用 `#` 统一归类；维度卡片标题用单字+维度（如“身份维度”），避免冗余；关键字段名保留原样（`permissionMode` 不翻译）。

---

### **图片 13：code-reviewer：只读审查员**
**布局与样式**  
- 主标题“code-reviewer：只读审查员”+ 右上角路径 `.claude/agents/code-reviewer.md`，建立文件即实体的认知。  
- 左侧为 `[ DEFINITION ]` 完整配置片段：含 `name`, `description`, `tools`, `model`, `permissionMode`，末尾附加角色宣言（“你是一名资深的代码审查专家…”），体现人格化设定。  
- 右侧三模块用图标区分：⚠️ 安全性（红）、◇ 代码质量（黄）、⚡ 性能（绿），每项列3–4个具体检查项，拒绝模糊描述。  
- 底部输出规范框：固定四部分 → 审查摘要 / 问题列表 / 改进建议，形成标准化交付物。

**飞书风文字内容提取**  
```
code-reviewer：只读审查员  
.claude/agents/code-reviewer.md  

[ DEFINITION ]  
name: code-reviewer  
description: |  
  审查代码质量、安全漏洞和性能问题  
tools:  
  - Read    # 只读  
  - Grep  
  - Glob  
model: sonnet  
permissionMode: plan  

你是一名资深的代码审查专家  
拥有 10 年以上的工程经验  

⚠ 安全性  
硬编码凭证 / SQL 注入 / XSS / CSRF / 输入验证 / 敏感数据处理  

◇ 代码质量  
单一职责 / 命名一致性 / DRY 原则 / 异常处理是否恰当  

⚡ 性能  
循环嵌套复杂度 / 数据结构选择 / N+1 查询 / 资源释放  

固定输出格式 → 审查摘要 / 问题列表 / 改进建议
```

> ✅ 飞书特征：`|` 表示多行字符串；工具项后加 `# 只读` 注释，强化约束；输出格式用 `→` 连接，无标点，节奏紧凑。

---

### **图片 14：关键字段逐一解读**
**布局与样式**  
- 标题“关键字段逐一解读”为行动导向；四卡片2×2网格布局，左上→右下对应 `name` → `description` → `tools` → `model`。  
- 每张卡含：字段名（青/黄/绿/紫标签）+ 角色比喻（如“身份证”“广告牌”“安全防线”）+ 技术本质说明。  
- 底部横向流程图：`Haiku → Sonnet → Opus` 用箭头连接，下方标注各模型适用场景，体现成本-能力权衡。

**飞书风文字内容提取**  
```
关键字段逐一解读  

name  身份证  
系统日志、调试追踪、内部调用链的唯一键值  

description  广告牌 · 路由依据  
主智能体通过语义相似度判断任务归属，自动委派  

tools  安全防线  
未在白名单的工具 — 不可见、不可调用  

model  被低估的成本杠杆  
简单任务用 Haiku，复杂推理用 Sonnet / Opus  

Haiku → Sonnet → Opus  
格式检查 / 日志过滤   代码审查 / 测试分析   架构设计 / 安全审计
```

> ✅ 飞书特征：字段名用标签色块封装；比喻词精炼（“身份证”“广告牌”）；模型演进用 `→` 表达能力跃迁，非“→”或“->”。

---

### **图片 15：test-runner：执行型示例**
**布局与样式**  
- 主标题“test-runner：执行型示例”+ 副标题“轻量模型 + 严格输出格式”，点明定位差异。  
- 左侧为 `[ test-runner.md ]` 配置片段：突出 `Bash` 为关键差异项（红字标注），`model: haiku` 注释“轻量速度优先”。  
- 右侧上部黄框强调“与 code-reviewer 的关键差异”；中部 `[ STRICT OUTPUT ]` 卡片用绿色高亮“5行”输出规范；底部青蓝条给出价值主张：“不需要聘请首席架构师来执行单元测试”。

**飞书风文字内容提取**  
```
test-runner：执行型示例  
轻量模型 + 严格输出格式  

[ test-runner.md ]  
name: test-runner  
description: |  
  运行测试套件并分析结果  
tools:  
  - Read, Grep, Glob  
  - Bash    # 关键差异  
model: haiku   # 轻量速度优先  

与 code-reviewer 的关键差异  
需要 Bash 工具执行命令 — 权限更宽，但输出格式更严  

[ STRICT OUTPUT ]  
总计：X 个测试  
通过：X  · 失败：X  · 跳过：X  
[严禁输出完整日志]  

不需要聘请首席架构师 来执行单元测试
```

> ✅ 飞书特征：关键差异项用 `# 关键差异` 注释；输出规范用 `[ ]` 封装禁令；结尾句无标点，口语化但专业，体现飞书“说人话”的产品哲学。

---

### **图片 16：两种调用方式**
**布局与样式**  
- 主标题“两种调用方式”+ 副标题“主对话最终收到的，永远是经过提炼的结论 — 而非原始噪声”，重申核心价值。  
- 左右双卡：① 自动委派（青蓝底，灯泡图标）vs ② 显式请求（金黄底，勾选图标），形成自动化 vs 显式控制的对比。  
- 每张卡内用“用户：”→“Claude：”对话体展示交互流；底部总结框用 `→` 强调结果：**主对话只看到精炼结论，所有细节留在子上下文**。

**飞书风文字内容提取**  
```
两种调用方式  
主对话最终收到的，永远是经过提炼的结论 — 而非原始噪声  

① 自动委派  
用户：帮我审查 src/payment/ 目录的代码变更  
Claude：好的，委派给 code-reviewer  
→ 启动子智能体 ...  

基于 description 语义相似度，自动匹配合适的子智能体  

② 显式请求  
用户：用 @code-reviewer 审查这段代码  
Claude：启动 code-reviewer 子智能体  
→ 直达指定 ...  

用户明确指定子智能体名称，绕过语义匹配  

→ 主对话只看到精炼结论，所有细节留在子上下文
```

> ✅ 飞书特征：对话体用“用户：”“Claude：”冒号分隔，无引号；箭头 `→` 表示系统动作；总结句独立成行，用 `→` 开头强化因果。

---

### **图片 17：5 种子智能体模式**
**布局与样式**  
- 主标题“5 种子智能体模式”超大字号；副标题强调“不是简单分类清单 — 是 5 种截然不同的架构范式”。  
- 五卡片横向排列，编号 `PATTERN · 01` 至 `05`，颜色渐变（青→蓝→黄→红→紫），体现模式演进。  
- 每张卡含：模式名 + 角色隐喻（如“安全的观察者”“高噪声处理器”），底部小字“深植于经典软件工程原则”统一收束。

**飞书风文字内容提取**  
```
5 种子智能体模式  
不是简单分类清单 — 是 5 种截然不同的架构范式  

PATTERN · 01  
只读型  
安全的观察者  

PATTERN · 02  
执行型  
高噪声处理器  

PATTERN · 03  
并行型  
多专家工作流  

PATTERN · 04  
流水线型  
串行处理链  

PATTERN · 05  
团队型  
自组织协作  

深植于经典软件工程原则
```

> ✅ 飞书特征：模式编号统一前缀 `PATTERN · XX`；角色隐喻用名词短语（非句子）；结尾句独立成行，无标点，形成理论闭环。

---

### **图片 18：模式① 只读型**
**布局与样式**  
- 标题“模式① 只读型”+ 英文副标题 `[ READ-ONLY OBSERVER ]`，中英对照强化专业感。  
- 核心原则句“只读不写 — 赋予观察权，剥夺修改权”置于顶部，定调价值观。  
- 左右双面板：左 `[ ALLOWED TOOLS ]`（青底，✅ 图标）列 `Read/Grep/Glob`；右 `[ DENIED · 物理边界 ]`（红底，❌ 图标）列 `Edit/Write/Bash`，形成强对比。  
- 底部结论框用 `→` 引出：“最小权限原则的完美体现 · 即使幻觉爆发也无法越界”。

**飞书风文字内容提取**  
```
模式① 只读型  
[ READ-ONLY OBSERVER ]  

只读不写 — 赋予观察权，剥夺修改权。审计员可以查阅账本，但绝不允许篡改账本。  

[ ALLOWED TOOLS ]  
◎ Read    读取文件内容  
◎ Grep    模式搜索  
◎ Glob    文件名匹配  

[ DENIED · 物理边界 ]  
✖ Edit    修改文件  
✖ Write   写入新文件  
✖ Bash    执行命令  

→ 最小权限原则的完美体现 · 即使幻觉爆发也无法越界
```

> ✅ 飞书特征：允许/禁止用 ◎/✖ 符号替代文字；“物理边界”作为关键词加粗；结论用 `→` 引导，强调原则落地效果。

---

### **图片 19：模式② 执行型**
**布局与样式**  
- 标题“模式② 执行型”+ `[ NOISE FILTER ]` 副标题，点明其核心功能是信噪比优化。  
- 三模块横向：左 `[ INPUT ]`（红底）显示“500行 海量原始噪声”；中 `[ AGENT ]`（青底）仅一个对勾 ✓ + “过滤·提炼”；右 `[ OUTPUT ]`（绿底）显示“5行 精炼摘要结论”。  
- 底部四标签按钮：`测试套件输出` / `系统日志分析` / `构建过程记录` / `大文件批量处理`，覆盖典型场景。

**飞书风文字内容提取**  
```
模式② 执行型  
[ NOISE FILTER ]  

日常开发中应用最广 — 核心价值是信噪比优化。  

[ INPUT ]  
500行  
海量原始噪声  

[ AGENT ]  
✓  
过滤 · 提炼  

[ OUTPUT ]  
5行  
精炼摘要结论  
总计 50 / 通过 47 / 失败 3  

测试套件输出   系统日志分析   构建过程记录   大文件批量处理
```

> ✅ 飞书特征：输入/输出用 `[ ]` 封装；数字突出显示（500行 / 5行）；底部场景用空格分隔，无标点，符合飞书快捷标签习惯。

---

### **图片 20：模式③ 并行型**
**布局与样式**  
- 标题“模式③ 并行型”+ `[ MAP-REDUCE ]` 副标题，直接关联经典分布式计算范式。  
- 顶部说明句：“从多个独立维度剖析同一问题 — 像派遣多名侦探同时调查不同线索”，用侦探隐喻降低理解门槛。  
- 中部三专家卡：`EXPERT · A`（security-auditor）、`B`（quality-checker）、`C`（perf-analyzer），命名规范统一，职责清晰。  
- 底部 `[ AGGREGATE · REDUCE ]` 卡片强调“汇总 3 份报告 → 综合评估结论”；最下方关键前提用青蓝字强调：“三个维度之间 不存在共享状态 · 否则改用流水线”。

**飞书风文字内容提取**  
```
模式③ 并行型  
[ MAP-REDUCE ]  

从多个独立维度剖析同一问题 — 像派遣多名侦探同时调查不同线索。  

[ DISPATCH · MAP ]  
主智能体 → 全面分析 src/  

EXPERT · A  
security-auditor  
安全审计 · 漏洞扫描  

EXPERT · B  
quality-checker  
代码质量评估  

EXPERT · C  
perf-analyzer  
性能瓶颈分析  

[ AGGREGATE · REDUCE ]  
汇总 3 份报告 → 综合评估结论  

关键前提：三个维度之间 不存在共享状态 · 否则改用流水线
```

> ✅ 飞书特征：`MAP-REDUCE` 作为技术锚点前置；专家命名 `EXPERT · X` 统一格式；关键前提用 `·` 连接，无逗号; 箭头 `→` 表示数据流，非“->”。

---

### **图片 21：模式④ 流水线型**
**布局与样式**  
- 主标题“模式④ 流水线型”+ 英文副标题 `[ CHAIN OF RESPONSIBILITY ]`，直接锚定设计模式经典理论。  
- 核心定义句居中：“具有明确阶段依赖的任务流 — 每个阶段的输出，是下一阶段的输入”，用破折号强调因果链。  
- 四阶段卡片横向排列（STAGE 01 → 04），颜色渐变（青→黄→绿→紫），每张卡含：阶段名、子智能体名、权限约束、输出物，形成清晰流水线。  
- 底部红色警示框带✅图标，提出“交接契约（Handoff Contract）”概念，并类比 Unix 管道（`cat | grep | sort | uniq`），实现跨领域知识迁移。

**飞书风文字内容提取**  
```
模式④ 流水线型  
[ CHAIN OF RESPONSIBILITY ]  

具有明确阶段依赖的任务流 — 每个阶段的输出，是下一阶段的输入。  

STAGE 01  
定位  
bug-locator  
权限：只读  
输出：根因定位  

STAGE 02  
修复  
bug-fixer  
权限：读写+Bash  
输出：修复方案  

STAGE 03  
验证  
bug-verify  
权限：只读+Bash  
输出：验证结果  

STAGE 04  
分析  
bug-report  
权限：只读  
输出：影响报告  

✅ 交接契约（Handoff Contract）  
每阶段的输出格式必须严格匹配下一阶段的输入要求 — 类比 Unix 管道：cat | grep | sort | uniq
```

> ✅ 飞书特征：阶段编号 `STAGE 0X` 统一格式；权限/输出用短横线分隔；Unix 管道用 `|` 连接，无空格；✅图标用于强调关键机制。

---

### **图片 22：模式⑤ 团队型**
**布局与样式**  
- 主标题“模式⑤ 团队型”+ `[ AUTONOMOUS TEAM ]` 副标题，突出“自组织”特性。  
- 上部三角色卡片：`Team Lead`（人形图标）、`Teammate`（双人图标）、`共享设施`（信封图标），构成完整协作单元。  
- 中部四模式选项（MODE A–D）用小卡片横向排列，描述不同协作策略（竞争假设 / 并行审查 / 模块归属 / 方案审批），体现决策多样性。  
- 底部结论框用 `→` 引出核心判断标准：“长期协作的复杂度，决定是否值得使用团队型”。

**飞书风文字内容提取**  
```
模式⑤ 团队型  
[ AUTONOMOUS TEAM ]  

长期存续 — 不再是一次性，而是持续协作 / 实时通信 / 自主分工。  

👤 Team Lead  
团队负责人 · 项目经理角色  
任务分解 / 资源调度 / 进度跟踪  

👥 Teammate  
团队成员 · 各有专长  
前端 / 后端 / 数据 / 安全 ...  

📧 共享设施  
Task List 任务列表  
Mailbox 异步邮箱通信  

MODE A  竞争假设 · 多 Agent 并行调查  
MODE B  并行审查 · 安全 + 功能 同时进行  
MODE C  模块归属 · 按模块划分跨人协作  
MODE D  方案审批 · 提案 + 魔鬼代言人  

→ 长期协作的复杂度，决定是否值得使用团队型
```

> ✅ 飞书特征：角色用 Unicode 图标（👤👥📧）替代文字标签；MODE X 用大写+空格分隔；结论句以 `→` 开头，保持节奏统一。

---

### **图片 23：子智能体与 Skills 协作**
**布局与样式**  
- 主标题“子智能体与 Skills 协作”+ 右上角小字“只有两种原子模式”，设定讨论边界。  
- 左右双栏对比：`DIRECTION · A`（子智能体 → 预加载 Skill）vs `DIRECTION · B`（Skill → 派生子智能体），箭头方向决定主从关系。  
- 每栏含配置片段（YAML 片段）+ 角色定位（“执行者” vs “调度者”）+ 典型场景，逻辑严密。  
- 底部绿色总结框提炼判断标准：“谁是主角？· 角色驱动选 A · 流程驱动选 B”，用 `·` 分隔选项，无标点。

**飞书风文字内容提取**  
```
子智能体与 Skills 协作  
只有两种原子模式  

[ DIRECTION · A ]  
子智能体 → 预加载 Skill  
子智能体 ⊃ Skill  

name: bug-fixer  
skills:  
  - secure-coding  

子智能体是 执行者（角色驱动）  
Skill 是它手中的 操作手册  
场景：流水线长期角色 · 多轮交互  

[ DIRECTION · B ]  
Skill → 派生子智能体  
Skill ⊃ 子智能体  

name: codebase-check  
context: fork  
agent: general-purpose  

Skill 是 调度者（流程驱动）  
子智能体只是 执行容器  
场景：单次重型任务 · 严格上下文隔离  

判断标准：谁是主角？ · 角色驱动选 A · 流程驱动选 B
```

> ✅ 飞书特征：`⊃` 符号表示包含关系（非“包含”文字）；角色定位用加粗关键词（执行者/调度者）；判断标准用问号+`·`分隔，符合飞书决策树风格。

---

### **图片 24：Token 经济学**
**布局与样式**  
- 主标题“Token 经济学”+ 副标题“不增反降 — 这是反直觉的真相”，制造认知冲突。  
- 双面板对比：左 `[ WITHOUT SUBAGENT ]`（红底）vs 右 `[ WITH SUBAGENT ]`（绿底），数据量化清晰（125K vs 116K）。  
- 关键差异点用颜色标注：“噪声进入主对话历史”（红）vs “噪声隔离在子上下文”（绿）；主对话接收量从“全量”降至“100 Token 摘要”。  
- 底部收益矩阵：`SAVE ~6.8%`（蓝）、`LONGER CHAT ↑ 50%`（黄）、`BONUS`（青）三列，用箭头↑和百分比强化价值。

**飞书风文字内容提取**  
```
Token 经济学  
不增反降 — 这是反直觉的真相  

10000 Token 测试输出 + 后续 5 轮对话场景测算 —  

[ WITHOUT SUBAGENT ]  
噪声进入主对话历史  
每轮重复传输 ...  
125 K  
5 轮后累计消耗  

[ WITH SUBAGENT ]  
噪声隔离在子上下文  
主对话仅收到 100 Token 摘要  
116 K  
5 轮后累计消耗  

SAVE      LONGER CHAT      BONUS  
~6.8%     ↑ 50%            上下文窗口保护  
                          响应质量提升
```

> ✅ 飞书特征：数据用 `K` 单位（非“千”）；`↑` 表示增长；收益项无动词，纯名词短语堆叠，符合飞书数据看板习惯；“反直觉的真相”作为钩子前置。

---

### **图片 25：何时启用子智能体？4 个核心维度**
**布局与样式**  
- 主标题设问式“何时启用子智能体？4 个核心维度”，激发读者自查需求。  
- 四卡片2×2网格：① 大规模文件读取、② 高频输出生成、③ 上下文完整性保护、④ 操作权限与安全边界，覆盖输入/输出/隔离/安全全链路。  
- 每张卡内：场景条件 + 价值主张（绿色高亮关键收益），如“输入噪声隔离”“输出噪声过滤”。  
- 底部红色反面提示框：“反面：输入 ≈ 输出 的小任务 — 子智能体反而得不偿失”，用 `≈` 符号表达近似关系，严谨且简洁。

**飞书风文字内容提取**  
```
何时启用子智能体？4 个核心维度  

满足任一维度，就值得引入 — 关键看输入与输出的体量比  

① 大规模文件读取  
超过 5 个文件 / 海量数据源  
输入噪声隔离 · 避免主上下文被填满  

② 高频输出生成  
大量中间输出 / 测试 / 构建日志  
输出噪声过滤 · 仅返回摘要  

③ 上下文完整性保护  
长期任务规划 / 复杂工作流  
主对话保留充足上下文空间  

④ 操作权限与安全边界  
敏感操作 / 文件写入 / 命令执行  
受限沙箱 · 风险局部化  

反面：输入 ≈ 输出 的小任务 — 子智能体反而得不偿失
```

> ✅ 飞书特征：维度编号用①②③④（非1.2.3.4）；`≈` 表示近似相等；反面提示用“反面：”开头，与正面形成镜像结构。

---

### **图片 26：从软件工程看子智能体**
**布局与样式**  
- 主标题“从软件工程看子智能体”+ 副标题“不是凭空创造 — 是经典智慧的跨领域迁移”，确立理论根基。  
- 四原则卡片2×2布局：PRINCIPLE 1～4，标题色与前文呼应（青/黄/绿/紫），每张卡含：原则名 + 本质解释 + 子智能体实例映射。  
- 关键映射关系用 `=` 或 `→` 连接（如“Bulkhead · 子上下文 = 故障水密舱”），实现跨域类比。  
- 底部金句总结：“优秀的架构原则，具有跨领域的普适性”，用 `→` 引导，收束有力。

**飞书风文字内容提取**  
```
从软件工程看子智能体  
不是凭空创造 — 是经典智慧的跨领域迁移  

PRINCIPLE 1  
单一职责原则  
SRP · 每个子智能体只做好一件事  
code-reviewer 不跨界做测试  

PRINCIPLE 2  
舱壁模式  
Bulkhead · 子上下文 = 故障水密舱  
局部异常不污染主系统  

PRINCIPLE 3  
MapReduce 模式  
Map 分发 → Reduce 汇总  
并行型子智能体的同根同源  

PRINCIPLE 4  
责任链模式  
Chain of Responsibility · Unix 管道  
流水线型的设计模式根脉  

→ 优秀的架构原则，具有跨领域的普适性
```

> ✅ 飞书特征：原则编号 `PRINCIPLE X` 统一；等号 `=` 用于强类比；箭头 `→` 表示流程演进；结尾句独立成行，无标点。

---

### **图片 27：实战注意事项**
**布局与样式**  
- 主标题“实战注意事项”+ 副标题“这些细节决定子智能体能否在生产环境稳定工作”，强调落地风险。  
- 四 Tip 卡片2×2排列，编号 `[ TIP · 01 ]` 至 `04`，每张含：机制名称 + 技术要点 + 优先级/限制说明。  
- 关键约束用颜色突出：`≤ 2 层`（紫）、`> 3 层`（红），视觉引导明确。  
- 底部红色警告框带⚠️图标：“子智能体不是银弹 — 简单任务直接做，不要过度设计”，用破折号连接价值观。

**飞书风文字内容提取**  
```
实战注意事项  
这些细节决定子智能体能否在生产环境稳定工作  

[ TIP · 01 ]  
CLAUD.md 继承关系  
所有子智能体自动加载项目级 CLAUDE.md  
优先级：子智能体 > 项目级 > 用户级  

[ TIP · 02 ]  
报文传输模式  
子智能体之间无法直接通信  
主智能体必须显式提取并转发结论  

[ TIP · 03 ]  
中断恢复机制  
中断会丢失内存中的中间产物  
耗时任务务必持久化到文件  

[ TIP · 04 ]  
嵌套深度控制  
嵌套层级建议 ≤ 2 层  
超过 3 层 = 任务分解需要重新设计  

⚠ 子智能体不是银弹 — 简单任务直接做，不要过度设计
```

> ✅ 飞书特征：Tip 编号统一 `[ TIP · XX ]`；`≤` / `>` 符号用于量化约束；警告框用 ⚠️ + 破折号，语气坚定但专业。

---

### **图片 28：委派式思考（章节总结）**
**布局与样式**  
- 标题“委派式思考”居中，上方 `[ CHAPTER SUMMARY ]` 小字标识总结属性。  
- 核心理念框用深灰卡封装，三行递进式文案：  
  1. 否定旧范式（“不再事必躬亲”）  
  2. 类比新角色（“像成熟管理者那样”）  
  3. 提炼方法论（“清晰界定职责边界、能力要求与交付标准”）  
- 下方三标签卡片：`5 种模式`（青）、`日常 80%`（黄）、`切忌过度`（红），用数据与禁忌收尾，形成行动指南闭环。

**飞书风文字内容提取**  
```
[ CHAPTER SUMMARY ]  
委派式思考  

不再事必躬亲地将所有细节塞进单一对话  
而是像 成熟管理者 那样 —  
清晰界定每个任务的职责边界、能力要求与交付标准  

5 种模式  
能力谱系，非孤立技巧  

日常 80%  
只读型 + 执行型 就够  

切忌过度  
嵌套不超 2 层
```

> ✅ 飞书特征：总结框无项目符号，纯三行递进句；标签卡片用单句概括，无标点; “80%”“2层”等量化指标增强可信度。

---

### **图片 29：下章预告**
**布局与样式**  
- 顶部小字 `[ 下章预告 ]` 为轻量引导；主标题“Hooks 工程实践”超大字号，白色粗体，占据视觉中心。  
- 主体为单一大卡片，左置蓝色对勾图标（✅），右置蓝色箭头（→），形成“已完成 → 待开启”的叙事动线。  
- 内容分三层：  
  1. 核心概念：`Hooks · 事件驱动自动化`  
  2. 机制说明：工具调用前后的自动触发钩子机制  
  3. 价值承诺：实现代码格式化、敏感信息过滤、规范强制执行  
- 结尾句“让 AI 工作流更安全、更可靠”独立成行，无标点，呼应飞书产品价值观。

**飞书风文字内容提取**  
```
[ 下章预告 ]  
Hooks 工程实践  

NEXT · CHAPTER 05  

✅ Hooks · 事件驱动自动化  
工具调用前后自动触发的钩子机制  
实现代码格式化、敏感信息过滤、规范强制执行  
让 AI 工作流更安全、更可靠  

→
```

> ✅ 飞书特征：预告页极简，仅保留必要信息；✅图标表示“本章结束”；箭头 `→` 预留翻页动线；结尾句无标点，短促有力，符合飞书文档收尾惯例。

---