### **图片 1：封面页**

**布局与样式分析：**
- 背景：深黑底 + 浅灰网格线（典型飞书技术文档风格）
- 中央聚焦式排版：主标题居中，上下留白充足
- 标题分层：
  - 顶部小标签：圆角矩形 `CHAPTER · 05`，橙棕渐变填充，白色小号字
  - 主标题英文：`Claude Code Harness`，大号无衬线体，浅米白至淡橙渐变，有轻微发光效果
  - 副标题中文：`核心要点解析指南`，更大字号，鲜明橙红色，加粗，带轻微投影
  - 分隔线：细长橙色横线（约1px宽，长度≈副标题宽度）
  - 章节说明：`第5章：Hooks 事件驱动自动化`，橙色中号字，居中
  - 英文补充：`Event-Driven Automation`，浅灰小字，居中，位于最下方

**完整文字提取（飞书风格式）：**

> **CHAPTER · 05**  
>   
> # Claude Code Harness  
> ## 核心要点解析指南  
> ————————  
> **第5章：Hooks 事件驱动自动化**  
> *Event-Driven Automation*

---

### **图片 2：章节导览页**

**布局与样式分析：**
- 左右分栏：左侧“上一章回顾”，右侧“本章重点”
- 左侧：蓝色标题栏 `第四章回顾`；下方4个深青色圆角卡片，每项前缀●黄点
- 右侧：橙红标题栏 `本章重点`；主卡片深棕黑底+橙红边框，标题 `Hooks` 大号橙红粗体，子标题 `· 事件驱动自动化` 黄色加粗；下方小字说明三特性：`系统执行层 · 强制拦截 · 不依赖认知`；底部灰色小字注释：`策略与机制分离的最佳实践`

**完整文字提取：**

> ### 第四章回顾  
> - ● 子智能体隔离上下文  
> - ● 任务委派与分发  
> - ● 并行执行架构  
> - ● 认知层的建议性约束  
>   
> ### 本章重点  
> #### Hooks  
> - **· 事件驱动自动化**  
>   
> *系统执行层 · 强制拦截 · 不依赖认知*  
>   
> > 策略与机制分离的最佳实践

---

### **图片 3：真实事故案例页**

**布局与样式分析：**
- 左上角 `[真实事故]` 橙色标签
- 主标题 `一次深夜的疏忽` 白色超大字，左对齐
- 左侧：模拟消息气泡（深棕黑底+橙边），内含团队群消息截图风格文本，引号包裹，时间标注在底部
- 右侧：两个垂直堆叠卡片：
  - 上卡：⚠️图标 + `泄露的内容`，列3项（箭头→引导）
  - 下卡：⏰图标 + `付出的代价`，列2项（首行为加粗“整个团队 半天时间”）

**完整文字提取：**

> **[真实事故]**  
> # 一次深夜的疏忽  
>   
> > “谁把 env 文件推上去了？  
> > API key 已暴露，所有密钥立即作废！”  
> >   
> > • 团队群消息 · 次日早晨 9:00  
>   
> ### ⚠️ 泄露的内容  
> → 3 个环境的数据库连接字符串  
> → 内部 API 密钥  
> → 支付网关测试账号密码  
>   
> ### ⏰ 付出的代价  
> **整个团队 半天时间**  
> 密钥轮换 · 日志排查 · 修正引用

---

### **图片 4：问题反思页**

**布局与样式分析：**
- 左侧两段对话体（模拟人物发言）：
  - “大佬·直接领导”：黄底黑字气泡，关键句高亮黄
  - “萌新”：白字普通段落，解释疏忽原因
- 右侧两卡片：
  - 上卡：文件图标 + `CLAUDE.md · 建议`，引述规范
  - 下卡：盾牌图标 + `Hooks · 强制执行`，强调“物理阻断”
- 底部橙色对比句：`策略（应该怎么做）vs 机制（一旦违反就被阻止）`

**完整文字提取：**

> **大佬 · 直接领导**  
> > 这种错误明明可以自动拦截，  
> > 为什么没有任何机制能阻止它？  
>   
> **萌新**  
> 并非不知道 `.env` 严禁提交——  
> 只是在深夜疲惫状态下，疏忽了检查。  
>   
> 📄 **CLAUDE.md · 建议**  
> “不要提交 .env 文件”  
> ——Claude 可以选择遵守，也可以忽略  
>   
> 🔒 **Hooks · 强制执行**  
> **操作被系统执行层物理阻断**  
> Claude 的决策被直接推翻，无法落地  
>   
> > **策略（应该怎么做） vs 机制（一旦违反就被阻止）**

---

### **图片 5：三种机制对比总览页**

**布局与样式分析：**
- 顶部大标题：白色加粗 `三种机制·影响力递增的谱系`
- 三列并排卡片（左→右影响力递增）：
  - 左：紫色 `CLAUDE.md`，认知层，建议性，类比“交通标志”
  - 中：青蓝 `Skills`，认知层，指导性，类比“驾驶手册”
  - 右：橙红 `Hooks`，**本章重点**标签，系统执行层，强制性，类比“路障/限速器”
- 每卡含：图标、标题、功能描述、约束性质+类比

**完整文字提取：**

> # 三种机制·影响力递增的谱系  
>   
> **📄 CLAUDE.md**  
> *认知层*  
> 确立项目规范，始终加载，告诉Claude应该遵守什么  
> **约束性质**：建议 · 类比：交通标志  
>   
> **📘 Skills**  
> *认知层*  
> 封装领域工作流，语义触发，指导Claude遇到此类问题如何做  
> **约束性质**：指导 · 类比：驾驶手册  
>   
> **🛡️ Hooks**  
> *系统执行层*｜**本章重点**  
> 事件自动触发，直接拦截行为  
> 不依赖Claude的认知与“自觉”  
> **约束性质**：强制 · 类比：路障/限速器

---

### **图片 6：四维度对比表页**

**布局与样式分析：**
- 标题：白色加粗 `三种机制·四维度对比` + 右上角小标签 `表 5-1`
- 表格：深棕底 + 橙色表头，4列：`机制` | `工作层面` | `触发方式` | `约束性质` | `类比`
- 行数据颜色区分：CLAUDE.md（紫）、Skills（青）、Hooks（橙）
- 表格下方：盾牌图标 + 金句总结，关键词高亮（交通标志/驾驶手册/路障）

**完整文字提取：**

> # 三种机制·四维度对比　`表 5-1`  
>   
> | 机制         | 工作层面     | 触发方式           | 约束性质 | 类比         |
> |--------------|--------------|--------------------|----------|--------------|
> | **CLAUDE.md** | 认知层       | 始终加载           | 建议     | 交通标志     |
> | **Skills**    | 认知层       | 语义匹配/显式触发  | 指导     | 驾驶手册     |
> | **Hooks**     | 系统执行层   | 事件自动触发       | **强制** | 路障/限速器  |  
>   
> 🔒 **交通标志让你知晓规则，驾驶手册让你掌握方法，路障确保在你忘记规则时系统依然守住安全底线**

---

### **图片 7：事件体系概览页**

**布局与样式分析：**
- 左侧：巨大橙色数字 `17` + “个事件”，下方说明“覆盖AI会话从启动到终止的完整生命周期”，版本对比（早期7个 → 当前17个）
- 右侧：5个垂直卡片，按重要性/分类排序，每卡左上角带编号圆点（橙/黄/蓝/绿/紫），标题加粗，下方列举具体事件名（省略号表示更多）

**完整文字提取：**

> # 17 个事件  
> 覆盖AI会话从启动到终止的完整生命周期  
>   
> **周期**  
> - 早期版本：7 个  
> - 当前版本：**17 个**  
>   
> - **3** 会话级事件  
>   `SessionStart · SessionEnd · PreCompact`  
>   
> - **5** 工具调用事件 · 最核心  
>   `PreToolUse · PostToolUse · PermissionRequest · ...`  
>   
> - **2** 子智能体事件  
>   `SubagentStart · SubagentStop`  
>   
> - **2** 完成事件  
>   `Stop · Notification`  
>   
> - **5** 较新的事件类型  
>   `TeammateIdle · ConfigChange · WorktreeCreate · ...`

---

### **图片 8：会话级事件详解页**

**布局与样式分析：**
- 顶部标题：白色 `会话级事件 · 3个`
- 三列卡片（左→右）：`SessionStart`（绿）、`SessionEnd`（橙）、`PreCompact`（蓝）
- 每卡含：事件名、触发时机、核心能力/原因区分、功能说明
- 底部独立代码块：`# SessionStart Hook 示例`，Shell语法高亮（绿色关键字、橙色变量、白色命令）

**完整文字提取：**

> # 会话级事件 · 3个  
>   
> **• SessionStart**  
> 会话启动或恢复时触发  
> - # 核心能力  
>   `CLAUDE_ENV_FILE`  
> - # 注入环境变量  
>   自动配置开发环境  
>   
> **• SessionEnd**  
> 会话终止时触发  
> - matcher 区分原因：  
>   `clear · logout · prompt_input_exit`  
> - 清理资源 · 记录统计  
>   
> **• PreCompact**  
> 上下文压缩前触发  
> - matcher 区分类型：  
>   `manual · auto`  
> - 压缩前备份完整对话  
>   
> ```bash
> # SessionStart Hook 示例
> if [ -n "$CLAUDE_ENV_FILE" ]; then
>   echo 'export NODE_ENV=development' >> "$CLAUDE_ENV_FILE"
> fi
> ```

---

### **图片 9：PreToolUse 事件详解页**

**布局与样式分析：**
- 左侧：标题 `PreToolUse` + 橙色标签 `最强大`
- 三大操作选项（卡片式）：
  - ✅ `allow`：绿色，绕过权限系统直接执行
  - ❌ `deny`：红棕，阻止并说明原因
  - ✏️ `updatedInput`：黄绿，修改输入参数后执行（标“最强大”）
- 底部注释：解释 `修改输入` 的静默安全增强逻辑
- 右侧：标题 `updatedInput 实战示例`，含 JSON 格式代码块 + 效果对比（原命令 → 修改后命令，绿色箭头↓）

**完整文字提取：**

> # PreToolUse　`最强大`  
> 工具决定调用后 · 实际执行前触发  
>   
> - ✅ **allow**　绕过权限系统直接执行  
> - ❌ **deny**　阻止执行并说明原因  
> - ✏️ **updatedInput**　修改输入参数后执行 · **最强大**  
>   
> > 修改输入：在不中断操作的前提下，静默添加安全参数  
>   
> ### updatedInput 实战示例  
> **hookSpecificOutput 格式**  
> ```json
> {
>   "hookSpecificOutput": {
>     "hookEventName": "PreToolUse",
>     "permissionDecision": "allow",
>     "updatedInput": {
>       "command": "rm -rf /tmp/test --dry-run"
>     }
>   }
> }
> ```  
> **效果**：  
> `rm -rf /tmp/test`  
> ↓ 静默改为  
> `rm -rf /tmp/test --dry-run`

---

### **图片 10：工具调用事件其余4个页**

**布局与样式分析：**
- 顶部标题：白色 `工具调用事件 · 其余4个`
- 2×2 四宫格卡片布局，每卡含：
  - 事件名（带圆点符号）
  - 状态标签（如“执行后”、“vs PreToolUse”）
  - 功能描述
  - 典型用途（部分含对比说明）

**完整文字提取：**

> # 工具调用事件 · 其余4个  
>   
> **• PostToolUse**　`执行后`  
> 工具成功执行后触发。无法撤销操作，但可通过 `additionalContext` 向 Claude 反馈信息  
> *典型用途：lint 结果反馈 · 自动格式化*  
>   
> **• PostToolUseFailure**  
> 工具执行失败后触发  
> *典型用途：错误告警 · 纠正性反馈*  
>   
> **• PermissionRequest**  
> 权限对话框弹出前触发  
> *vs PreToolUse：仅在需要用户手动确认时激活*  
> 以编程方式自动批准或拒绝权限  
>   
> **• UserPromptSubmit**  
> 用户提交输入后、Claude 开始处理前触发  
> *典型用途：输入预处理 · 自动附加 Git 分支信息*

---

好的，我们继续按顺序处理 **图片11至图片19**（您共提供了9张新图，编号应为11–19；其中第14张需替换为您的新增表格内容）。严格遵循飞书风格：深色网格背景、语义化图标/颜色、卡片式分栏、关键词高亮、层级清晰。

---

### **图片 11：子智能体事件 & 完成事件详解页**

**布局与样式分析：**
- 左右双栏标题：左 `子智能体事件`（青蓝），右 `完成事件`（青绿）
- 每栏含2个卡片，左栏上下排列，右栏上下排列
- 卡片设计：
  - 标题行：事件名（加粗）+ 小标签（如“质量门控核心”）
  - 正文：功能描述 + 关键行为（动词高亮，如 `block`、`注入`）
  - 示例/参数：代码片段或字段（浅灰小字）
- 右下角 `Notification` 卡片含集成平台列表（Slack/钉钉/弹窗）

**完整文字提取（飞书风）：**

> ### 子智能体事件  
>   
> **• SubagentStart**  
> 子智能体启动时触发。不能阻止启动，但可通过 `additionalContext` 注入关键上下文  
> *示例：启动 code-reviewer 时自动加载团队编码规范*  
>   
> **• SubagentStop**  
> 任务完成后触发。**可以拦截**，强制子智能体继续工作直至满足质量标准  
> `transcript_path`　`agent_transcript_path`  
>   
> ### 完成事件  
>   
> **• Stop**　`质量门控核心`  
> Claude 完成整轮响应时触发。检测到不达标时 **`block`**，强制继续修正  
> ```json
> decision: "block"
> reason: "测试失败，请修复后再停止"
> ```  
>   
> **• Notification**  
> 系统发送通知时触发，可区分：  
> `permission_prompt` · `idle_prompt` · `auth_success`  
> *集成 Slack / 钉钉 · 本地桌面弹窗*

---

### **图片 12：“能否阻止”：最关键的分类维度页**

**布局与样式分析：**
- 顶部大标题：橙红粗体 `“能否阻止”：最关键的分类维度`
- 左右双栏对比：
  - 左栏：盾牌图标 + `可阻止 · 9个`，列出9个事件名（圆角标签），底部注释 `控制流程 · 决定行为`
  - 右栏：眼睛图标 + `只读 · 其余事件`，列出4个事件名 + `...`，底部说明 `观察记录 · 注入信息 · 无法阻止执行`
- 底部横幅：黄底黑字强调 `日常最常用3个：PreToolUse · PostToolUse · Stop — 精通这三个，构建健壮自动化闭环`

**完整文字提取：**

> # “能否阻止”：最关键的分类维度  
>   
> ### 🔒 可阻止 · 9个  
> - `PreToolUse`　`PermissionRequest`　`UserPromptSubmit`　`Stop`  
> - `SubagentStop`　`TeammateIdle`　`TaskCompleted`　`ConfigChange`  
> - `WorktreeCreate`  
> > 控制流程 · 决定行为  
>   
> ### 👁️ 只读 · 其余事件  
> - `PostToolUse`　`Notification`　`SubagentStart`　`...`  
> > 观察记录 · 注入信息 · 无法阻止执行  
>   
> > **日常最常用3个**：`PreToolUse` · `PostToolUse` · `Stop`  
> > — 精通这三个，构建健壮自动化闭环

---

### **图片 13：配置体系：六层优先级架构页**

**布局与样式分析：**
- 左侧主标题：橙红 `配置体系：六层优先级架构`
- 中央垂直堆叠6个卡片，从上到下编号①→⑥，颜色递变（橙→黄→蓝→青→绿→紫），顶部标“最高”，底部标“最低”
- 每卡含：层级名、配置文件路径、作用说明（含 Git/个人覆盖等关键词）
- 右侧两辅助卡片：
  - `叠加合并`：强调“不是覆盖，是合并+高优优先级”
  - `团队协作`：绿色图标，说明 Git 提交与自动同步机制

**完整文字提取：**

> # 配置体系：六层优先级架构  
>   
> **① 企业策略配置**　`最高`  
> `managed-settings.json` · 组织强制规范  
>   
> **② 项目配置**  
> `.claude/settings.json` · 提交到 Git · 团队共享  
>   
> **③ 项目本地配置**  
> `.claude/settings.local.json` · `.gitignore` 排除 · 个人覆盖  
>   
> **④ 用户全局配置**  
> `~/.claude/settings.json` · 跨项目个人偏好  
>   
> **⑤ 插件内置 Hooks**  
> Plugin 自带配置 · 跟随插件安装  
>   
> **⑥ 子智能体 Frontmatter 内联 Hooks**　`最低`  
> 仅在该子智能体运行期间生效  
>   
> ### ⚙️ 叠加合并  
> 多层配置不是覆盖关系  
> 而是 **合并叠加，高优优先级**  
> 在冲突时优先生效  
>   
> ### 🤝 团队协作  
> 项目配置提交到 Git  
> 所有克隆成员 **自动同步**  
> 团队安全检查规则

---
感谢您的澄清！您说的是：

> **第14张不是替换原内容，而是“插入”这个新增表格（作为新图）**，即在原有图片序列中**新增一张图**（编号为14），而原第14–19张顺延为15–20张

我们已处理完图片1–13（前13张），现在按正确逻辑继续：

- **图片14**：您提供的新增表格（`type | 说明 | 典型场景`）——**作为全新一页插入**
- 原“退出码·意图信号”页 → 现为 **图片15**
- 原“hookSpecificOutput”页 → 现为 **图片16**
- ……依此类推，直至原最后一张变为 **图片20**

---

### ✅ **图片14： —— Hook 执行类型协议规范**

**布局与样式分析：**
- 深黑网格背景，顶部无标题（或可理解为隐式标题：`Hook 执行类型协议`）
- 中央一个宽卡片，标题为 `type 协议规范`（橙色粗体）
- 表格结构清晰：3列 `type` | `说明` | `典型场景`
- 每行 `type` 字段加粗+高亮色（与飞书代码高亮风格一致）：
  - `command` → 橙红
  - `http` → 青蓝
  - `mcp_tool` → 紫灰
  - `prompt` → 黄金
  - `agent` → 绿松
- “典型场景”列使用短语式描述，含具体动词（拦截、上报、联动、检查、审查）
- 底部无额外注释，保持简洁专业感

**完整文字提取（飞书风）：**

> ### type 协议规范  
>   
> | type         | 说明                                                                 | 典型场景                     |
> |--------------|----------------------------------------------------------------------|------------------------------|
> | **`command`**   | 执行本地 Shell 命令，JSON 通过 stdin 传入，exit code 控制行为（0=通过，2=阻断） | 拦截危险命令、跑 lint、格式化 |
> | **`http`**      | POST JSON 到指定 URL，响应同 `command` 格式                             | Slack/飞书通知、审计日志上报 |
> | **`mcp_tool`**  | 直接调用已连接的 MCP Server 的工具                                    | 与已有 MCP 工具联动          |
> | **`prompt`**    | 将 prompt 发给轻量 Claude 模型做单轮判断                              | 语义检查（如“这段代码是否泄露密钥”） |
> | **`agent`**     | 启动子 Agent，可使用 Read/Grep 等工具多步验证                         | 复杂代码审查、多条件校验     |

> 💡 **设计原则**：所有类型共享统一响应格式（`permissionDecision`, `reason`, `updatedInput`），确保 Hooks 层逻辑一致性。

---

### **图片15：退出码 · 意图信号页**

**布局与样式分析：**
- 左侧主区：标题 `退出码 · 意图信号`（黄橙渐变），3个垂直状态卡片：
  - ✅ `0`：绿色背景，`成功 · 放行`，右上角✅图标
  - ❌ `2`：深红背景，`有意阻止 · 关键！`，右上角❌图标，加粗强调
  - ⚠️ `1/3+`：灰黑背景，`脚本异常 · 透传错误`，右上角⚠️图标
- 右侧辅助区：标题 `设计哲学`，含两个子模块：
  - `烟雾报警器类比`（保留原内容，因您未要求删除，仅作为上下文补充）
  - `核心原则`（绿色高亮句）

> 注：您此前说明“第14张替换为新增表格”，但未要求删除原退出码页，故此处仍保留其作为独立页（图片15），符合“插入新图、余者顺延”逻辑。

**完整文字提取（飞书风）：**

> # 退出码 · 意图信号  
>   
> **✅ 0**　成功 · 放行  
> 脚本正常完成，操作继续执行  
>   
> **❌ 2**　有意阻止 · 关键！  
> 操作被主动拦截，严格区分于脚本故障  
>   
> **⚠️ 1/3+**　脚本异常 · 透传错误  
> 脚本自身出错，不应影响正常工作流  
>   
> ### 设计哲学  
> #### ☕ 烟雾报警器类比  
> - **退出码 2 = 真实火警**  
>   报警器检测到烟雾 → 有意触发 → 人员疏散  
> - **退出码 1 = 报警器故障**  
>   报警器自身断电/坏掉 → 不应禁止人员进出大楼  
>   
> #### 核心原则  
> > **脚本故障 不应 阻碍正常工作流运行**

---

### **图片16：hookSpecificOutput 协议详解页**

**布局与样式分析：**
- 左侧：主标题 `hookSpecificOutput` + 副标题说明（含“2025年升级为嵌套对象格式”提示）
- 4个决策能力卡片（✅ allow / ❌ deny / ⚪ ask / 📥 additionalContext），颜色语义统一
- 右侧：两个代码块：
  - 上：`stdout 输出格式`，展示完整 JSON 结构，关键字段如 `"permissionDecisionReason"` 用橙色突出
  - 下：`allow + updatedInput` 示例，展示参数修改逻辑
- 底部无额外注释，保持技术文档简洁性

**完整文字提取：**

> # hookSpecificOutput  
> PreToolUse 与 Claude 通信的专用协议  
> *2025 年升级为嵌套对象格式（推荐）*  
>   
> - ✅ **allow**　绕过权限系统直接执行  
> - ❌ **deny**　阻止执行，附带原因说明  
> - ⚪ **ask**　我不确定，请由人工决定  
> - 📥 **additionalContext**  
>   适用于所有事件类型，内容注入 Claude 上下文，构建高效反馈闭环  
>   
> ### 完整协议格式（推荐嵌套格式）  
> **stdout 输出格式**  
> ```json
> {
>   "hookSpecificOutput": {
>     "hookEventName": "PreToolUse",
>     "permissionDecision": "deny",
>     "permissionDecisionReason": "禁止提交 .env 文件",
>     "additionalContext": "检测到敏感文件..."
>   }
> }
> ```  
>   
> ### allow + updatedInput（修改参数执行）  
> ```json
> {
>   "hookSpecificOutput": {
>     "hookEventName": "PreToolUse",
>     "permissionDecision": "allow",
>     "updatedInput": {
>       "command": "cmd --dry-run"
>     }
>   }
> }
> ```

---

### **图片17：三道防线体系 · 第一道防线页**

**布局与样式分析：**
- 左侧：标题 `三道防线体系`，3个防线卡片（①危险命令拦截｜②敏感文件保护｜③全量操作审计），编号圆点色阶递进
- 底部横向标签组：`可拦截的高危操作`（4个红色圆角标签）
- 右侧：`第一道防线：脚本核心设计`，展示 `block-dangerous.sh` 脚本
  - 关键逻辑用注释标注（`# jq 解析`、`# 事前拦截`）
  - `exit 2` 显著标出，呼应退出码2的“强制阻断”语义
- 底部3个✅勾选条目总结设计亮点

**完整文字提取：**

> # 三道防线体系  
>   
> **① 危险命令拦截**　`当前重点`  
> 事前拦截 · PreToolUse · Bash  
>   
> **② 敏感文件保护**  
> 事中防护 · Write/Edit matcher  
>   
> **③ 全量操作审计**  
> 事后审计 · matcher: `"*"`  
>   
> **可拦截的高危操作**  
> `rm -rf`　`DROP DATABASE`　`curl | bash`　`chmod 777`  
>   
> ### 第一道防线：脚本核心设计  
> `hooks/block-dangerous.sh`  
> ```bash
> #!/bin/bash
> INPUT=$(cat)  # 从 stdin 读取 Claude 请求
> 
> # jq 解析（避免脆弱的字符串匹配）
> CMD=$(echo "$INPUT" | jq -r '.tool_input.command // ""')
> 
> if echo "$CMD" | grep -qE 'rm -rf /'; then
>   # 生成 deny 响应并输出到 stderr
>   jq -n '{
>     hookSpecificOutput: {
>       hookEventName: "PreToolUse",
>       permissionDecision: "deny",
>       permissionDecisionReason: "危险命令已拦截"
>     }
>   }' >&2
>   exit 2  # ← 关键：退出码2 = 强制阻断
> fi
> ```  
> ✅ stderr 输出调试　✅ jq 安全解析　✅ 明确退出码语义

---

### **图片18：第二道防线 · 敏感文件保护页**

**布局与样式分析：**
- 左侧：标题 `第二道防线 敏感文件保护`，结构清晰：
  - matcher 触发条件（Write|Edit）
  - 三类保护范围（文件名/扩展名/目录），每类用 • 列表+代码式示例
  - 底部警示语（情感化表达，增强记忆点）
- 右侧：`核心检测逻辑`，脚本 `protect-sensitive.sh`
  - 使用 `for` 循环 + `[[ ]]` 条件判断，体现 Shell 工程化
  - `deny` 语句带动态变量（`$BASENAME`, `$EXT`），突出可维护性
  - 无冗余注释，代码即文档

**完整文字提取：**

> ### 第二道防线 敏感文件保护  
>   
> **matcher 配置 – 精准触发**  
> `"matcher": "Write|Edit"`  
> • 仅在文件写入或编辑时触发  
>   
> **🛡️ 保护范围三类**  
> - **受保护文件名**  
>   `.env` `credentials.json` `.netrc`  
> - **受保护扩展名**  
>   `.pem` `.key` `.p12` `.pfx`  
> - **受保护目录**  
>   `.git/` `.ssh/` `/etc/` `~/.aws/`  
>   
> > 🔔 如果这道防线当天就位，萌新的悲剧本可完全避免  
>   
> ### 核心检测逻辑  
> `hooks/protect-sensitive.sh`  
> ```bash
> FILE=$(echo "$INPUT" | jq -r '.tool_input.file_path // ""')
> BASENAME=$(basename "$FILE")
> EXT="${BASENAME##*.}"
> 
> # 检查受保护文件名
> PROTECTED_NAMES=(.env credentials.json)
> for name in "${PROTECTED_NAMES[@]}"; do
>   [[ "$BASENAME" == "$name" ]] && deny "敏感文件禁止写入：$BASENAME"
> done
> 
> # 检查受保护扩展名
> PROTECTED_EXTS=(pem key p12)
> for ext in "${PROTECTED_EXTS[@]}"; do
>   [[ "$EXT" == "$ext" ]] && deny "密钥文件禁止写入：.$EXT"
> done
> ```

---

### **图片19：第三道防线 · 全量操作审计页**

**布局与样式分析：**
- 左侧：标题 `第三道防线 全量操作审计 — 事后可追溯`
  - 配置说明：`matcher: "***"`（星号通配符，强调全覆盖）
  - 日志脚本逻辑：提取 `TIMESTAMP`/`TOOL`/`TARGET`，追加至 `~/.claude/audit.log`
  - 底部三问模型：**When? What? Where?**（青绿高亮关键词）
- 右侧：`三道防线 · 安全闭环` 流程图式卡片
  - 3步编号卡片（①→②→③），颜色与左侧防线对应
  - 底部升华句：`依赖人工谨慎的脆弱流程 ↓ 升级为 代码即法律 · 自动化体系`（箭头+加粗）

**完整文字提取：**

> # 第三道防线 全量操作审计　— 事后可追溯  
>   
> **配置：matcher: "***"**　捕获所有工具调用  
>   
> ```bash
> # 每条日志回答3个核心问题
> TIMESTAMP=$(date '+%Y-%m-%d %H:%M:%S')
> TOOL=$(echo "$INPUT" | jq -r '.tool_name')
> TARGET=$(echo "$INPUT" | jq -r '.tool_input | to_entries[0].value')
> 
> # 写入审计日志（不阻断操作）
> echo "[$TIMESTAMP] $TOOL → $TARGET" >> ~/.claude/audit.log
> ```  
>   
> **每条日志清晰回答三个核心问题**  
> - **When?**　时间戳  
> - **What?**　操作类型  
> - **Where?**　操作目标  
>   
> ### 三道防线 · 安全闭环  
> **① 事前拦截**　危险命令 · PreToolUse  
> **② 事中防护**　敏感文件 · Write/Edit  
> **③ 事后审计**　全量日志 · matcher: "***"  
>   
> > 依赖人工谨慎的脆弱流程  
> > ↓ 升级为  
> > **代码即法律 · 自动化体系**

---

### **图片20：代码质量自动化页**

**布局与样式分析：**
- 左侧：标题 `代码质量自动化`，聚焦“关注点分离”理念
  - 三大要点：Claude 专注逻辑｜格式化后台无感｜无需感知规范
  - 工具映射表：3行键值对，左对齐，箭头 `→` 统一风格
  - 底部原则：✅ 工具未安装 → 静默跳过（强调优雅降级）
- 右侧：`PostToolUse Hook 实现`，脚本 `auto-format.sh`
  - `case $EXT in` 多分支结构清晰
  - 每分支含 `command -v` 检查，不存在则 `fi` 直接跳过（无报错）
  - 末尾 `exit 0` 明确非阻断行为

**完整文字提取：**

> # 代码质量自动化  
>   
> ### 自动格式化  
> 🔍 **关注点分离**  
> - Claude 专注 **代码逻辑**  
> - 格式化在后台 **无感完成**  
> - 无需感知项目的格式化规范  
>   
> **自动选择对应格式化工具**  
> - `.ts / .js`　→ `prettier`  
> - `.py`　　　→ `black`  
> - `.go`　　　→ `gofmt`  
>   
> ✅ 工具未安装 → 静默跳过，体现优雅降级原则  
>   
> ### PostToolUse Hook 实现  
> `hooks/auto-format.sh`  
> ```bash
> FILE=$(echo "$INPUT" | jq -r '.tool_input.file_path // ""')
> EXT="${FILE##*.}"
> 
> case "$EXT" in
>   ts|js|tsx|jsx)
>     command -v prettier >/dev/null && prettier --write "$FILE" >/dev/null
>     ;;
>   py)
>     command -v black >/dev/null && black "$FILE" >/dev/null
>     ;;
> esac
> exit 0  # 非阻断：即使无工具也允许流程继续
> ```

---

收到！我们继续按顺序处理 **图片21至图片26**（您新提供了6张图，编号为21–26），严格遵循飞书风格：深色网格背景、语义化图标/颜色、流程图式左→右/上→下动线、关键代码高亮、标题层级清晰。

---

### **图片21：Lint 反馈循环 — 全自动代码修复迭代**

**布局与样式分析：**
- 顶部主标题：`Lint 反馈循环`（白+紫渐变），副标题 `— 全自动代码修复迭代`
- 左侧：垂直4步流程图，带向下箭头连接，每步为圆角卡片，编号①→④，颜色递进（紫→黄→橙→绿）
  - ① Claude 修改 JS 文件（PostToolUse 触发）
  - ② Hook 运行 eslint（检测规范错误）
  - ③ additionalContext 注入错误详情
  - ④ Claude 自动分析并修复（无需人工干预）
  - 底部回环箭头：`← 回到步骤 1 重新校验`（形成闭环）
- 右侧：标题 `Hook 脚本关键实现`，展示 `hooks/lint-feedback.sh` 脚本
  - 关键逻辑分段注释：`# 仅检查 JS/TS 文件`、`# 错误注入 Claude 上下文`
  - 使用 `jq -n` 构造 `additionalContext` JSON，动态插入 `$CTX`
  - 结尾 `exit 0` 表明非阻断（允许循环继续）
- 底部横幅：✅ 绿色勾选句 `代码生成 → 质量达标，全程无人工干预`

**完整文字提取（飞书风）：**

> # Lint 反馈循环　— 全自动代码修复迭代  
>   
> ### 🔄 四步闭环流程  
> **①** `Claude 修改 JS 文件`  
> *PostToolUse 事件触发*  
>   
> **②** `Hook 运行 eslint`  
> *检测代码规范错误*  
>   
> **③** `additionalContext 注入错误详情`  
> *Lint 结果写入 Claude 上下文*  
>   
> **④** `Claude 自动分析并修复`  
> *无需人工干预，循环直至达标*  
>   
> ↻ **回到步骤 1 重新校验**  
>   
> ### Hook 脚本关键实现  
> `hooks/lint-feedback.sh`  
> ```bash
> FILE=$(echo "$INPUT" | jq -r '.tool_input.file_path // ""')
> 
> # 仅检查 JS/TS 文件
> if [[ "$FILE" =~ \.(js|ts)$ ]]; then
>   LINT_RESULT=$(eslint "$FILE" --format compact 2>&1)
>   
>   # 错误注入 Claude 上下文
>   if [ -n "$LINT_RESULT" ]; then
>     jq -n '{
>       additionalContext: "Lint 错误:\n'$LINT_RESULT'"
>     }' | cat >&2
>   fi
> fi
> exit 0
> ```  
> ✅ 代码生成 → 质量达标，全程无人工干预

---

### **图片22：Stop Hook · 质量门控页**

**布局与样式分析：**
- 左侧：标题 `Stop Hook · 质量门控`（青绿），副标题说明触发时机
- 主体为脚本 `hooks/quality-gate.sh`，含3个核心逻辑块：
  - 防死循环：通过 `STOP_HOOK_ACTIVE` 标志位控制
  - 运行测试套件：`npm test 2>&1`
  - 测试失败处理：`exit 2` + `decision: "block"` 响应
- 右侧：两栏“关键机制”：
  - 上卡：⚠️ `STOP_HOOK_ACTIVE` 机制，强调“打破无限修复循环”
  - 中卡：↩️ 递归类比，用“递归函数终止条件”类比质量门控必要性
  - 下卡：状态流转图：`测试失败 → block → Claude 修复` → `再次完成 → 测试通过 → 允许停止`

**完整文字提取：**

> # Stop Hook · 质量门控  
> Claude 宣称“完成”时触发，测试失败则阻止会话结束  
>   
> ### `hooks/quality-gate.sh`  
> ```bash
> # 关键：防止死循环
> if [[ "${STOP_HOOK_ACTIVE}" == "true" ]]; then
>   # 已在 Stop Hook 中，放行避免无限循环
>   exit 0
> fi
> 
> # 运行测试套件
> TEST_RESULT=$(npm test 2>&1)
> TEST_EXIT=$?
> 
> if [[ $TEST_EXIT -ne 0 ]]; then
>   # 测试失败 → 阻止停止，强制继续修复
>   jq -n '{
>     decision: "block",
>     reason: "测试失败，请修复后再停止"
>   }' >&2
>   exit 2
> fi
> ```  
>   
> ### 关键机制  
> #### ⚠️ `STOP_HOOK_ACTIVE`  
> Stop Hook 执行失败 → 系统再次触发  
> 标志置为 `true` → 脚本检测后放行  
> **打破无限修复循环**  
>   
> #### ↩️ 递归类比  
> 就像递归函数的 **终止条件**  
> 没有它，Claude 会陷入：  
> `修复 → 测试失败 → 再修复 → ...`  
> **永远无法自行脱困**  
>   
> > 测试失败 → `block` → Claude 修复  
> > 再次完成 → 测试通过 → 允许停止

---

### **图片23：子智能体 Hooks 页**

**布局与样式分析：**
- 左侧：标题 `子智能体 Hooks`，含2个对比卡片：
  - 上卡：`全局 settings.json`（标“不推荐”），说明“无差别拦截所有子智能体 Bash 命令”风险
  - 下卡：`Frontmatter 内联 Hooks`（标“推荐”），列3条优势（生命周期绑定、配置集成、无需改全局）
  - 底部补充：`SubagentStart` / `SubagentStop` 的外部注入与验收角色
- 右侧：标题 `实战示例：db-reader 子智能体`，展示 `.claude/agents/db-reader.md` 配置片段
  - 包含 `name`, `description`, `hooks` 层级
  - `PreToolUse` 中指定 `matcher: "Bash"` + `command: "bash hooks/allow-readonly-sql.sh"`
  - 底部注释：明确权限边界——“只允许 SELECT 语句，禁止任何写操作”

**完整文字提取：**

> # 子智能体 Hooks  
>   
> ### ❌ 全局 `settings.json`　`不推荐`  
> 无差别拦截所有子智能体的 Bash 命令  
> 容易误拦截正常操作，维护困难  
>   
> ### ✅ `Frontmatter` 内联 Hooks　`推荐`  
> - ✔ Hook 随子智能体生命周期激活/清理  
> - ✔ 配置与定义集成于同一文件  
> - ✔ 随 `.md` 文件分发，无需修改全局配置  
>   
> > `SubagentStart`　外部注入上下文  
> > `SubagentStop`　外部验收工作成果  
>   
> ### 实战示例：db-reader 子智能体  
> `.claude/agents/db-reader.md`  
> ```yaml
> ---
> name: db-reader
> description: 专门执行 SQL 只读查询
> hooks:
>   PreToolUse:
>     - matcher: "Bash"
>       hooks:
>         - type: command
>           command: "bash hooks/allow-readonly-sql.sh"
> ---
> ```  
> > 💡 你是只读数据库查询助手  
> > **只允许 `SELECT` 语句，禁止任何写操作。**

---

### **图片24：异步 Hooks 页（2026 年初）**

**布局与样式分析：**
- 左侧：标题 `异步 Hooks` + 时间标签 `2026 年初`
  - 主卡片：`"async": true` 配置说明，强调“Claude 无需等待 Hook 完成即可继续工作”
  - 警告区：⚠️ `两个关键限制`（仅 `command` 类型支持异步；无法阻止当前操作）
  - 底部：`适用场景` 三标签按钮（日志记录 / 异步通知 / 监控上报）
- 右侧：标题 `调试三板斧`，3个调试方案卡片（编号①→③）：
  - ① `stderr` 输出调试信息（JSON 不污染 stdout）
  - ② 手动构造输入直接测试（`bash hooks/check.sh`）
  - ③ `claude --debug` 查看完整执行细节

**完整文字提取：**

> # 异步 Hooks　`2026 年初`  
>   
> ```yaml
> "async": true
> # Hook 在后台非阻塞运行
> ```
> > Claude 无需等待 Hook 完成即可继续工作  
>   
> ### ⚠️ 两个关键限制  
> → 仅 `command` 类型支持异步  
> → 无法阻止当前操作（只读/通知用途）  
>   
> **适用场景**  
> `日志记录`　`异步通知`　`监控上报`  
>   
> ### 调试三板斧  
> **①** `stderr` 输出调试信息  
> `echo "Debug: $VAR" >&2`  
> *不污染 stdout 的 JSON 决策输出*  
>   
> **②** 手动构造输入直接测试  
> ```bash
> echo '{"tool_name":"Bash",...}' | bash hooks/check.sh
> ```  
> *无需运行完整会话即可测试脚本*  
>   
> **③** `claude --debug` 查看执行细节  
> `claude --debug`  
> *查看完整 Hook 触发、输入输出日志*

---

### **图片25：[本章小结] 页**

**布局与样式分析：**
- 中央聚焦式设计：大标题 `[ 本章 小结 ]`（黄橙粗体），副标题 `Hooks · 事件驱动自动化`
- 下方一个深棕黑底圆角卡片，内含4条核心总结（每条前缀 `>`，颜色区分）：
  - 橙：17个事件 · 系统级拦截  
  - 青：6层配置 · 灵活组合  
  - 绿：三道防线 · 安全保障  
  - 紫：代码即法律 · 强制执行  
- 左侧小盾牌图标（橙红描边），强化“安全”主题
- 整体极简，无冗余元素，突出章节收束感

**完整文字提取：**

> # [ 本章 小结 ]  
> ## Hooks · 事件驱动自动化  
>   
> > 🔒 **17个事件 · 系统级拦截**  
> > 🧩 **6层配置 · 灵活组合**  
> > 🛡️ **三道防线 · 安全保障**  
> > ⚖️ **代码即法律 · 强制执行**

---

### **图片26：[下章预告] 页**

**布局与样式分析：**
- 顶部标题：`[ 下章 预告 ]`（黄橙），副标题 `MCP · 模型上下文协议`
- 中央一个发光蓝边卡片，左图标为锁形（🔒），右箭头（→）指向未来
- 卡片内：
  - 小标题 `NEXT · CHAPTER 06`
  - 主标题 `MCP · 标准化工具插座`
  - 两行说明文案：功能定位 + 战略意义
- 背景仍为深黑网格，整体风格与封面页呼应，营造章节过渡仪式感

**完整文字提取：**

> # [ 下章 预告 ]  
> ## MCP · 模型上下文协议  
>   
> > **NEXT · CHAPTER 06**  
> > ### MCP · 标准化工具插座  
> > 访问数据库、调用 API、与外部 AI 服务协作  
> > 工具生态标准化，是 Claude 企业级落地的关键一步  
> > 🔒 →

---