
### **图片 1：章节封面页**
**布局与样式**：
- 背景：深黑底 + 浅灰细密网格线（飞书典型科技感底纹）
- 中央：一个半透明金色三角锥体（象征“聚焦”或“工程结构”），作为视觉锚点
- 标题分层：3级文字垂直居中排列，字号递减，颜色由浅黄→亮黄→金黄过渡
- 顶部小标签：圆角矩形蓝底白字 `CHAPTER · 03`，悬浮于三角锥顶
- 底部辅助文字：极细灰色小字，英文为斜体，体现专业感

**飞书风文字提取**：
```
CHAPTER · 03

Claude Code Harness  
核心要点解析指南  

第 3 章：Skills 工程实践  
Skills Engineering Practice
```

---

### **图片 2：本章导览页**
**布局与样式**：
- 左右双栏布局：左为“上一章回顾”（紫色标题栏），右为“本章重点”（金色标题栏）
- 左侧：4个深灰圆角卡片，每项前缀黄色实心圆点（•）
- 右侧：主标题“Skills 工程实践”采用大号黄+白双色字（Skills 黄，工程实践 白），下方4个要点用浅灰圆点（•），底部有行动号召句（灰色小字）
- 整体对齐严谨，留白充足，符合飞书信息密度控制原则

**飞书风文字提取**：
```
【第二章回顾】

上一章掌握的核心
• 记忆系统 CLAUDE.md 核心  
• 四层作用域全景  
• 结构化写法与最佳实践  
• CLAUDE.md 全量加载成本  

【本章重点】

Skills  
工程实践  

• 触发即注入，不触发零消耗  
• Skill 文件结构与命名规范  
• description 精准触发策略  
• 权限控制与参数注入实战  

让 Claude 精准执行复杂任务
```

---

### **图片 3：真实场景问题页**
**布局与样式**：
- 左侧大标题：“每次都要重新交代”（超大粗体白字，带阴影增强可读性）
- 左下角引述框：深灰底+双引号图标，问题以黄色提问式排版，署名小字灰色
- 右侧三步流程：STEP 01/02/03 分别用蓝/黄/红标签区分优先级，内容区颜色随标签变化（蓝→黄→暗红），形成视觉动线
- “真实场景”标签为黄色小字方括号，置于左上角，轻量但醒目

**飞书风文字提取**：
```
[ 真实场景 ]

每次都要重新交代

“为什么每次开启新会话  
都要从头重新交代一遍？”

• 萌新 · AI 开发工程师  

STEP 01  
每次花 3 分钟交代背景  
RESTful 规范 · OpenAPI 3.0 · 中英双语输出  

STEP 02  
上周这样，上上周也这样  
相同的知识，每次都要重复投喂  

STEP 03  
切换任务还要重来一遍  
分明是在为“失忆临时工”反复入职培训
```

---

### **图片 4：问题-方案对比页**
**布局与样式**：
- 顶部双标签：左“问题”红底白字，右“方案”绿底白字，形成强烈对比
- 中央大标题“Skills”居中，金色粗体，下方两个并列要点（• 用户触发时才注入 Prompt / • 不触发 = 零 Token 消耗），用等宽间距分隔
- 问题与方案框均为深灰圆角卡片，内文使用引号包裹，关键术语（如 `CLAUD.md`、`Skills`）加粗或变色（绿色高亮）

**飞书风文字提取**：
```
问题  萌新：  
“我的 CLAUDE.md 已经两千字了，Claude 每次  
都要读一遍……成本太高了”

方案  
“CLAUD.md 是常驻知识，Skills 是按需加载的  
专属工具包，两者分工不同”

Skills  
• 用户触发时才注入 Prompt  
• 不触发 = 零 Token 消耗
```

---

### **图片 5：CLAUD.md vs Skills 对比表**
**布局与样式**：
- 主标题大号白字居中，`vs` 用金色圆形图标包裹（视觉焦点）
- 左右双卡片：左蓝底（CLAUD.md），右黄底（Skills），颜色区分角色定位
- 每张卡片内：顶部小标题（蓝/黄），副标题（浅灰），4个要点用对应色圆点（●），底部附带示例代码块（缩进+语法高亮风格）
- 示例部分用 `/` 开头路径格式，模拟 CLI 命令，增强工程感

**飞书风文字提取**：
```
CLAUD.md vs Skills

CLAUD.md  
常驻知识  
● 每次对话自动加载  
● 全局生效，始终存在  
● 适合：项目规范、代码风格  
● 适合：团队约定、架构原则  

# 代码风格  
- 使用 TypeScript  
- 缩进 2 空格  
- 优先函数式编程  

VS  

Skills  
按需知识  
● 用户触发时才加载  
● 按需激活，零闲置成本  
● 适合：复杂任务流程  
● 适合：专属工具、一次性操作  

/smarter-commit  
/code-review  
/financial-analysis  
→ 触发时注入 Prompt
```

---

### **图片 6：Skills 本质定义页**
**布局与样式**：
- 主标题“Skills 本质定义”居中，白字加粗
- 中央核心公式框：黑底圆角，用 `Markdown + YAML = Skill` 图形化表达，三者分别标色（蓝+黄=绿），下方小字注释
- 公式下方三列图标卡片：闪电（按需激活）、锁（权限隔离）、循环箭头（可复用），图标+标题+说明三层结构，配色统一（黄/蓝/绿）
- 关键短语“教而不劳”用引号+金黄加粗突出，体现理念升华

**飞书风文字提取**：
```
Skills 本质定义

“教而不劳”  
提前教会 Claude，用时零消耗  

Markdown  +  YAML  =  Skill  
指令文件     元数据      可复用工具  

⚡ 按需激活  
不触发  
零 Token 消耗  

🔒 权限隔离  
每个 Skill  
独立权限控制  

🔄 可复用  
团队共享  
版本控制
```

---

### **图片 7：Skill 目录结构页**
**布局与样式**：
- 左侧大标题“Skill 目录结构”，副标题 `[标准文件 · 组织规范]` 黄色小字
- 中央文件树：深灰背景，文件夹图标（📁）+ 文件名，层级缩进清晰，主文件 `SKILL.md` 加粗高亮
- 右侧四张说明卡片：按功能分区（主文件/脚本/模板/文档），标题色块区分（黄/蓝/灰/红），关键限制条件用红色警示（如 `README.md ×`）
- 文件名大小写、后缀、路径斜杠全部保留原貌，体现工程严谨性

**飞书风文字提取**：
```
Skill  
目录结构  

[标准文件 · 组织规范]

文件树  
📁 my-skill/  
  📄 SKILL.md — 主文件  
  📁 scripts/  
    run.sh  
    analyze.py  
  📁 templates/  
    report.md  
  README.md — 不识别  

SKILL.md  
技能主文件，包含 YAML 元数据 +  
Markdown 指令，触发时注入 Prompt  

scripts/  
辅助脚本，由 SKILL.md 中的 allowed-tools 调用执行  

templates/  
输出模板，供 Claude 填充生成标准格式文档  

README.md ×  
不会被识别为技能主文件，命名必须是 SKILL.md
```

---

### **图片 8：命名规则：三条铁则**
**布局与样式**：
- 顶部大标题“命名规则：三条铁则”，白字加粗
- 三列等宽卡片，编号 01/02/03，标题色块对应内容主题（蓝/黄/红）
- 每卡内：主规则（大字）、解释（小字）、✅/❌ 示例（带勾叉图标），形成“规则→原理→正误对照”逻辑链
- “kebab-case” 用黄色加粗强调，体现命名规范核心

**飞书风文字提取**：
```
命名规则：三条铁则

01 主文件命名  
SKILL.md  
必须大写 SKILL.md，Claude Code 才能识别，触发时自动注入 Prompt  

✓ SKILL.md  
✗ README.md  
✗ skill.md  

02 目录命名规范  
kebab-case  
使用全小写连字符格式，与 CLI 命令风格保持一致，便于 tab 补全  

✓ smart-commit  
✓ code-review  
✗ SmartCommit  

03 README 不识别  
README.md  
README.md 仅作人类文档，不被识别为技能入口，职责严格分离  

smart-commit/  
  SKILL.md ← 主文件  
  README.md ← 忽略
```

---

### **图片 9：YAML 元数据三维度**
**布局与样式**：
- 顶部标题“YAML 元数据三维度”，白字
- 上方展示一段 YAML 示例代码（带注释），字段名（name/description/allowed-tools/user-invocable）用不同颜色高亮
- 下方三列卡片：对应三个维度（身份/权限/执行），标题色块匹配 YAML 字段色（蓝/黄/紫），每卡含字段名+解释+技术说明
- “三维度”概念通过视觉分区强化，避免信息混杂

**飞书风文字提取**：
```
YAML 元数据三维度

---
name: smart-commit          # 身份维度  
description: 分析 git 变更，生成规范提交  # 身份维度  
allowed-tools: [Bash, Read] # 权限维度  
user-invocable: true        # 执行维度  
---

身份维度  
name  description  
技能的唯一标识与语义描述，决定触发匹配逻辑，name 即命令名  

权限维度  
allowed-tools  
精确控制 Claude 可调用的工具列表，实现最小权限原则，防止越权操作  

执行维度  
user-invocable  
disable-model-invocation  
控制技能的激活方式与执行模式，支持显式命令与纯脚本两种形态
```

---

### **图片 10：两个正交字段**
**布局与样式**：
- 主标题“两个正交字段”，白字居中
- 左右双卡片：FIELD 01（蓝底）与 FIELD 02（黄底），标题用小号灰字 `FIELD 01/02`
- 每卡顶部字段名大号加粗（蓝色/黄色），下方解释用常规白字，关键配置值（如 `true`）用亮色突出
- 底部用符号图标（✓/◎/☑）分类说明适用场景，增强可操作性
- “显式触发”、“Tab 补全提示”等用户价值点单独列出，体现产品思维

**飞书风文字提取**：
```
两个正交字段

FIELD 01  
disable-model-invocation  

设为 true 时，Skill 只执行脚本，不调用 Claude AI 模型  
disable-model-invocation: true  

✓ 纯脚本任务：代码格式化、文件批处理  
✓ 确定性操作：不需要 AI 判断的自动化  
◎ 零模型 Token 消耗，极低成本  

FIELD 02  
user-invocable  

设为 true 时，用户可直接用 /skill-name 命令显式触发  
user-invocable: true  

◎ 显式触发：/smart-commit  
◎ 同时支持语义匹配自动激活  
☑ Tab 补全提示可用的技能列表
```

---

### **图片 11：渐进式披露 图书馆模型：三层加载结构**
**布局与样式**：
- 主标题居中，白字加粗，“三层加载结构”用黄色强调
- 左侧三阶垂直流程图：TIER 1→2→3，用向下箭头连接，每层为圆角卡片，颜色由蓝→黄→青绿渐变，体现“由浅入深”
- 每层含：层级编号（TIER X）、中英双语标题（加粗）、功能描述（小字）、右侧对应文件名（浅灰斜体）
- 右侧三栏信息区：顶部“核心思想”蓝底白字；中部“Token 效率”黄底黑字，含具体数值对比；底部“适用场景”青绿底白字，文字左对齐
- 数值（~50 / ~500 / ~2000 tokens）用等宽字体+波浪号，符合工程文档习惯

**飞书风文字提取**：
```
渐进式披露 图书馆模型：三层加载结构

TIER 1  
索引层 / Index  
极简目录，始终加载，仅包含章节名称与简介  
index.md  

↓  

TIER 2  
章节层 / Chapter  
按需读取特定章节，包含详细规则与示例  
ch-N.md  

↓  

TIER 3  
附录层 / Appendix  
深度参考资料，仅在特定查询时读取  
appendix/  

【核心思想】  
如同图书馆：先看目录，再找书架，按需取书。不一次性加载全部内容  

【Token 效率】  
索引层仅需数十 Token，只在需要时读取完整章节，节省 90% 以上消耗  
index.md: ~50 tokens  
chapter: ~500 tokens  
appendix: ~2000 tokens  

【适用场景】  
大型文档、财务分析、合规手册等需要深度知识但使用频率低的复杂技能
```

---

### **图片 12：Token 对比：内联 vs 渐进式**
**布局与样式**：
- 主标题大号白字居中，“内联 vs 渐进式”用空格分隔，视觉平衡
- 中央双列对比表：左“全量加载”（蓝标题），右“渐进式披露”（黄标题），顶部有细线分隔
- 四行规模分类（小型→超大型），每行左侧文字 + 右侧两个色块数值（蓝/黄），超大型行末尾加红色△警示图标
- 底部绿色横幅为结论摘要，关键数字“95%+ Token”用亮绿高亮，句末加粗强调“最优选择”

**飞书风文字提取**：
```
Token 对比：内联 vs 渐进式

规模                全量加载             渐进式披露  
小型文档 (~100行)    ~800 tokens         ~800 tokens  
中型文档 (~500行)    ~4,000 tokens       ~200 tokens  
大型文档 (~2000行)   ~16,000 tokens      ~500 tokens  
超大型 (~5000行)     ~40,000 tokens △    ~800 tokens  

小型文档（≤500行）无需渐进式，直接内联更简单；  
超过 500 行后，渐进式可节省 95%+ Token，是大型知识库的最优选择
```

---

### **图片 13：description Token 预算**
**布局与样式**：
- 主标题“description Token 预算”，白字加粗，“description”与“预算”分色（白+黄）
- 中央核心规则框：`description 字段 ≤ 100 tokens`，其中“100”用超大黄色数字突出，下方小字说明其重要性
- 左下“预算分配参考”三栏卡片：蓝/黄/青绿底，字段名加粗，数值右对齐（~40 / ~35 / ~25 tokens）
- 右下“三条写作技巧”编号列表，动词短语（用/列举/说明）前置，关键术语（如 `git commit`）黄底高亮
- 底部红色警告框带⚠️图标，明确阈值边界与风险

**飞书风文字提取**：
```
description Token 预算

description 字段 ≤ 100 tokens  
语义匹配的唯一依据，写好它等于写好触发逻辑  

【预算分配参考】  
核心功能        ~40 tokens  
触发场景        ~35 tokens  
关键词          ~25 tokens  

【三条写作技巧】  
1️⃣ 用动宾短语：分析变更、生成提交  
2️⃣ 列举触发词：git commit, 提交代码  
3️⃣ 说明边界：仅用于 git 操作  

⚠️ description 超过 100 tokens 后匹配准确率下降；低于 20 tokens 则过于模糊，容易漏触发或误触发
```

---

### **图片 14：双通道触发机制**
**布局与样式**：
- 左侧大标题“双通道触发机制”，副标题 `[显式命令 · 语义匹配]` 黄色小字
- 双栏并列流程图：CHANNEL A（蓝底）与 CHANNEL B（黄底），每栏含三阶段：用户输入 → 匹配逻辑 → 结果
- 用户输入框用代码块样式（`/smart-commit` / `"帮我提交这些改动"`），模拟真实交互
- 匹配逻辑区用灰色小字标注技术原理（“精确匹配 name 字段” / “语义匹配 description 字段”）
- 底部补充说明：`user-invocable: true` 与 `description 质量决定匹配准确率` 分别对应两通道，形成闭环

**飞书风文字提取**：
```
双通道触发机制  
[显式命令 · 语义匹配]

显式命令触发（CHANNEL A）  
用户输入  
  /smart-commit  

匹配逻辑  
  精确匹配 name 字段  

结果  
  注入 SKILL.md 内容  
  执行技能指令  

需要 user-invocable: true  

语义匹配触发（CHANNEL B）  
用户输入  
  “帮我提交这些改动”  

匹配逻辑  
  语义匹配 description 字段  
  置信度超过阈值后触发  

结果  
  自动激活技能  
  无需显式命令  

description 质量决定匹配准确率  
显式命令直接精确，语义匹配更智能。选择触发模式，平衡精度与便利
```

---

### **图片 15：description 是技能的灵魂**
**布局与样式**：
- 主标题大号白字，“description 是技能的灵魂数”中“灵魂”二字加粗强化
- 中央公式：`好的 description = 触发词 + 功能 + 边界`，三要素用黄色高亮
- 三栏对比：左（✗ 过于模糊）、中（✗ 过于冗长）、右（✓ 恰到好处），用红×/绿✓图标标识状态
- 每栏含：问题示例（引号包裹）、后果说明（小字）、优化建议（绿色高亮）
- 右栏“恰到好处”案例完整展示理想结构，含触发词列表（提交、commit、git），并标注 token 估算（约 40 tokens）

**飞书风文字提取**：
```
description 是技能的灵魂

好的 description = 触发词 + 功能 + 边界  

✗ 过于模糊  
“帮助处理 git 操作”  
→ 无功能描述，无触发场景，模型无法判断何时激活  

✗ 过于冗长  
“当用户想要提交代码时，分析所有改动，考虑代码风格，生成符合 Conventional Commits 规范的提交消息……”  
→ 超过 100 tokens，匹配精度下降，细节属于 SKILL.md 正文  

✓ 恰到好处  
“分析 git 变更，生成符合 Conventional Commits 规范的提交消息。触发词：提交、commit、git”  
→ 明确功能 + 触发词 + 规范边界，约 40 tokens
```

---

### **图片 16：三步黄金策略**
**布局与样式**：
- 主标题“三步黄金策略”，白字加粗，居中
- 三列等宽卡片：01/02/03 编号，标题色块区分（蓝/黄/紫），每卡含步骤名、说明、示例列表
- 示例项用引号+蓝色高亮（如 `"分析 git 变更"`），体现可操作性
- 底部“黄金公式”横幅：公式用方括号+等号结构，关键范围“40–80 tokens”用蓝色加粗，强调最优区间
- “不适用于”栏用紫色标题，列出明确排除场景，避免误用

**飞书风文字提取**：
```
三步黄金策略

01 写什么  
动宾短语描述核心功能，精确说明 Skill 能做什么  
• “分析 git 变更”  
• “生成测试报告”  
• “审查代码质量”  

02 何时触发  
列出自然语言触发词，覆盖用户可能的表述方式  
• “触发词：提交、commit”  
• “当用户需要 git 操作”  
• “用于 PR 合并前”  

03 不适用于  
明确排除边界，防止误触发，提高精准度  
• “不适用于代码审查”  
• “仅限 git 操作场景”  
• “不处理部署流程”  

黄金公式：[功能动词 + 对象] + [触发场景] + [边界排除] = 完美 description  
控制在 40–80 tokens 之间，语义密度最高
```

---

### **图片 17：触发失败 vs 触发过度**
**布局与样式**：
- 主标题“触发失败 vs 触发过度”，白字加粗，双栏对比结构
- 左栏“触发失败 / Under-trigger”：红色标题，56% 用超大红字突出，原因归为“description 过于模糊”
- 右栏“触发过度 / Over-trigger”：橙色标题，“噪音”二字加粗，引用用户原话增强真实感
- 每栏下方用 → 符号列出连锁后果（3条），逻辑递进
- 底部“验收标准”独立模块：圆形图标（●/○/✧）表示三种状态（意图为100% / 闲聊为0% / 模糊需确认），清晰定义成功边界

**飞书风文字提取**：
```
触发失败 vs 触发过度

触发失败 / Under-trigger  
56%  
由 description 过于模糊导致  

“用户说了提交代码，Skill 没有激活，Claude 徒手操作 git”  
→ 用户不知道 Skill 存在  
→ 丢失标准化的提交格式  
→ 需要手动重复操作  

触发过度 / Over-trigger  
噪音  
“用户只是问了一句 git 是什么，Skill 就开始执行提交流程”  
→ 打断用户正常对话  
→ 浪费大量 Token  
→ 用户体验极差  

【验收标准（黄金区间）】  
● 意图明确时 100% 触发  
○ 闲聊/问答时 0% 触发  
✧ 模糊意图时询问确认
```

---

### **图片 18：参考型 vs 任务型**
**布局与样式**：
- 主标题“参考型 vs 任务型”，白字加粗
- 双栏对比：左“参考型 Skill”（蓝标题），右“任务型 Skill”（黄标题）
- 每栏含：类型定义、三大特性（带图标：□/Q/Ø）、示例、YAML 配置片段（`allowed-tools`）
- 特性图标语义明确：□=只读权限，Q=提供知识，Ø=安全关
- YAML 配置用代码块格式，工具列表用 `[Read]` / `[Bash, Read]` 体现权限差异
- 底部补充操作差异（只读取 vs 执行+修改），强化行为边界

**飞书风文字提取**：
```
参考型 vs 任务型

参考型 Skill  
Reference Skill  
□ 只读权限，不执行写操作  
Q 提供标准、规范、知识库  
Ø 安全把关，不产生副作用  
示例：代码审查 Skill  

allowed-tools: [Read]  
• 只读取文件，不修改  
• 输出审查报告  

任务型 Skill  
Task Skill  
⚙ 执行具体动作，产生实际效果  
🔧 调用 Bash、Edit、Write 等工具  
◉ 完成端到端的工作流  
示例：智能提交 Skill  

allowed-tools: [Bash, Read]  
• 执行 git add/commit  
• 修改文件、创建分支
```

---

### **图片 19：最坏情况测试法**
**布局与样式**：
- 主标题“最坏情况测试法”，白字加粗
- 顶部黄色提示框：“如果 Claude 完全误解了我的意图…”，引导用户代入极端场景
- 下方双路径决策树：左“感到紧张？”→ 任务型；右“无关痛痒？”→ 参考型，箭头用 ↗/↘ 指向对应卡片
- 两卡片颜色区分（黄/蓝），内容聚焦“副作用”与“影响范围”
- 底部“记忆口诀”横幅：用黄色关键词（后悔操作 / 最小权限 / 只读权限）总结原则，便于记忆复用

**飞书风文字提取**：
```
最坏情况测试法

如果 Claude 完全误解了我的意图…  
想象最坏的情况，选择合适的类型  

↗ 感到紧张？  
→ 任务型  
操作会产生副作用，需要精确权限控制和步骤确认  

↘ 无关痛痒？  
→ 参考型  
纯知识输出，误解最多输出一份错误报告，不影响系统  

【记忆口诀】  
会产生“后悔操作”的用任务型 + 最小权限；  
只输出文字的用参考型 + 只读权限
```

---

### **图片 20：路由器思维 vs 仓库存思维**
**布局与样式**：
- 主标题“路由器思维 vs 仓库存思维”，白字居中
- 左栏“仓库存思维”：红×图标 + 红色标题，列出9个知识域标签（代码风格/Git规范等），下方总结痛点：“所有知识塞进一个 SKILL.md，每次都全量加载，浪费严重”
- 右栏“路由器思维”：绿✓图标 + 黄色标题，中央“路由器 SKILL”圆形徽章，下方四技能按钮（带图标：✏️/🔍/⊕/📞），体现模块化路由
- 底部结论句：“每个子技能独立、精准、按需加载”，呼应前文“渐进式披露”理念
- 整体构成强烈认知冲突→解决方案的说服路径

**飞书风文字提取**：
```
路由器思维 vs 仓库存思维

✗ 仓库存思维  
• 代码风格　• Git 规范　• 测试规则  
• 部署流程　• 安全扫描　• 文档生成  
• 代码审查　• 性能优化　• …  

所有知识塞进一个 SKILL.md  
每次都全量加载，浪费严重  

✓ 路由器思维  
　　　　路由器  
　　　　SKILL  
↓ 按需路由  

✏️ smart-commit　🔍 code-review  
⊕ financial　　　📞 deploy  

每个子技能独立、精准、按需加载
```

---

### **图片 21：契约式 description + 500行原则**
**布局与样式**：
- 主标题居中，“契约式 description + 500行原则”中“契约式”与“500行”用黄色高亮
- 左侧“对比”区：上下两卡片，上红×（反例）、下绿✓（正例），形成强烈视觉对比；关键术语如“审查、review、代码检查”用绿色高亮
- 右侧“500行原则”区：黑底黄字标题，四条规则垂直排列，核心数字“500”加粗+黄色突出
- 底部流程图：左“重构前”（红底）→ 箭头 → 右“重构后”（绿底），展示从单文件（800行）到三独立 Skill 的演进
- 底部结论句用小号灰字总结方法论：“职责混合 → 拆分为独立 Skill，提高触发精准度”

**飞书风文字提取**：
```
契约式 description + 500行原则

【对比】  
✗ “帮助处理代码”  
模糊，无边界，无触发词  

✓ “审查 PR 代码质量，检查安全漏洞和性能问题。用于 PR 合并前。触发词：审查、review、代码检查”  
契约式：功能 + 边界 + 触发词  

【500行原则】  
单个 SKILL.md 超过 500 行，应当拆分  
• 一个 Skill 一个职责  
• 多职责 → 多 Skill  
• 用路由器 Skill 统一调度  

【重构前】  
dev-tools/  
SKILL.md  
(800行)  

→  

【重构后】  
smart-commit/  
code-review/  
test-runner/  

职责混合 → 拆分为独立 Skill，提高触发精准度
```

---

### **图片 22：allowed-tools 四种模板**
**布局与样式**：
- 主标题“allowed-tools 四种模板”，白字加粗
- 四宫格布局：左上（审计型）、右上（生成型）、左下（分析型）、右下（执行型），每格含图标+标题+YAML片段+说明
- 图标语义化：📋 审计 / ✏️ 生成 / 🔍 分析 / ⚙ 执行
- YAML 片段严格保留缩进与格式（`- Read` / `- Bash(git log:*)`），关键权限限制（如 `禁止 git push`）在底部统一说明
- 底部补充技术细节：“冒号加星号语法”解释 `git status:*` 的通配含义，体现工程严谨性

**飞书风文字提取**：
```
allowed-tools 四种模板

📋 审计型  
allowed-tools:  
- Read  
- Glob  
只读权限，代码审查、文档分析，零副作用  

✏️ 生成型  
allowed-tools:  
- Write  
- Edit  
- Read  
读取后生成或修改文件，代码生成、重构  

🔍 分析型  
allowed-tools:  
- Read  
- Bash(git log:*)  
- Bash(git diff:*)  
读取 + 有限 Bash，分析代码历史和变更  

⚙ 执行型  
allowed-tools:  
- Bash(git add:*)  
- Bash(git commit:*)  
- Read  
精确 Bash 前缀，最小权限执行 git 操作  

※ Bash(git status:*) — 前缀匹配  
冒号加星号语法：允许 git status 及所有参数，但禁止 git push 等危险操作
```

---

### **图片 23：Bash 精细化权限控制**
**布局与样式**：
- 主标题“Bash 精细化权限控制”，白字加粗
- 左侧“精准前缀匹配”三卡片：绿✓图标，每项含具体 `Bash(...)` 语法 + 权限说明（✅ 允许... 禁止...）
- 右侧“危险：Bash(*)”红底黑字警告框：骷髅图标 + “放弃所有防线”大字，列出4类高危后果（攻击窃密、删库、植后门、泄敏信）
- 底部结论句用黄色强调“最小权限原则”，与左侧实践形成“正例 vs 反例”强对比
- 所有 Bash 语法保留括号、冒号、星号原貌，体现安全设计细节

**飞书风文字提取**：
```
Bash 精细化权限控制

✅ 精准前缀匹配  
Bash(git status:*)  
✓ 允许查看状态，禁止所有写操作  

Bash(git add:*)  
✓ 允许暂存文件，禁止 push/reset  

Bash(git commit:*)  
✓ 仅允许提交，精确控制  

⚠ 危险：Bash(*)  
Bash(*) = 允许执行任意 Shell 命令  

※ 提示注入攻击窃取密钥  
※ 删除生产数据库  
※ 植入后门脚本  
※ 向外网发送敏感信息  

遵循最小权限原则：只授予完成任务所需的最小权限
```

---

### **图片 24：参数传递 + 动态上下文注入**
**布局与样式**：
- 主标题“参数传递 + 动态上下文注入”，白字加粗
- 左侧“$ARGUMENTS”区：展示静态参数注入语法（`/skill "feat: 新增登录"` → `$ARGUMENTS = "feat: 新增登录"`），用箭头表示映射关系
- 右侧“动态注入”区：标题“!`命令`”用黄色感叹号+反引号强调，展示 `!` 前缀触发 Shell 执行，结果自动注入 Prompt
- 底部“效果对比”表格：三行维度（上下文来源 / 首次响应速度 / 适用场景），左右列对比“静态 $ARGUMENTS” vs “动态注入 !`cmd`”
- 关键优势“速度快三倍”用黄色标注，体现性能收益

**飞书风文字提取**：
```
参数传递 + 动态上下文注入

$ARGUMENTS  
$ARGUMENTS[0]  

/skill "feat: 新增登录"  
→ $ARGUMENTS = "feat: 新增登录"  
→ $ARGUMENTS[0] = "feat"  

运行时传入参数，让用户自定义 Skill 的行为  

动态注入  
!`命令`  

当前状态：!`git status`  
变更统计：!`git diff --stat`  
→ Claude 首次响应即可行动  

发送前在 Shell 执行，结果内联到 Prompt，无需额外工具调用  

【效果对比】  
维度          静态 $ARGUMENTS         动态注入 !`cmd`  
上下文来源    用户手动输入           Shell 自动采集  
首次响应速度  需要工具调用           直接行动，速度快 50%  
适用场景      需要用户指定参数       可自动获取的环境数据
```

---

### **图片 25：作用域优先级 + 三类实战 Skill**
**布局与样式**：
- 主标题“作用域优先级 + 三类实战 Skill”，白字加粗
- 左侧“优先级（高 → 低）”三阶列表：项目级（1）、用户级（2）、企业级（3），编号用大号数字+颜色区分，路径用代码字体
- 右侧三 Skill 实例卡片：按类型分组（参考型/任务型/复合型），每卡含 Skill 名、功能描述、allowed-tools 配置
- 底部注释说明“同名 Skill 按优先级解析”，并给出 Git 版本控制建议，体现工程落地细节
- “复合型”卡片用紫色标题，强调其为“渐进式三层架构”综合应用

**飞书风文字提取**：
```
作用域优先级 + 三类实战 Skill

【优先级（高 → 低）】  
1 项目级  
  .claude/skills/  
  仅当前项目生效  

2 用户级  
  ~/.claude/skills/  
  个人所有项目  

3 企业级  
  /etc/claude/skills/  
  全员默认生效  

同名 Skill 按优先级解析，项目级最高。建议纳入 Git 版本控制，团队成员克隆后自动生效  

【实战 Skill】  
📌 参考型  
code-review  
只读权限，读取代码文件，输出质量审查报告。  
allowed-tools: [Read]  

🎯 任务型  
smart-commit  
动态注入 git 状态，生成规范提交。  
allowed-tools: [Bash(git:*), Read]  

🧩 复合型  
financial-analysis  
渐进式三层架构，路由器设计 + 执行脚本，完整企业级 Skill 范例
```

---

### **图片 26：下章预告**
**布局与样式**：
- 顶部小标签 `[ 下章预告 ]` 黄色小字，居中轻量提示
- 主标题“Hooks 工程实践”大号白字，居中突出
- 中央深灰圆角卡片：左为紫色波形图标（象征事件流），右为紫色箭头（→），形成“内容预告”动线
- 卡片内三行描述：首行为章节主题（Hooks · 事件驱动自动化），次行为机制说明（工具调用前后自动触发的钩子机制），末行为价值总结（安全、可靠）
- 整体极简，留白充足，符合飞书“预告页”设计惯例——不透露细节，只激发期待

**飞书风文字提取**：
```
[ 下章预告 ]

Hooks 工程实践

NEXT · CHAPTER 04  
Hooks · 事件驱动自动化  

工具调用前后自动触发的钩子机制  
实现代码格式化、安全检查、规范强制执行  
让 AI 工作流更安全、更可靠
```

---