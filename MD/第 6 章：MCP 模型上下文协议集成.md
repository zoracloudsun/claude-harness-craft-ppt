
### **图片 1**

#### ▶ 视觉要素分析：
- **背景**：深黑色底 + 浅灰细密网格线（科技感、工程感）。
- **顶部居中**：一个蓝灰色圆角矩形标签，内含小号无衬线体英文 `CHAPTER · 06`，字体为浅蓝白，带轻微发光效果。
- **主标题**：两行居中大字。
  - 第一行：`Claude Code Harness` —— 米白色粗体无衬线体（类似 SF Pro Display Bold），字号最大，有轻微外发光。
  - 第二行：`核心要点解析指南` —— 亮金色粗体中文（思源黑体 Bold 或阿里普惠体 Bold），字号略小于上行，同样带发光效果；“MCP”三字母以半透明水印形式嵌入“要点”二字之间，呈圆形徽章状。
- **分隔线**：一条短而粗的金色水平线，居中，两端略收窄。
- **副标题**：`第 6 章：MCP 模型上下文协议集成` —— 亮金色中等字号，加粗，居中。
- **底部小字**：`Model Context Protocol Integration` —— 浅灰细体英文，全大写，居中，作为英文注释，透明度较低，形成层次感。
- **整体风格**：高对比、强聚焦、电影级片头感；文字排布严格居中对称，强调章节权威性与技术深度。

#### ▶ 飞书风文字提取：
```
CHAPTER · 06

Claude Code Harness
核心要点解析指南  
（MCP）

第 6 章：MCP 模型上下文协议集成  
Model Context Protocol Integration
```

> ✅ 飞书风体现：标题层级分明（主/副/注释）、关键词高亮（MCP）、中英对照、无冗余装饰，信息密度高且易扫读。

---

### **图片 2**

#### ▶ 视觉要素分析：
- **布局**：左右双栏结构。左栏为“第五章回顾”，右栏为“本章重点”。
- **左栏**：
  - 顶部紫色横条标签：`第五章回顾`（浅紫文字，深紫底）。
  - 标题 `上一章掌握的核心`：白色粗体，左对齐。
  - 四个深灰圆角卡片，垂直排列，每项前有黄色实心圆点（•）作为项目符号。
  - 文字为白色常规体，无衬线。
- **右栏**：
  - 顶部金黄横条标签：`本章重点`（深褐底+金黄字）。
  - 主体为一个大圆角深棕渐变卡片，内含：
    - 顶部大号金黄 `MCP` 字母；
    - 下方白色粗体 `模型上下文协议`；
    - 两条细金线分隔；
    - 两行白色说明文字（左对齐）；
    - 底部小字灰色 `AI 集成混乱的终结方案`。
  - 整体采用“黄金焦点”设计：MCP 为视觉锚点，其余为支撑信息。
- **风格**：信息对比强烈（左回顾/右前瞻），右栏卡片使用微妙渐变与阴影增强立体感，符合飞书“多维表格卡片式”呈现逻辑。

#### ▶ 飞书风文字提取：
```
【第五章回顾】
上一章掌握的核心
• 17 个事件拦截点  
• 6 层配置作用域  
• 三道防线机制  
• 代码即法律  

【本章重点】
MCP  
模型上下文协议  

让 AI 安全、标准地  
接入企业数据系统  

→ AI 集成混乱的终结方案
```

> ✅ 飞书风体现：模块化分组（【】标签）、项目符号统一、关键术语独立成行、箭头引导结论，高度适配飞书文档的“要点摘要”区块。

---

### **图片 3**

#### ▶ 视觉要素分析：
- **顶部居中**：黄色标题 `[ 真实场景 ]` + 大号黄字 `一个看似简单的需求`，营造叙事感。
- **主体**：左右两个深色圆角卡片，中间用红色“隔离”标签+竖线分隔，象征物理/网络隔离。
  - 左卡（蓝调）：角色 `产品经理`（人像图标 + 蓝字），场景 `技术评审会议上`；需求框内引号文本，关键词高亮（`Claude`, `Jira`, `P1 级 bug`）。
  - 右卡（黄调）：角色 `萌新·开发工程师`（灯泡图标 + 黄字），场景 `心中疑问`；问题列表分行，关键词着色（`Jira`紫、`Claude`黄、`云`绿）。
- **色彩语义**：蓝=业务侧/需求方，黄=技术侧/执行方；关键词颜色区分实体类型（系统/工具/AI）。
- **风格**：对话剧式场景还原，强调“需求简单但实现复杂”的冲突感，是典型的飞书“用户故事”呈现手法。

#### ▶ 飞书风文字提取：
```
[ 真实场景 ]
一个看似简单的需求

🔹 产品经理｜技术评审会议上  
“让 Claude 分析 Jira 中上周所有 P1 级 bug 的分布规律”

🔸 萌新·开发工程师｜心中疑问  
Jira 在公司内网  
Claude 在云端  
两个世界 — 怎样互通？
```

> ✅ 飞书风体现：角色标签+场景标注、引号需求原话、符号区分视角（🔹/🔸）、关键词着色（非强制但飞书支持富文本高亮），极似飞书妙记中的“多方视角摘要”。

---

### **图片 4**

#### ▶ 视觉要素分析：
- **顶部标题**：`传统方案 vs MCP`，白+金配色，“vs”为金黄粗体，突出对比。
- **副标题**：小字提问 `如何让 AI 安全接入企业数据？`，居中，浅灰。
- **三栏并列**：从左到右为方案01→02→03，颜色依次为红→蓝→金（代表问题→过渡→推荐）。
  - 每栏顶部有图标+编号标签（⊕ / <> / ⇄），右上角“推荐”标签仅方案03有（黄底黑字）。
  - 方案01（红）：标题 `手动导出粘贴`，流程箭头 `导出 Excel → 复制 → 粘贴到对话框`，下方红底白字痛点总结。
  - 方案02（蓝）：标题 `脚本抓取投喂`，描述 `为每个数据源写专属适配器`，下方蓝底白字成本说明。
  - 方案03（金）：标题 `MCP 协议`，核心价值 `统一标准 · 即插即用`，下方金底黑字三大优势：`安全 · 标准 · 一次接入`。
- **风格**：典型飞书“方案对比表”视觉语言——颜色编码状态、图标辅助理解、痛点/优势分层呈现，适合插入多维表格或文档决策矩阵。

#### ▶ 飞书风文字提取：
```
传统方案 vs MCP  
如何让 AI 安全接入企业数据？

📌 方案 01｜手动导出粘贴  
导出 Excel → 复制 → 粘贴到对话框  
⚠️ 效率低 · 数据陈旧 · 无法自动化  

🔧 方案 02｜脚本抓取投喂  
为每个数据源写专属适配器  
⚠️ 维护成本高 · 重复造轮子  

✅ 方案 03｜MCP 协议 【推荐】  
统一标准 · 即插即用  
✓ 安全 · 标准 · 一次接入
```

> ✅ 飞书风体现：状态符号（📌/🔧/✅）、流程箭头、警告/勾选图标、推荐标识，完全复刻飞书“方案评估”模板。

---

### **图片 5**

#### ▶ 视觉要素分析：
- **主标题**：`USB-C 的故事`，白粗体；副标题 `[ 从混乱专用线 → 统一标准 ]`，浅灰小字，括号强调演进路径。
- **左右对比结构**：
  - 左“之前”：暗红底卡片，标题 `每台设备一根专用线`，列举三种接口类型（Micro-USB / Lightning / USB-A...），底部红字吐槽 `出差恨不得带 6~7 根`。
  - 右“之后”：青绿渐变底卡片，标题 `一根 USB-C 走天下`，中央大按钮式 `USB-C` logo，下方两行绿色勾选项（✓ 手机平板笔记本统一 / ✓ 充电数据视频一根搞定）。
- **底部金底黑字结论**：`MCP = AI 工具集成领域的 USB-C`，用类比强化认知。
- **风格**：强类比教学法，借大众熟知的硬件标准化案例解释抽象协议价值，是飞书知识库中高频使用的“认知嫁接”技巧。

#### ▶ 飞书风文字提取：
```
USB-C 的故事  
[ 从混乱专用线 → 统一标准 ]

🔴 之前｜每台设备一根专用线  
○ Micro-USB (Android 旧机)  
○ Lightning (iPhone)  
○ USB-A / Mini-USB / DC 圆头…  
→ 出差恨不得带 6~7 根  

🟢 之后｜一根 USB-C 走天下  
✓ 手机、平板、笔记本统一  
✓ 充电、数据、视频一根搞定  

💡 MCP = AI 工具集成领域的 USB-C
```

> ✅ 飞书风体现：颜色分区（红/绿）、符号列表（○/✓）、箭头引导结论、类比句式（💡），高度契合飞书“知识卡片”传播逻辑。

---

### **图片 6**

#### ▶ 视觉要素分析：
- **顶部标题**：`M × N 问题`，红白撞色（M×N 红，问题白），副标题 `复杂度炸裂的根源` 浅灰。
- **左图**：深红底示意图，标题 `USB-C 之前 · M 设备 × N 外设`，左侧4个设备节点（笔记本/手机/相机/平板），右侧4个外设节点（充电器/显示器/U盘/读卡器），中间密集交叉连线（红紫线），直观表现组合爆炸。
- **右栏**：上下两个卡片，形成“问题→解法”对比：
  - 上卡（红底）：`复杂方案` + `M × N`（大红字）+ `每一对都要专属适配`
  - 下卡（绿底）：`标准化之后` + `M + N`（大绿字）+ `每端只实现一次标准`
- **箭头**：黄色向下箭头连接两卡，表示演进。
- **风格**：用数学符号（M×N vs M+N）量化复杂度，配合可视化连线图，是飞书技术文档中“用图解代替公式”的典范。

#### ▶ 飞书风文字提取：
```
M × N 问题  
复杂度炸裂的根源

📉 问题本质：  
USB-C 之前 · M 设备 × N 外设  
→ 每一对都要专属适配  

📈 标准化之后：  
M + N  
→ 每端只实现一次标准
```

> ✅ 飞书风体现：箭头符号（→）、趋势符号（📉/📈）、数学表达精炼、无图也能理解逻辑，符合飞书“一页讲清一个概念”的原则。

---

### **图片 7**

#### ▶ 视觉要素分析：
- **标题**：`MCP 出现前 · 适配器地狱`，红字+黑底，副标题 `每一对 AI × 数据源 都需要独立适配器`。
- **三栏结构**：
  - 左：`5 个 AI 客户端`（Claude/GPT-4/Gemini/Llama/Others），蓝底卡片堆叠。
  - 中：抽象“地狱”图——红色蛛网状结构，中心圆标 `500 个适配器`。
  - 右：`100 个数据源`（Jira/GitHub/Slack/Notion/DB/FS/S3/Email...），紫底卡片网格。
- **底部红底横幅**：`5 × 100 = 500 个适配器需要维护` + `这是 M × N 的地狱`，加粗强调。
- **风格**：用具体数字（5×100=500）将抽象“地狱”具象化，视觉冲击力强，属于飞书“数据震撼”型呈现。

#### ▶ 飞书风文字提取：
```
MCP 出现前 · 适配器地狱  
每一对 AI × 数据源 都需要独立适配器

▸ 5 个 AI 客户端  
  – Claude  
  – GPT-4  
  – Gemini  
  – Llama  
  – Others...

▸ 100 个数据源  
  – Jira, GitHub  
  – Slack, Notion  
  – DB, FS, S3, Email  
  – … 共 100+ 种

❗ 5 × 100 = 500 个适配器需要维护  
→ 这是 M × N 的地狱
```

> ✅ 飞书风体现：分项符号（▸）、缩进列表、感叹号警示、箭头归因，完全匹配飞书故障复盘/技术债梳理的表述习惯。

---

### **图片 8**

#### ▶ 视觉要素分析：
- **主标题**：`MCP 标准化方案`，金黄粗体；副标题 `定义统一协议 · 即插即用`，浅灰。
- **三段式流程图**：
  - 左：`之前 · M × N` → 红底大字 `500` → 小字 `个独立适配器` → 底部注 `5 客户端 × 100 数据源`
  - 中：黄色椭圆 `MCP` → 向下箭头 → `统一协议层`
  - 右：`之后 · M + N` → 绿底大字 `105` → 小字 `次标准实现` → 底部注 `每端只实现一次协议`
- **底部横幅**：深灰底金边，`工作量降低 79%`（79% 金黄加粗）+ `– 这就是标准化的复利`
- **风格**：数据驱动决策，用“500→105”和“79%”量化收益，是飞书OKR复盘/技术投入产出比分析的标准模板。

#### ▶ 飞书风文字提取：
```
MCP 标准化方案  
定义统一协议 · 即插即用

← 之前 · M × N  
500 个独立适配器  
（5 客户端 × 100 数据源）

↓  
MCP｜统一协议层  

→ 之后 · M + N  
105 次标准实现  
（每端只实现一次协议）

📊 工作量降低 79%  
– 这就是标准化的复利
```

> ✅ 飞书风体现：箭头流程、括号补充说明、统计指标突出（📊）、结论短句收尾，精准复刻飞书“数据结论页”。

---

### **图片 9**

#### ▶ 视觉要素分析：
- **主标题**：`从内部项目到 行业标准`，白+金配色；副标题 `仅用 13 个月走完一个协议的成长之路`。
- **时间轴**：水平线贯穿，三个里程碑节点，按时间从左到右：
  - 2024.11：黄底标签，星形图标，卡片 `Anthropic 发布` + 两行说明。
  - 2025.03：蓝底标签，对勾图标，卡片 `OpenAI 采纳` + 两行说明。
  - 2025.12：绿底标签，时钟图标，卡片 `Linux 基金会` + 两行说明。
- **颜色语义**：黄=启动，蓝=关键采纳，绿=生态成熟；图标分别代表“发布”“验证”“治理”。
- **风格**：典型飞书“产品路线图”或“协议演进时间线”，用于向高管/社区展示成长势能。

#### ▶ 飞书风文字提取：
```
从内部项目到 行业标准  
仅用 13 个月走完一个协议的成长之路

🕒 2024.11｜Anthropic 发布  
• MCP 协议正式开源  
• 内部项目首度亮相  

✅ 2025.03｜OpenAI 采纳  
• 深度集成至 ChatGPT  
• 桌面应用  

🌐 2025.12｜Linux 基金会  
• 捐赠至 Agentic AI Foundation
```

> ✅ 飞书风体现：时间标签+图标、项目符号列表、动词开头（发布/采纳/捐赠）、省略冗余主语，高度凝练，适配飞书时间轴组件。

---

### **图片 10**

#### ▶ 视觉要素分析：
- **主标题**：`MCP 生态：爆发式增长`，金黄粗体。
- **三栏数据看板**，等宽并列，每栏含：
  - 顶部图标（下载/电脑/链接）
  - 超大数字（9700万+ / 10,000+ / 75+）
  - 小标题（月度 SDK 下载 / 公开 MCP 服务器 / 官方连接器）
  - 两行说明文字（开发者用脚投票 / 社区贡献的能力即插即用 / 主流 SaaS 已覆盖）
- **底部居中按钮**：金底黑字 `标准化的力量`，作为总结升华。
- **风格**：纯数据驱动，采用“数字+图标+短句”三要素，是飞书仪表盘/周报中“成果速览”模块的标准样式。

#### ▶ 飞书风文字提取：
```
MCP 生态：爆发式增长

📥 9700万+  
月度 SDK 下载  
• 开发者用脚投票  
• 事实标准已形成  

🖥️ 10,000+  
公开 MCP 服务器  
• 社区贡献的能力即插即用  

🔗 75+  
官方连接器  
• 主流 SaaS 已覆盖  
• 开箱即用  

✨ 标准化的力量
```

> ✅ 飞书风体现：图标前置、数字放大、要点用 • 符号、结尾金句升华，完美复刻飞书“数据亮点卡片”模板。

---

### **图片 11**

#### ▶ 视觉要素分析：
- **顶部导航栏**：左上角为 `AI walker` Logo（蓝底白字图标 + “walker”），标题 `Claude Code Harness 核心要点解析指南`；居中为章节标题 `第6章：MCP 模型上下文协议集成`；右上角标注 `第11节 / 共32节`，体现课程进度。
- **主标题**：`客户端-服务器架构`，亮金色粗体，居中，字号大，带轻微发光，是本页核心主题。
- **主体结构**：三层垂直堆叠卡片，自上而下为“顶层→中层→底层”，用双向箭头 `↑↓` 连接，强调通信流向。
  - **顶层卡片（蓝调）**：
    - 左侧图标：电脑显示器（Client）
    - 标题：`顶层 · MCP 客户端`（蓝字）
    - 描述：`Claude Code / Claude Desktop · 发起请求、消费能力`
    - 右侧标签：`Client`（蓝底白字圆角按钮）
  - **中层卡片（黄调）**：
    - 左侧图标：代码符号 `>_`
    - 标题：`中层 · MCP 协议`（黄字）
    - 描述：`基于 JSON-RPC 2.0 · 标准化消息格式 · 双向通信`
    - 右侧标签：`Protocol`（黄底黑字圆角按钮）
  - **底层卡片（绿调）**：
    - 左侧图标：服务器机架（Servers）
    - 标题：`底层 · MCP 服务器群`（绿字）
    - 描述：`封装外部资源 · 数据库 · API · 文件系统 · SaaS 服务`
    - 右侧标签：`Servers`（绿底白字圆角按钮）
- **底部居中**：白色小字 `基于JSON-RPC 2.0通信`，作为技术底座说明。
- **整体风格**：典型的飞书“分层架构图”——用颜色区分层级（蓝=前端/用户侧，黄=协议层，绿=后端/资源侧），图标+标签+简练描述三要素完备; 箭头明确通信关系，适配飞书多维表格中的“系统设计”模块。

#### ▶ 飞书风文字提取：
```
【第6章 · 第11节】  
客户端-服务器架构

🔹 顶层 · MCP 客户端｜Client  
• Claude Code / Claude Desktop  
• 发起请求、消费能力  

🔸 中层 · MCP 协议｜Protocol  
• 基于 JSON-RPC 2.0  
• 标准化消息格式 · 双向通信  

🟢 底层 · MCP 服务器群｜Servers  
• 封装外部资源：数据库 / API / 文件系统 / SaaS 服务  

📌 底层通信协议：基于 JSON-RPC 2.0
```

> ✅ 飞书风体现：层级符号（🔹/🔸/🟢）、角色标签（｜Client/Protocol/Servers）、项目符号列表、关键协议加粗标注，完全匹配飞书“系统架构摘要卡”。

---

### **图片 12**

#### ▶ 视觉要素分析：
- **主标题**：`MCP 借鉴 LSP 的设计哲学`，金黄粗体，居中，点明核心思想来源。
- **左右双栏对比结构**：
  - **左栏（LSP）**：深蓝底，标题 `LSP` + `Language Server Protocol`，含两层子模块：
    - 上：`VSCode · Vim · IntelliJ` + `编辑器侧 · Client`
    - 下：`TypeScript · Rust · Python` + `语言工具链 · Server`
    - 底部总结：`M 编辑器 × N 语言 → M+N`（蓝字）
  - **右栏（MCP）**：深棕渐变底，标题 `MCP` + `Model Context Protocol`，对应两层：
    - 上：`Claude · ChatGPT · Cursor` + `AI 应用 · Client`
    - 下：`数据库 · SaaS · 文件 · API` + `数字世界 · Server`
    - 底部总结：`M AI × N 服务 → M+N`（金黄）
  - 两栏中间均有 `↓ 协议 ↓` 箭头，强调协议居中解耦作用。
- **底部引用框**：深褐底，双引号图标 `”`，引用语 + 署名，字体稍小但清晰。
- **风格**：经典“类比迁移”教学法，用 LSP 成熟范式映射 MCP 新范式，是飞书知识沉淀中高频使用的“模式复用”表达方式。

#### ▶ 飞书风文字提取：
```
MCP 借鉴 LSP 的设计哲学

🔍 LSP（Language Server Protocol）  
├─ Client：VSCode / Vim / IntelliJ（编辑器侧）  
├─ Server：TypeScript / Rust / Python（语言工具链）  
└→ M 编辑器 × N 语言 = M+N  

🧠 MCP（Model Context Protocol）  
├─ Client：Claude / ChatGPT / Cursor（AI 应用）  
├─ Server：数据库 / SaaS / 文件 / API（数字世界）  
└→ M AI × N 服务 = M+N  

💡 引用：  
“MCP 直接借鉴了 LSP 的设计哲学 —— 用协议消除 N×M 的集成爆炸。”  
– David Soria Parra · MCP 共同发明人
```

> ✅ 飞书风体现：树状缩进（├─/└→）、对比平行结构、引号引用规范署名、思想提炼句（💡），高度契合飞书“设计原则卡片”。

---

### **图片 13**

#### ▶ 视觉要素分析：
- **主标题**：`Tools · 工具能力`，金黄粗体；副标题 `最核心、最常用的能力`，浅灰。
- **左侧面板**：深灰圆角卡片，标题 `🔧 工具定义结构`，内嵌代码块（模拟 JSON Schema）：
  - 关键字段高亮：`"name"`（黄）、`"description"`（青）、`"parameters"`（蓝）、`"returns"`（紫）
  - 参数类型标注：`string`, `enum` 等为浅蓝
  - 底部说明句：`Claude 像调用本地函数一样调用远程能力 —— 主动决策、自动传参、自动接收返回。`
- **右侧面板**：三张垂直排列的浅色卡片，每张含图标+标题+说明：
  1. 📄 `创建 Jira 工单` → `从对话直达任务系统`
  2. 🗃️ `查询生产数据库` → `SQL 自动生成与执行`
  3. 💬 `发送 Slack 消息` → `通知/告警自动化`
- **色彩逻辑**：左技术细节（暗色代码区），右业务场景（亮色功能卡），形成“定义→应用”闭环.
- **风格**：飞书“能力说明书”标准模板——左侧放 Schema/接口定义，右侧列典型用例，常用于产品 PRD 或开发者文档。

#### ▶ 飞书风文字提取：
```
Tools · 工具能力  
最核心、最常用的能力

🔧 工具定义结构  
// MCP Tool Schema  
{  
  "name": "create_jira_issue",  
  "description": "创建 Jira 工单",  
  "parameters": {  
    project: string,  
    title: string,  
    priority: enum  
  },  
  "returns": { issue_id, url }  
}  

✅ Claude 调用模式：  
像调用本地函数一样调用远程能力  
→ 主动决策 · 自动传参 · 自动接收返回  

📌 典型能力：  
📄 创建 Jira 工单｜从对话直达任务系统  
🗃️ 查询生产数据库｜SQL 自动生成与执行  
💬 发送 Slack 消息｜通知/告警自动化
```

> ✅ 飞书风体现：代码块保留结构、关键字段着色（飞书支持）、箭头归纳行为模式、图标+短句功能卡，完全复刻飞书“能力清单”组件。

---

### **图片 14**

#### ▶ 视觉要素分析：
- **主标题**：`三类能力对比`，金黄粗体，居中。
- **三栏并列卡片**，从左到右为 `Tools` / `Resources` / `Prompts`，颜色依次为黄/蓝/绿，对应不同能力性质。
  - **Tools 卡片（黄）**：
    - 标题：`Tools｜工具`，红底标签 `有副作用 · Active`
    - 描述：`Claude 主动调用的函数，可改变外部世界状态。`
    - 典型用例：创建工单 / SQL 查询 / 发消息（左对齐列表）
  - **Resources 卡片（蓝）**：
    - 标题：`Resources｜资源`，绿底标签 `只读 · Passive`
    - 描述：`Claude 按需读取的数据源，不会修改外部状态。`
    - 典型用例：读取文件 / API 配置 / 知识库内容
  - **Prompts 卡片（绿）**：
    - 标题：`Prompts｜提示词`，紫底标签 `模板 · Reusable`
    - 描述：`用户主动触发的提示模板，参数化、可复用。`
    - 典型用例：代码评审 / 报告生成 / 翻译模板
- **底部横幅**：金底黑字 `✓ 实战中 Tools 用得最多`，带勾选图标，强调实践重点。
- **风格**：飞书“能力分类矩阵”——通过“副作用性”“主动性”“复用性”三个维度区分能力类型，是产品/工程团队对 AI 能力建模的标准方法论呈现。

#### ▶ 飞书风文字提取：
```
三类能力对比

🟡 Tools｜工具  
🏷 有副作用 · Active  
• Claude 主动调用的函数，可改变外部世界状态。  
▸ 典型用例：  
  - 创建工单  
  - SQL 查询  
  - 发消息  

🔵 Resources｜资源  
🏷 只读 · Passive  
• Claude 按需读取的数据源，不会修改外部状态。  
▸ 典型用例：  
  - 读取文件  
  - API 配置  
  - 知识库内容  

🟢 Prompts｜提示词  
🏷 模板 · Reusable  
• 用户主动触发的提示模板，参数化、可复用。  
▸ 典型用例：  
  - 代码评审  
  - 报告生成  
  - 翻译模板  

✅ 实战中 Tools 用得最多
```

> ✅ 飞书风体现：颜色标签（🟡/🔵/🟢）、属性徽章（🏷）、项目符号分级（•/▸）、结论加勾选符号，精准还原飞书“能力分类表”。

---

### **图片 15**

#### ▶ 视觉要素分析：
- **主标题**：`stdio · 本地进程通信`，金黄粗体。
- **左侧面板**：深蓝底，标题 `本地进程通信示意`，含两个进程框：
  - 上：`Claude Code`（星形图标）→ `主进程 · 父进程`
  - 下：`MCP Server`（`>_` 图标）→ `本地子进程`
  - 中间流程：`spawn 子进程` → `stdin/stdout/stderr` → `↑ JSON-RPC 消息`
- **右侧面板**：四张小卡片，2×2网格，分别强调四大优势：
  1. 🏠 `完全本地`：进程级隔离 / 不暴露端口  
  2. 🌐 `零网络`：无 TCP/HTTPS / 不依赖网络栈  
  3. ⚡ `最低延迟`：毫秒级响应 / 管道直传  
  4. 🛡️ `最高安全`：本地凭证 / 不出主机  
- **底部标签栏**：四个圆角按钮：`本地文件系统` / `SQLite / Git` / `本地开发工具` / `CLI 包装`，标明适用场景。
- **风格**：飞书“技术方案优势清单”——用图标+短句浓缩关键技术收益，适合插入技术评审文档的“方案选型依据”章节。

#### ▶ 飞书风文字提取：
```
stdio · 本地进程通信

🖥️ 本地进程通信示意  
● Claude Code（主进程 · 父进程）  
   ↓ spawn 子进程  
   stdin / stdout / stderr  
   ↑ JSON-RPC 消息  
● MCP Server（本地子进程）

🌟 四大优势：  
🏠 完全本地｜进程级隔离 · 不暴露端口  
🌐 零网络｜无 TCP/HTTPS · 不依赖网络栈  
⚡ 最低延迟｜毫秒级响应 · 管道直传  
🛡️ 最高安全｜本地凭证 · 不出主机  

✅ 适用场景：  
• 本地文件系统  
• SQLite / Git  
• 本地开发工具  
• CLI 包装
```

> ✅ 飞书风体现：流程箭头（↓/↑）、优势图标+竖线分隔、场景列表用 • 符号, 适配飞书“技术方案对比卡”。

---

### **图片 16**

#### ▶ 视觉要素分析：
- **主标题**：`HTTP · 远程服务器推荐方案`，金黄粗体。
- **左侧面板**：深棕底，标题 `远程通信流程`，含两层：
  - 上：`Claude Code`（星形图标）→ `客户端`
  - 下：`远程 MCP Server`（眼睛图标）→ `SaaS / 云函数 / K8s`
  - 中间流程：`POST /mcp` → `JSON-RPC over HTTPS · OAuth 2.0` → `Response`
- **右侧面板**：四张卡片，2×2布局，突出远程方案特性：
  1. 🌍 `跨网络`：公网/内网均可 · 无地域限制  
  2. 🔐 `OAuth 2.0`：标准授权流 · Token 安全  
  3. 💼 `企业集成`：SSO/审计兼容 · 合规友好  
  4. 🚀 `易部署`：复用现有 HTTP 基础设施  
- **底部提示条**：深灰底白字，带禁止图标 `⊘`，强调 `SSE 传输已废弃 · 旧版本兼容已移除，请使用 HTTP / stdio`
- **风格**：飞书“部署方案指南”模板——明确流程+关键特性+弃用警告，用于运维/实施侧交付文档。

#### ▶ 飞书风文字提取：
```
HTTP · 远程服务器推荐方案

🌐 远程通信流程  
● Claude Code（客户端）  
   ↓ POST /mcp  
   JSON-RPC over HTTPS + OAuth 2.0  
   ↑ Response  
● 远程 MCP Server（SaaS / 云函数 / K8s）

✅ 推荐特性：  
🌍 跨网络｜公网/内网均可 · 无地域限制  
🔐 OAuth 2.0｜标准授权流 · Token 安全  
💼 企业集成｜SSO/审计兼容 · 合规友好  
🚀 易部署｜复用现有 HTTP 基础设施  

⚠️ 注意：  
⊘ SSE 传输已废弃 · 旧版本兼容已移除  
→ 请统一使用 HTTP / stdio
```

> ✅ 飞书风体现：流程符号（↓/↑）、特性标签（🌍/🔐等）、警告符号（⚠️/⊘）、箭头指引替代方案，完全符合飞书“实施须知”卡片规范。

---

### **图片 17**

#### ▶ 视觉要素分析：
- **主标题**：`如何选择传输方式？`，金黄粗体，居中提问式引导。
- **双栏对比**：
  - **左栏（蓝色）**：`💻 本地 → stdio`
    - 三点优势：`本地进程间通信` / `零网络延迟` / `进程隔离安全`
    - 底部适用范围框：`适用：文件系统、本地数据库、CLI 工具`
  - **右栏（深棕）**：`☁️ 远程 → HTTP`
    - 三点优势：`跨网络服务调用` / `多客户端共享` / `标准 RESTful 协议`
    - 底部适用范围框：`适用：云端 API、SaaS 平台、企业内部服务`
- **底部居中横幅**：金底黑字 `本地 stdio · 远程 HTTP`，总结二元选择。
- **风格**：飞书“决策树快查表”——用场景驱动选择，避免技术细节干扰，适用于面向产品经理或初级工程师的快速指南。

#### ▶ 飞书风文字提取：
```
如何选择传输方式？

🔵 本地 → stdio  
• 本地进程间通信  
• 零网络延迟  
• 进程隔离安全  
→ 适用：文件系统 / 本地数据库 / CLI 工具  

🟤 远程 → HTTP  
• 跨网络服务调用  
• 多客户端共享  
• 标准 RESTful 协议  
→ 适用：云端 API / SaaS 平台 / 企业内部服务  

📌 决策准则：  
本地 stdio · 远程 HTTP
```

> ✅ 飞书风体现：颜色分区、箭头导向适用场景、结论短句收尾, 极简高效, 是飞书“决策速查卡”的标准形态。

---

### **图片 18**

#### ▶ 视觉要素分析：
- **主标题**：`环境变量替换机制`，金黄粗体.
- **左侧面板**：深绿圆角卡片，标题 `<> .mcp.json 配置示例`，内嵌 JSON 片段：
  - 展示 `mcpServers.postgres.env` 下的 `API_KEY` 和 `DB_HOST` 使用 `${VAR_NAME}` 与 `${VAR:-default}` 语法
  - 关键变量高亮：`${API_KEY}`（黄）、`${DB_HOST:-local}`（青）
- **右侧面板**：四张垂直卡片，解释四种替换机制：
  1. 🔄 `${VAR_NAME}` → `运行时动态替换`
  2. ⏱️ `${VAR:-default}` → `提供回退默认值`
  3. 🔒 敏感数据隔离 → `保存于本地 .env`
  4. ✅ 安全提交 Git → `配置不含明文密钥`
- **风格**：飞书“配置最佳实践”卡片——左侧给实例，右侧给规则，兼顾开发者实操与安全合规要求。

#### ▶ 飞书风文字提取：
```
环境变量替换机制

⚙️ .mcp.json 配置示例  
{  
  "mcpServers": {  
    "postgres": {  
      "command": "npx",  
      "env": {  
        "API_KEY": "${API_KEY}",     // 运行时动态替换  
        "DB_HOST": "${DB_HOST:-local}" // 默认值回退  
      }  
    }  
  }  
}

📌 四种机制：  
🔄 ${VAR_NAME}｜运行时动态替换  
⏱️ ${VAR:-default}｜提供回退默认值  
🔒 敏感数据隔离｜保存于本地 .env  
✅ 安全提交 Git｜配置不含明文密钥
```

> ✅ 飞书风体现：注释式代码（//）、机制图标+竖线说明、安全优先原则显性化，完全匹配飞书“配置规范”文档风格。

---

### **图片 19**

#### ▶ 视觉要素分析：
- **主标题**：`配置文件作用域`，金黄粗体；副标题 `位置决定生效范围`，浅灰。
- **双栏对比**：
  - **左栏（蓝）**：`📁 项目级` → 路径 `./.mcp.json`
    - 三点特性：`仅当前项目生效` / `随仓库提交共享` / `团队配置统一`
    - 典型场景框：`业务数据库、专属 API`
  - **右栏（棕）**：`🏠 用户级` → 路径 `~/.claude/.mcp.json`
    - 三点特性：`对所有项目生效` / `个人偏好沉淀` / `跨项目复用`
    - 典型场景框：`通用搜索、笔记工具`
- **布局对称**：图标（📁/🏠）、路径高亮、特性 bullet point、场景标签框，结构高度一致。
- **风格**：飞书“配置管理模型”标准图——用作用域划分解决“团队统一性”与“个人灵活性”矛盾，常见于 DevOps 或平台产品文档。

#### ▶ 飞书风文字提取：
```
配置文件作用域  
位置决定生效范围

📁 项目级｜./.mcp.json  
• 仅当前项目生效  
• 随仓库提交共享  
• 团队配置统一  
→ 典型场景：业务数据库、专属 API  

🏠 用户级｜~/.claude/.mcp.json  
• 对所有项目生效  
• 个人偏好沉淀  
• 跨项目复用  
→ 典型场景：通用搜索、笔记工具
```

> ✅ 飞书风体现：路径斜体标识、箭头引导场景、对称结构、无冗余词，是飞书“配置策略卡”的典范。

---

### **图片 20**

#### ▶ 视觉要素分析：
- **主标题**：`数据库查询实战`，金黄粗体。
- **主体**：五步水平流程图，由左至右箭头连接：
  1. 💬 `用户提问`：`“帮我查下订单...”`
  2. 🔍 `识别`：`postgres 服务`
  3. 📜 `SQL`：`SELECT * FROM ...`
  4. 🗃️ `执行`：`数据库返回`
  5. ✅ `结果`：`自然语言呈现`
- 每步为圆角卡片，配图标+标题+简短说明，颜色渐变（蓝→紫→棕→绿→红），暗示流程推进。
- **底部横幅**：深绿底金边，大字 `无需 SQL · 无需客户端`，作为价值主张总结。
- **风格**：飞书“用户旅程图”简化版——聚焦 AI 代理如何隐去技术细节，实现“自然语言→结果”闭环; 适用于产品演示或客户案例。

#### ▶ 飞书风文字提取：
```
数据库查询实战

1. 💬 用户提问  
   “帮我查下订单...”

2. 🔍 识别  
   postgres 服务

3. 📜 SQL  
   SELECT * FROM ...

4. 🗃️ 执行  
   数据库返回

5. ✅ 结果  
   自然语言呈现

✨ 无需 SQL · 无需客户端
```

> ✅ 飞书风体现：序号+图标引导步骤、引号保留用户原话、结尾金句升华, 完全契合飞书“场景化演示卡”。

---

### **图片 21**

#### ▶ 视觉要素分析：
- **顶部标题**：`⚠️ 关键安全原则`，金黄粗体，左侧配黄色三角警示图标，强调严肃性；副标题 `数据库连接必须只读`，浅灰小字居中。
- **主体结构**：左右双栏对比，左“危险”（红调），右“安全”（绿调），形成强烈视觉对立。
  - **左栏（危险）**：
    - 标题：`🚫 危险：读写账号`（红字+禁止图标）
    - 子标题：`Claude 理论上能执行：`
    - 三行深红卡片，内含 SQL 语句（`INSERT INTO ...` / `UPDATE table SET ...` / `DROP TABLE users`），字体为等宽代码体，红色高亮
    - 底部警示句：`误操作 = 数据灾难`（红字）
  - **右栏（安全）**：
    - 标题：`🛡️ 安全：只读账号`（绿字+盾牌图标）
    - 子标题：`仅允许查询动作：`
    - 三行深绿卡片，内含受限 SQL（`SELECT col FROM ...` / `WHERE / GROUP BY` / `GRANT SELECT ONLY`），绿色代码体
    - 底部原则：`最小权限原则`（白字）
- **底部横幅**：金底黑字 `Read-Only User = 最基本防护`，作为结论性金句。
- **风格**：飞书“安全红线卡”典型设计——用颜色/图标区分风险等级，代码片段具象化威胁，适用于安全培训或架构评审文档。

#### ▶ 飞书风文字提取：
```
⚠️ 关键安全原则  
数据库连接必须只读

🔴 危险：读写账号  
• Claude 理论上能执行：  
  – INSERT INTO ...  
  – UPDATE table SET ...  
  – DROP TABLE users  
→ 误操作 = 数据灾难  

🟢 安全：只读账号  
• 仅允许查询动作：  
  – SELECT col FROM ...  
  – WHERE / GROUP BY  
  – GRANT SELECT ONLY  
→ 最小权限原则  

📌 Read-Only User = 最基本防护
```

> ✅ 飞书风体现：风险色标（🔴/🟢）、代码缩进列表、箭头归因、结论金句，完全匹配飞书“安全规范卡片”。

---

### **图片 22**

#### ▶ 视觉要素分析：
- **主标题**：`自定义 MCP 服务器`，金黄粗体；副标题 `TypeScript / Python SDK`，浅灰。
- **左侧面板**：深蓝圆角代码框，标题 `<> TypeScript SDK`，内含：
  - 导入语句（`import { McpServer } from '@mcp/sdk'`）
  - 实例创建（`new McpServer({ name: 'weather-tool' })`）
  - 工具注册（`server.tool('get_weather', { description, handler })`）
  - 启动监听（`server.listen()`）
  - 关键词高亮：`name`（黄）、`description`（青）、`handler`（紫）、`fetchWeather`（蓝）
- **右侧面板**：四步垂直流程卡，编号 1–4，颜色渐变（黄→蓝→紫→绿）：
  1. `创建服务器实例` → `new McpServer(config)`
  2. `定义工具` → `server.tool(name, handler)`
  3. `处理请求` → `async handler 内业务逻辑`
  4. `返回结果` → `return data / throw error`
- **整体风格**：飞书“SDK 快速上手指南”模板——左侧给可运行代码，右侧给步骤摘要，适配开发者文档的“5分钟接入”章节。

#### ▶ 飞书风文字提取：
```
自定义 MCP 服务器  
TypeScript / Python SDK

💻 TypeScript SDK 示例  
import { McpServer } from '@mcp/sdk';  

const server = new McpServer({  
  name: 'weather-tool'  // 工具标识  
});  

server.tool('get_weather', {  
  description: '查询城市天气',  
  handler: async ({ city }) => {  
    return await fetchWeather(city); // 业务逻辑  
  }  
});  

server.listen();  // 启动服务

📌 四步开发流程：  
1️⃣ 创建服务器实例｜new McpServer(config)  
2️⃣ 定义工具｜server.tool(name, handler)  
3️⃣ 处理请求｜async handler 内业务逻辑  
4️⃣ 返回结果｜return data / throw error
```

> ✅ 飞书风体现：代码注释标注、步骤编号+图标、关键参数说明，高度还原飞书“开发者速查卡”。

---

### **图片 23**

#### ▶ 视觉要素分析：
- **主标题**：`工具设计的关键细节`，金黄粗体。
- **顶部横幅**：深棕渐变卡片，标题 `📖 Tool Description`，副标题 `是 Claude 判断何时调用的核心依据`，突出描述字段的战略地位。
- **中部三栏卡片**，水平排列，颜色区分功能维度：
  1. **左（蓝）**：`✅ 类型安全` + 图标 ✔️ → `TS 类型 / Pydantic Schema`
  2. **中（紫）**：`🛡️ 参数校验` + 盾牌 → `边界值 / 注入防护`
  3. **右（绿）**：`📄 日志规范` + 文档 → `结构化输出 / 追踪 ID`
- **底部流程条**：金底黑字 `描述 → 意图识别 → 调用决策`，用箭头串联设计逻辑链。
- **风格**：飞书“设计 Checklist”——将抽象“工具描述”拆解为可落地的三个工程保障点，适用于技术方案评审中的“质量属性”部分。

#### ▶ 飞书风文字提取：
```
工具设计的关键细节

📖 Tool Description  
是 Claude 判断何时调用的核心依据

🔹 类型安全｜TS 类型 / Pydantic Schema  
🔹 参数校验｜边界值 · 注入防护  
🔹 日志规范｜结构化输出 · 追踪 ID  

⚙️ 设计逻辑链：  
描述 → 意图识别 → 调用决策
```

> ✅ 飞书风体现：图标+短标签、逻辑链箭头、无冗余描述，精准匹配飞书“设计原则清单”。

---

### **图片 24**

#### ▶ 视觉要素分析：
- **主标题**：`[ MCP 服务器生态 ]` + `官方与社区生态`，金黄+白粗体；副标题 `Anthropic 即用服务器 + 社区繁荣的第三方集成`。
- **左右双栏结构**：
  - **左栏（官方）**：标题 `◎ Anthropic 官方服务器`，含5个工具卡片：
    - `filesystem`（文件读写、目录遍历）
    - `fetch`（HTTP 请求、网页抓取）
    - `postgres`（数据库查询、SQL 执行）
    - `memory`（跨会话持久存储）
    - `git`（版本控制、分支、提交、合并）
    - 每项配专属图标（📁/🌐/🗃️/💾/🐙）
  - **右栏（社区）**：标题 `👥 社区第三方服务器`，含4个知名项目：
    - `GitHub`（🌐图标）
    - `Notion`（📝图标）
    - `Sentry`（⚠️图标）
    - `Slack`（💬图标）
    - 底部提示：`更多 100+ 社区服务器持续涌现...`
- **风格**：飞书“生态全景图”——官方能力保底，社区扩展边界，用图标+短描述快速建立认知，常见于产品发布会或生态合作页。

#### ▶ 飞书风文字提取：
```
[ MCP 服务器生态 ]  
官方与社区生态  
Anthropic 即用服务器 + 社区繁荣的第三方集成

◎ 官方服务器  
📁 filesystem｜文件读写 · 目录遍历  
🌐 fetch｜HTTP 请求 · 网页抓取  
🗃️ postgres｜数据库查询 · SQL 执行  
💾 memory｜跨会话持久存储  
🐙 git｜版本控制 · 分支 · 提交 · 合并  

👥 社区第三方服务器  
🌐 GitHub｜代码托管与协作  
📝 Notion｜知识库与文档  
⚠️ Sentry｜错误监控与告警  
💬 Slack｜消息通知与自动化  
→ 更多 100+ 社区服务器持续涌现...
```

> ✅ 飞书风体现：图标前缀、功能短语用·分隔、社区部分用→引导扩展，完全复刻飞书“生态合作伙伴卡”。

---

### **图片 25**

#### ▶ 视觉要素分析：
- **主标题**：`[ 全栈集成 ]` + `全栈开发者的 MCP 工具箱`，白+金配色；副标题 `单个 Claude 会话 · 无缝完成复杂工作流`。
- **中心焦点**：金色圆形徽章 `Claude` + `统一调度`，象征核心枢纽。
- **四周四卡片**，呈十字布局：
  - 左上：`📁 文件系统` → `读写源码 · 操作日志 · 文档检索`
  - 左下：`🐙 代码协作` → `Git 分支 · PR 评审 · Issue 跟踪`
  - 右上：`🌐 网络请求` → `HTTP 抓取 · API 调用 · 数据获取`
  - 右下：`🗃️ 数据库管理` → `SQL 查询 · 数据迁移 · Schema 检查`
- **底部对比条**：深灰底，红字 `过去：多工具来回切换` → 箭头 → 绿字 `现在：Claude 统一调度 MCP`
- **风格**：飞书“价值主张图”——以 Claude 为中心，辐射四大能力域，用“过去 vs 现在”强化升级感，适用于产品价值宣讲。

#### ▶ 飞书风文字提取：
```
[ 全栈集成 ]  
全栈开发者的 MCP 工具箱  
单个 Claude 会话 · 无缝完成复杂工作流

🎯 核心：Claude 统一调度

📦 四大能力域：  
📁 文件系统｜读写源码 · 操作日志 · 文档检索  
🐙 代码协作｜Git 分支 · PR 评审 · Issue 跟踪  
🌐 网络请求｜HTTP 抓取 · API 调用 · 数据获取  
🗃️ 数据库管理｜SQL 查询 · 数据迁移 · Schema 检查  

🔄 价值跃迁：  
过去：多工具来回切换  
→  
现在：Claude 统一调度 MCP
```

> ✅ 飞书风体现：中心聚焦符号（🎯）、能力域图标+短语、对比箭头、动词驱动描述，是飞书“产品价值卡”的标准结构。

---

### **图片 26**

#### ▶ 视觉要素分析：
- **主标题**：`[ 安全机制 ]` + `安全的三重防御`，金黄+白粗体；副标题 `扩展能力边界 · 也守住信任底线`。
- **三栏横向布局**，按 Layer 1→2→3 递进：
  - **Layer 1（蓝）**：`💬 交互式审批`  
    - 描述：`首次连接服务器时，由用户交互式确认，建立信任基石`  
    - 示例命令：`$ claude mcp add` → `你信任此服务器吗？[y/N]`
  - **Layer 2（黄）**：`⚙️ 工具级权限`  
    - 描述：`细粒度控制每个工具的调用权限，敏感操作需二次确认`  
    - 示例：`tool: postgres.query` → `允许执行 SQL? [y/N]`
  - **Layer 3（绿）**：`🔐 OAuth 2.0`  
    - 描述：`动态认证替代静态密钥 · 令牌可撤销 · 范围最小化`  
    - 技术细节：`OAuth flow → token` → `短时效 · 自动刷新 · 可撤销`
- **风格**：飞书“纵深防御模型”——用 Layer 编号体现安全深度，每层含场景描述+交互示例，适用于安全白皮书或合规文档。

#### ▶ 飞书风文字提取：
```
[ 安全机制 ]  
安全的三重防御  
扩展能力边界 · 也守住信任底线

🛡️ Layer 1｜交互式审批  
• 首次连接服务器时，用户交互确认  
• 建立信任基石  
→ 示例：$ claude mcp add → “你信任此服务器吗？[y/N]”

🛡️ Layer 2｜工具级权限  
• 细粒度控制每个工具的调用权限  
• 敏感操作需二次确认  
→ 示例：tool: postgres.query → “允许执行 SQL? [y/N]”

🛡️ Layer 3｜OAuth 2.0  
• 动态认证替代静态密钥  
• 令牌可撤销 · 范围最小化  
→ 技术流：OAuth flow → token → 短时效 / 自动刷新 / 可撤销
```

> ✅ 飞书风体现：Layer 编号+🛡️统一前缀、交互示例用 → 引导、技术流用 → 分步，完全符合飞书“安全架构分层卡”。

---

### **图片 27**

#### ▶ 视觉要素分析：
- **主标题**：`[ 风险与防护 ]` + `三类安全风险与防护`，金黄+白粗体；副标题 `研究人员揭示 · 防护可执行`。
- **六宫格布局**：上排3个风险（红调），下排3个对应防护策略（绿调），一一映射。
  - **左上（风险）**：`🚫 提示注入攻击`  
    - 描述：`恶意内容通过 MCP 数据流注入 Claude 上下文，诱导非预期操作`
    - 下方对应防护：`✅ 防护策略` → `仅安装官方与知名社区服务器，警惕未审计来源`
  - **中上（风险）**：`⚠️ 工具权限滥用`  
    - 描述：`多个工具组合调用可能产生意外后果，例如读 + 写 + 网络发出敏感数据`
    - 对应防护：`✅ 防护策略` → `数据库使用只读账号 · 最小权限原则 · 禁用危险操作`
  - **右上（风险）**：` impersonate 冒名顶替`  
    - 描述：`恶意服务器伪装为合法工具，骗取密钥或诱导执行有害命令`
    - 对应防护：`✅ 防护策略` → `API 密钥通过环境变量注入 · 不写入代码 · 不外传配置`
- **色彩逻辑**：红=威胁，绿=对策；每组用相同图标（🚫/⚠️/ impersonate）保持关联性。
- **风格**：飞书“红队/蓝队对照表”——风险与防护成对呈现，便于安全团队制定检查清单。

#### ▶ 飞书风文字提取：
```
[ 风险与防护 ]  
三类安全风险与防护  
研究人员揭示 · 防护可执行

🔴 风险 1｜提示注入攻击  
• 恶意内容通过 MCP 数据流注入上下文，诱导非预期操作  
→ 防护：仅安装官方与知名社区服务器，警惕未审计来源  

🔴 风险 2｜工具权限滥用  
• 多工具组合调用 → 读 + 写 + 网络 = 敏感数据泄露  
→ 防护：数据库只读账号 · 最小权限原则 · 禁用危险操作  

🔴 风险 3｜冒名顶替  
• 恶意服务器伪装合法工具，骗取密钥或执行有害命令  
→ 防护：API 密钥 via 环境变量 · 不写入代码 · 不外传配置
```

> ✅ 飞书风体现：风险编号（1/2/3）、→ 映射防护、动词短语精炼，是飞书“安全Checklist”的标准输出格式。

---

### **图片 28**

#### ▶ 视觉要素分析：
- **主标题**：`[ 能力 × 策略 ]` + `MCP × Skills 完美协作`，白+金粗体；副标题 `能力 × 策略 = 完整方案`。
- **左侧面板**：`MCP 专业厨房 · 能力`，类比烹饪：
  - `冰箱 · 数据库`（食材存储）
  - `炉灶 · 执行`（火力输出）
  - `食材 · 文件`（原料）
  - `刀具 · API`（加工工具）
- **右侧面板**：`SKILLS 标准菜谱 · 策略`，指导如何组合：
  1. `规定食材 · 哪些工具可用`
  2. `规定步骤 · 调用顺序`
  3. `规定火候 · 参数与节奏`
- **底部流程链**：四步横向流程卡：  
  `用户需求 → Skill 识别 → MCP 工具 → 输出结果`  
  颜色依次：蓝→黄→深蓝→绿，体现流转。
- **风格**：飞书“隐喻教学法”——用“厨房/菜谱”类比技术架构，降低理解门槛，适用于面向非技术决策者的方案汇报。

#### ▶ 飞书风文字提取：
```
[ 能力 × 策略 ]  
MCP × Skills 完美协作  
能力 × 策略 = 完整方案

🍳 MCP｜专业厨房 · 能力  
• 冰箱 · 数据库｜存储能力  
• 炉灶 · 执行｜计算能力  
• 食材 · 文件｜数据输入  
• 刀具 · API｜接口能力  

📜 SKILLS｜标准菜谱 · 策略  
指导 Claude 以最优策略运用能力：  
1. 规定食材｜哪些工具可用  
2. 规定步骤｜调用顺序  
3. 规定火候｜参数与节奏  

➡️ 协作流程：  
用户需求 → Skill 识别 → MCP 工具 → 输出结果
```

> ✅ 飞书风体现：隐喻符号（🍳/📜）、能力-策略对应、流程箭头链, 完全契合飞书“概念科普卡”。

---

### **图片 29**

#### ▶ 视觉要素分析：
- **主标题**：`[ 企业级部署 ]` + `企业级集中管理`，金黄+白粗体；副标题 `managed-mcp.json · 全组织一次性下发`。
- **三栏对比结构**：
  - **左（分散配置）**：红调，标题 `">×</span> 分散配置`，列3个问题用户：
    - `小李 · 自己装 filesystem`
    - `小王 · 忘配 GitHub Token`
    - `新人 · 不知道装哪个`
    - 底部总结：`配置漂移 · 难以审计`
  - **中（配置文件）**：深棕，标题 `<> managed-mcp.json`，展示 JSON 片段：
    - `mcpServers` 包含 github/postgres/filesystem
    - `scope: "organization"`
    - 底部注：`下发到全员 · 自动生效`
  - **右（集中管理）**：绿调，标题 `✅ 集中管理`，三点优势：
    - `所有人 · 同一份预配置`
    - `新人 · 入职即就绪`
    - `凭据 · 集中管控更新`
    - 底部金句：`统一基线 · 可审计`
- **底部按钮组**：三个圆角按钮 `开箱即用` / `零配置` / `统一审计`，强化价值。
- **风格**：飞书“企业治理方案卡”——用“问题用户”具象化痛点，中间给解决方案，右侧给收益，是 IT 管理层最爱的汇报结构。

#### ▶ 飞书风文字提取：
```
[ 企业级部署 ]  
企业级集中管理  
managed-mcp.json · 全组织一次性下发

🔴 分散配置（问题）  
• 小李：自己装 filesystem  
• 小王：忘配 GitHub Token  
• 新人：不知道装哪个  
→ 配置漂移 · 难以审计  

⚙️ central config｜managed-mcp.json  
{  
  "mcpServers": {  
    "github": { ... },  
    "postgres": { ... },  
    "filesystem": { ... }  
  },  
  "scope": "organization"  
}  
→ 下发到全员 · 自动生效  

🟢 集中管理（收益）  
• 所有人 · 同一份预配置  
• 新人 · 入职即就绪  
• 凭据 · 集中管控更新  
→ 统一基线 · 可审计  

📌 三大价值：开箱即用｜零配置｜统一审计
```

> ✅ 飞书风体现：问题用户具象化、配置片段精简、收益用 • 列举、底部价值标签，完美复刻飞书“企业级方案卡”。

---

### **图片 30**

#### ▶ 视觉要素分析：
- **主标题**：`[ 调试实战 ]` + `调试与故障排除`，金黄+白粗体；副标题 `常见问题速查 + Token 智能管理`。
- **左侧面板**：`○ 常见问题`，四类问题卡片（红调）：
  1. `▶ 启动失败`：`服务器进程无法拉起 · 检查 npx 路径与依赖`
  2. `⏳ 连接超时`：`网络阻塞或代理 · 增加 timeout 或代理穿透`
  3. `🔐 认证失败`：`Token 过期或权限不足 · 重新走 OAuth 流程`
  4. `📈 输出截断`：`超过 Token 上限 · 调整环变或缩小查询范围`
- **右侧面板**：`📊 Token 智能管理`，含进度条（0 → 10K警告 → 25K截断）及三条规则：
  - `超过 10,000 · 警告`：提示输出过大，建议精简查询
  - `超过 25,000 · 自动截断`：防止上下文溢出，保护对话稳定
  - `MCP_TOOL_OUTPUT_MAX`：环境变量调整输出上限
- **风格**：飞书“运维手册速查页”——左侧问题分类+解决线索，右侧智能策略+可调参数，适用于 SRE 或技术支持团队。

#### ▶ 飞书风文字提取：
```
[ 调试实战 ]  
调试与故障排除  
常见问题速查 + Token 智能管理

🔍 常见问题：  
▶ 启动失败｜服务器进程无法拉起 · 检查 npx 路径与依赖  
⏳ 连接超时｜网络阻塞或代理 · 增加 timeout 或代理穿透  
🔐 认证失败｜Token 过期或权限不足 · 重新走 OAuth 流程  
📈 输出截断｜超过 Token 上限 · 调整环变或缩小查询范围  

🤖 Token 智能管理：  
[████████░░░░] 10K（警告） → 25K（截断）  
• >10K：提示输出过大 · 建议精简查询  
• >25K：自动截断 · 保护对话稳定  
• 环境变量：MCP_TOOL_OUTPUT_MAX 可调上限
```

> ✅ 飞书风体现：问题分类图标（▶/⏳/🔐/📈）、进度条可视化、环境变量显式标注，是飞书“故障排查卡”的终极形态。

---

### **图片 31**

#### ▶ 视觉要素分析：
- **背景**：延续全系列统一的深黑网格底，保持科技感与一致性。
- **顶部居中标签**：金黄色方括号包裹文字 `[ 下章预告 ]`，字体为无衬线粗体，明确标识“预告”属性。
- **主标题**：`Headless 模式与 CI/CD 集成`，采用超大号白色粗体字（类似 SF Pro Display Bold），其中 `Headless` 为英文原词，`模式与 CI/CD 集成` 为中文，形成中英混排的现代技术文档风格；整体居中，占据视觉核心。
- **中部卡片**：一个半透明深蓝圆角矩形卡片，带有微妙发光边框，营造“悬浮感”。
  - 左上角小字：`NEXT · CHAPTER 07`（浅青色，提示章节序号）
  - 左侧图标：蓝色圆形内含抽象“无人值守”符号（类似服务器+时钟或无头终端图标 `8` 或 `≡` 变体）
  - 主标题：`Headless · 无人值守运行`（白字，加粗）
  - 副描述两行（浅灰）：
    - `从交互式跳到自动化部署`
    - `与 CI/CD 流程深度集成的终极形态`
  - 右侧：一个醒目的蓝色右向箭头 `→`，引导视线，强化“下一章”的行动暗示。
- **整体风格**：典型的飞书“章节过渡页”或“课程预告卡”——简洁、聚焦、有引导性；用颜色（金/白/蓝）、图标与箭头构建清晰的信息流，避免任何冗余元素。

#### ▶ 飞书风文字提取：
```
[ 下章预告 ]

Headless 模式与 CI/CD 集成

➡ NEXT · CHAPTER 07  
🔵 Headless · 无人值守运行  
• 从交互式跳到自动化部署  
• 与 CI/CD 流程深度集成的终极形态
```

> ✅ 飞书风体现：  
> - 方括号标签 `[ ]` 标识元信息（预告/章节）  
> - `➡` 箭头替代“下一页”文字，更现代简洁  
> - 图标前缀（🔵）替代纯文字分类  
> - 项目符号 `•` 列举核心价值点  
> - 关键术语保留英文原词（Headless, CI/CD），符合技术文档规范  

---