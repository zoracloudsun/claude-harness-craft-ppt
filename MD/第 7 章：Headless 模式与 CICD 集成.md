
### **图片 1**

#### ▶ 视觉要素分析：
- **背景**：深黑网格底，延续统一科技感风格。
- **顶部标签**：居中蓝灰圆角矩形 `CHAPTER · 07`，小号无衬线体，轻发光。
- **主标题**：两行居中大字  
  - 第一行：`Claude Code Harness`（米白粗体，类似 SF Pro Display）  
  - 第二行：`核心要点解析指南`（亮金粗体，带“MCP”半透明水印嵌入“要点”间）
- **副标题**：`第 7 章：Headless 模式与 CI/CD 集成`（亮金，加粗，居中）  
- **底部英文注释**：`Headless Mode & CI/CD Integration`（浅灰细体，全大写，低透明度）
- **整体**：与第1张封面高度一致，仅章节号与标题更新，体现系列连贯性。

#### ▶ 飞书风文字提取：
```
CHAPTER · 07

Claude Code Harness  
核心要点解析指南  

第 7 章：Headless 模式与 CI/CD 集成  
Headless Mode & CI/CD Integration
```

> ✅ 飞书风：层级分明、关键词高亮（Headless/CI/CD）、中英对照、无装饰，标准封面卡。

---

### **图片 2**

#### ▶ 视觉要素分析：
- **双栏结构**：左“第六章回顾”，右“本章重点”。
- **左栏**：  
  - 标题 `上一章掌握的核心`（白粗体）  
  - 四个深灰卡片，黄点项目符号：  
    • MCP 协议标准化接口  
    • 外部系统连接能力  
    • 数据库 / API / 工具扩展  
    • 前提：人在屏幕前操作  
- **右栏**：  
  - 金黄标签 `本章重点`  
  - 主卡片深棕渐变，大字 `Headless 模式 · CI/CD 集成`（金+白）  
  - 副标题 `无人值守的自动化审查`（白）  
  - 底部小字引语：`运筹帷幄之中，决胜于千里之外`（灰，古文点睛）

#### ▶ 飞书风文字提取：
```
【第六章回顾】  
上一章掌握的核心  
• MCP 协议标准化接口  
• 外部系统连接能力  
• 数据库 / API / 工具扩展  
• 前提：人在屏幕前操作  

【本章重点】  
Headless 模式 · CI/CD 集成  
无人值守的自动化审查  

→ 运筹帷幄之中，决胜于千里之外
```

> ✅ 飞书风：模块标签【】、项目符号统一、引语独立成行，典型飞书“章节过渡摘要”。

---

### **图片 3**

#### ▶ 视觉要素分析：
- **顶部标题**：`高级工程师`（蓝字） + 漏斗图（标“瓶颈”） + `待审查 PR`（黄字列表：PR #201 ~ #206）  
- **漏斗下方**：红字 `20+ PR / 每天`，强调吞吐压力。
- **底部三卡并列**（红/棕/红）：  
  1. `PR 积压`：图标 📋，描述“队列不断膨胀，发布周期被迫延长”  
  2. `上下文切换`：图标 ↻，描述“等待审查期间频繁切换，开发效率大幅下降”  
  3. `质量下滑`：图标 ⚠️，描述“疲惫审查遗漏漏洞，缺陷流入生产环境”  
- **风格**：问题驱动叙事，用“工程师→瓶颈→后果”链条构建痛点共识，是飞书“业务痛点卡”范式。

#### ▶ 飞书风文字提取：
```
高级工程师 → 待审查 PR  
▸ PR #201 ~ #206（6个）  
→ 瓶颈  
→ 20+ PR / 每天  

🔴 PR 积压  
• 队列不断膨胀  
• 发布周期被迫延长  

🟤 上下文切换  
• 等待审查期频繁切换  
• 开发效率大幅下降  

🔴 质量下滑  
• 疲惫审查遗漏漏洞  
• 缺陷流入生产环境
```

> ✅ 飞书风：箭头流程、颜色区分问题严重性、短句痛点罗列，适配飞书“问题场景速写”。

---

### **图片 4**

#### ▶ 视觉要素分析：
- **顶部流程图**：四步横向链  
  `PR 提交` → `Claude 初审`（黄卡，标“安全/质量/覆盖率”） → `人工终审`（蓝卡，标“业务逻辑/架构设计”） → `Merge`（绿卡）  
  箭头为虚线+实线交替，表自动化衔接。
- **主体大卡**：深灰背景，标题 `核心概念｜Headless 模式`  
  - 定义：`通过 -p 参数，告诉 Claude 这是一次非交互调用。`（“非交互”黄高亮）  
  - 行为：`任务完成后直接输出结果并退出，无需等待任何人工输入。`  
- **右侧终端框**：模拟命令  
  `$ claude -p "Review this PR for security issues"`  
  下方三点绿色勾选：`✓ 非交互 · 自动执行 · 直接输出`

#### ▶ 飞书风文字提取：
```
📌 流程：PR 提交 → Claude 初审 → 人工终审 → Merge  
• Claude 初审：安全 / 质量 / 覆盖率  
• 人工终审：业务逻辑 / 架构设计  

💡 核心概念｜Headless 模式  
• 通过 `-p` 参数，告诉 Claude 这是一次 **非交互** 调用  
• 任务完成 → 直接输出结果并退出，无需人工输入  

 terminal  
$ claude -p "Review this PR for security issues"  

✓ 非交互 · 自动执行 · 直接输出
```

> ✅ 飞书风：流程箭头、关键参数 `-p` 突出、终端命令保留、勾选清单，完全匹配飞书“功能定义卡”。

---

### **图片 5**

#### ▶ 视觉要素分析：
- **双栏对比表**：左 `交互模式`（蓝标），右 `Headless 模式`（黄标），7行维度对比。
  - 界面：`终端 TUI 界面` vs `无界面，仅 stdout`  
  - 输入方式：`多轮对话交互` vs `一次性 Prompt / 管道`  
  - 权限处理：`人工确认弹窗` vs `自动跳过 / 白名单执行`  
  - 会话管理：`自动保存会话` vs `默认不保存`  
  - 人类角色：`审批者 + 检查者 + 控制者` vs `完全缺席`（红字强调）  
  - 规则载体：`实时人工判断` vs `参数硬编码预设`  
  - 适用场景：`开发 / 调试 / 探索` vs `CI/CD / 自动化 / 批处理`  
- **底部警示条**：红底白字带⚠️图标：  
  `人类扮演三重角色：权限审批者 / 质量检查者 / 流程控制者 → Headless 下全部缺席，必须预先硬编码`

#### ▶ 飞书风文字提取：
```
维度          │ 交互模式                  │ Headless 模式  
──────────────┼───────────────────────────┼────────────────────────────  
界面          │ 终端 TUI 界面             │ 无界面，仅 stdout  
输入方式      │ 多轮对话交互              │ 一次性 Prompt / 管道  
权限处理      │ 人工确认弹窗              │ 自动跳过 / 白名单执行  
会话管理      │ 自动保存会话              │ 默认不保存  
人类角色      │ 审批者 + 检查者 + 控制者   │ ❌ 完全缺席  
规则载体      │ 实时人工判断              │ 参数硬编码预设  
适用场景      │ 开发 / 调试 / 探索         │ CI/CD / 自动化 / 批处理  

⚠️ 关键结论：  
人类三重角色（权限审批者 / 质量检查者 / 流程控制者）→ Headless 下全部缺席  
→ 必须预先硬编码规则
```

> ✅ 飞书风：表格对比（飞书支持Markdown表格）、❌强调缺失、箭头归因，是飞书“模式对比矩阵”的标准输出。

---

### **图片 6**

#### ▶ 视觉要素分析：
- **主标题**：`四维参数体系`，白粗体居中。
- **四宫格布局**，每格含图标+标题+说明：
  1. **左上（蓝）**：`📄 输出格式` → `output-format` → `下游消费协议`  
  2. **右上（黄）**：`🎙️ 成本护栏` → `max-turns / max-budget-usd` → `防止无人值守预算失控`  
  3. **左下（绿）**：`🛡️ 安全边界` → `allowedTools / disabledTools` → `严格限定工具权限确保合规`  
  4. **右下（紫）**：`⚙️ 执行控制` → `model / system-prompt` → `模型选择与 Prompt 定制`  
- **底部总结句**：`生产级无人值守调用，必须在四个维度上做出明确决策`（浅灰）

#### ▶ 飞书风文字提取：
```
四维参数体系

📄 输出格式｜output-format  
• 下游消费协议  

🎙️ 成本护栏｜max-turns / max-budget-usd  
• 防止无人值守预算失控  

🛡️ 安全边界｜allowedTools / disabledTools  
• 严格限定工具权限确保合规  

⚙️ 执行控制｜model / system-prompt  
• 模型选择与 Prompt 定制  

📌 生产级无人值守调用，必须在四个维度上做出明确决策
```

> ✅ 飞书风：图标前缀、参数名保留原样（`max-turns`）、结论句收尾，飞书“配置框架卡”典范。

---

### **图片 7**

#### ▶ 视觉要素分析：
- **主标题**：`输出格式 · output-format`，白+金。
- **三栏并列**，对应三种输出模式：
  1. **左（蓝）**：`T text` → `纯文本流`，含终端命令示例与输出片段，底部按钮 `人类阅读`  
  2. **中（黄）**：`<> json` → `结构化对象`，展示 JSON Schema 片段（session_id, result, duration_ms...），底部按钮 `机器解析`  
  3. **右（绿）**：`~ stream-json` → `逐行事件流`，展示事件序列（init, tool_use, assistant, result），底部按钮 `实时监控`  
- **底部提示**：`人看用 text，机器解析用 json，实时监控用 stream-json`（浅灰小字）

#### ▶ 飞书风文字提取：
```
输出格式 · output-format

🔵 text｜纯文本流  
$ claude -p "review" --output-format text  
→ The code looks clean... Found 2 potential issues.  
✅ 人类阅读  

🟡 json｜结构化对象  
{  
  "session_id": "abc...",  
  "result": "...",  
  "duration_ms": 4200,  
  "cost_usd": 0.03  
}  
✅ 机器解析  

🟢 stream-json｜逐行事件流  
init session started  
tool_use Read file.ts  
tool_result ok  
assistant reviewing...  
result complete  
✅ 实时监控  

📌 使用原则：  
人看用 text｜机器解析用 json｜实时监控用 stream-json
```

> ✅ 飞书风：颜色标签+用途标注、代码片段精简、✅行动指引、底部原则总结，飞书“输出选项卡”标准模板。

---

### **图片 8**

#### ▶ 视觉要素分析：
- **主标题**：`成本护栏 · 双保险`，金黄粗体.
- **左右双卡**：
  - **左卡（蓝）**：`max-turns`  
    - 命令示例：`--max-turns 6`  
    - 说明：`限制交互轮数。例如读取5个文件再给结论，通常只需6轮。`  
    - 图表：半圆进度条显示“6 turns / 10”，底部 `轮数耗尽 → 输出已有结论`  
  - **右卡（黄）**：`max-budget-usd`  
    - 命令示例：`--max-budget-usd 1.0`  
    - 说明：`设置硬性金额上限。即使轮数很少，大量 Token 累计成本仍可能 $1.0 超标。`  
    - 图表：水平进度条标 `$0` → `limit $1.0`，底部 `预算触发 → 立即强制停止`  
- **底部警示条**：红底⚠️图标：`生产环境务必同时配置，两道防线缺一不可`

#### ▶ 飞书风文字提取：
```
成本护栏 · 双保险

🔵 max-turns  
$ claude -p "review" --max-turns 6  
• 限制交互轮数（例：5文件+结论 = 6轮）  
• 轮数耗尽 → 输出已有结论  

🟡 max-budget-usd  
$ claude -p "review" --max-budget-usd 1.0  
• 硬性金额上限（防Token累积超支）  
• 预算触发 → 立即强制停止  

⚠️ 强制要求：  
生产环境务必同时配置，两道防线缺一不可
```

> ✅ 飞书风：参数名突出、行为结果用 → 表达、警示独立成条，飞书“安全配置卡”核心样式。

---

### **图片 9**

#### ▶ 视觉要素分析：
- **主标题**：`安全边界 · allowedTools`，白+绿。
- **左右双栏**：
  - **左（绿）**：`✅ 白名单 allowedTools`（推荐标签）  
    - 原则：`仅允许明确列出的工具，其余一律拒绝`  
    - 示例：  
      ✓ Read（只读文件）  
      ✓ Grep（搜索内容）  
      ✓ Bash(git log *)（模式匹配）  
      ✓ Glob（文件查找）  
  - **右（红）**：`🚫 黑名单 disabledTools`（慎用标签）  
    - 原则：`除禁止的工具以外，其余都可使用`  
    - 示例：  
      × Edit（禁止编辑）  
      × Write（禁止写入）  
      × Bash(rm *)（禁止删除）  
    - 底部警告：`未列出的工具均可使用 — 风险较高`  
- **底部红色警示条**：  
  `dangerously-skip-permissions` — 切勿忽视名称中的 **dangerously**，严禁在生产 CI 中使用

#### ▶ 飞书风文字提取：
```
安全边界 · allowedTools

✅ 白名单 allowedTools｜推荐  
• 仅允许明确列出的工具，其余一律拒绝  
  ✓ Read｜只读文件  
  ✓ Grep｜搜索内容  
  ✓ Bash(git log *)｜模式匹配  
  ✓ Glob｜文件查找  

🚫 黑名单 disabledTools｜慎用  
• 除禁止工具外，其余均可使用  
  × Edit｜禁止编辑  
  × Write｜禁止写入  
  × Bash(rm *)｜禁止删除  
→ 未列出的工具均可使用 · 风险较高  

⚠️ 严令禁止：  
`dangerously-skip-permissions`  
切勿忽视名称中的 **dangerously**，严禁在生产 CI 中使用
```

> ✅ 飞书风：✅/🚫图标区分策略、风险等级显性化、关键字加粗（dangerously），飞书“安全策略卡”最高规范。

---

### **图片 10**

#### ▶ 视觉要素分析：
- **主标题**：`执行控制 · 三大维度`，白粗体 + 齿轮图标。
- **三栏结构**：
  1. **左（蓝）**：`🔀 模型选择`  
     - Haiku：快速 / 低成本 / 简单任务  
     - Sonnet：深度分析 / 复杂推理  
     - 底部：`--fallback-model` → 主模型过载时自动降级  
  2. **中（黄）**：`📄 System Prompt`  
     - `--append-system-prompt`（推荐）：追加约束，保留 Claude 基础能力  
     - `--system-prompt`（覆盖）：彻底替换，默认行为 → 极易导致工具调用失败  
     - 底部总结：`追加 = 增强｜覆盖 = 风险`  
  3. **右（紫）**：`<> 结构化输出`  
     - `--json-schema`：强制验证输出 JSON 格式  
     - 流程：`Claude 输出 JSON` → `Schema 验证` → `通过 / 不符 → 重试`  
     - 底部：`确保下游系统能直接消费`

#### ▶ 飞书风文字提取：
```
执行控制 · 三大维度

🔹 模型选择  
• Haiku｜快速 / 低成本 / 简单任务  
• Sonnet｜深度分析 / 复杂推理  
→ --fallback-model：主模型过载时自动降级  

🔸 System Prompt  
• --append-system-prompt ✅ 推荐  
  追加约束，保留 Claude 基础能力  
• --system-prompt ❌ 覆盖  
  彻底替换默认行为，极易导致工具调用失败  
→ 追加 = 增强｜覆盖 = 风险  

🔹 结构化输出  
• --json-schema：强制验证输出 JSON 格式  
  Claude 输出 JSON → Schema 验证 →  
    ✓ 通过｜✗ 不符 → 重试  
→ 确保下游系统能直接消费
```

> ✅ 飞书风：维度符号（🔹/🔸）、✅/❌状态标识、箭头流程、对比结论短句，完全契合飞书“执行策略配置卡”。

---

### **图片 11**

#### ▶ 视觉要素分析：
- **主标题**：`> Unix 管道 · 无缝集成`，白字+黄箭头前缀，居中，体现“命令行哲学”。
- **三行水平流程卡**，模拟 Unix 管道 `|` 连接：
  1. `cat error.log` → `|` → `claude -p "分析错误"` → 右侧标签 `日志分析`（蓝底白字）
  2. `git diff` → `|` → `claude -p "总结变更"` → 右侧标签 `变更摘要`（绿底白字）
  3. `claude -p "分析" --output-format json` → `|` → `jq '.result'` → 右侧标签 `结构化提取`（紫底白字）
- 每行左侧为命令图标（📄/🐙/🛡️），中间用 `— | —` 符号模拟管道，右侧为功能标签。
- **底部横幅**：深灰底，黄字公式 `> 模式匹配 + ☑ 语义分析 = 优势互补`，点明技术融合价值。
- **风格**：高度还原 Unix 哲学——组合小工具完成复杂任务；是飞书“开发者友好型”设计的典范。

#### ▶ 飞书风文字提取：
```
> Unix 管道 · 无缝集成

📄 cat error.log  │  claude -p "分析错误"  
→ 日志分析  

🐙 git diff       │  claude -p "总结变更"  
→ 变更摘要  

🛡️ claude -p "分析" --output-format json  │  jq '.result'  
→ 结构化提取  

💡 优势互补：  
模式匹配 + 语义分析 = 高效自动化
```

> ✅ 飞书风体现：`│` 代替 `|` 更美观、图标前缀、箭头归因、公式式结论，完全契合飞书“命令行集成卡”。

---

### **图片 12**

#### ▶ 视觉要素分析：
- **主标题**：`GitHub Actions · 官方 Action`，白粗体 + GitHub Octocat 图标。
- **上部**：深灰代码块，展示 `workflow.yml` 片段：
  - `uses: anthropics/claudе-code-action@v1`
  - `mode: agent | tag`
  - `claude_args: "--max-turns 3 --model sonnet"`
  - `concurrency.group: claude-${{ github.event.pull_request.number }}`
  - `cancel-in-progress: true`
  - 关键字段高亮：`agent`（蓝）、`sonnet`（黄）、`cancel-in-progress`（青）
- **下部双卡**：
  - 左（蓝）：`Agent 模式` → `PR 创建或更新时，自动执行预设审查任务`
  - 右（黄）：`Tag 模式` → `评论中 @claude 触发，交互式智能响应`
- **底部提示条**：绿色圆角框，带 ○ 图标：  
  `cancel-in-progress：同一 PR 连续推送时自动取消旧任务，节省 API 费用`

#### ▶ 飞书风文字提取：
```
GitHub Actions · 官方 Action

⚙️ workflow.yml 关键配置：  
uses: anthropics/claudе-code-action@v1  
with:  
  mode: agent | tag                # 触发模式  
  claude_args: "--max-turns 3 --model sonnet"  
  concurrency:  
    group: claude-${{ github.event.pull_request.number }}  
  cancel-in-progress: true        # 防重复执行  

🔹 Agent 模式  
• PR 创建或更新时  
• 自动执行预设审查任务  

🔸 Tag 模式  
• 评论中 @claude 触发  
• 交互式智能响应  

✅ 最佳实践：  
`cancel-in-progress: true`  
→ 同一 PR 连续推送时自动取消旧任务，节省 API 费用
```

> ✅ 飞书风体现：配置片段保留关键参数、模式用🔹/🔸区分、✅实践建议独立成条，飞书“CI 配置卡”标准格式。

---

### **图片 13**

#### ▶ 视觉要素分析：
- **主标题**：`直接 CLI · 三项能力`，白粗体。
- **左列三卡**（自上而下）：
  1. `$ 成本透明化` → `每次审查费用直接显示在 PR 评论中`
  2. `🛡️ 质量门禁` → `Critical 级别问题阻塞合并`
  3. `🗂️ 路径过滤` → `仅 src/ 目录变更时触发`
- **右列单卡**：标题 `CI/CD 最佳实践`，含：
  - `env:` 区块：`CLAUD_E_CODE_DISABLE_NONESSENTIAL_TRAFFIC: "1"`
  - 三条禁用项（红×图标）：
    - 自动更新  
    - 遥测上报  
    - 错误上报  
- **整体**：左为能力，右为落地配置，形成“能力→实践”闭环。

#### ▶ 飞书风文字提取：
```
直接 CLI · 三项能力

💰 成本透明化  
• 每次审查费用直接显示在 PR 评论中  

🛡️ 质量门禁  
• Critical 级别问题阻塞合并  

🗂️ 路径过滤  
• 仅 src/ 目录变更时触发  

📌 CI/CD 最佳实践  
env:  
  CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC: "1"  

❌ 禁用项：  
• 自动更新  
• 遥测上报  
• 错误上报
```

> ✅ 飞书风体现：货币/盾牌/文件夹图标直指能力本质、环境变量原样保留、❌禁用清单清晰，飞书“CLI 能力速查表”范式。

---

### **图片 14**

#### ▶ 视觉要素分析：
- **主标题**：`多阶段 AI 流水线`，白粗体。
- **中心流程图**：三层结构
  - **Stage 1（顶）**：`📁 Stage 1：格式检查` → `Haiku $0.05 3 turns`
  - **Stage 2（左下）**：`🛡️ Stage 2：安全扫描` → `Sonnet $1.00` → `OWASP Top 10 + json-schema`
  - **Stage 3（右下）**：`📈 Stage 3：覆盖分析` → `Sonnet $0.50` → `测试覆盖率分析与报告`
- **连接线**：虚线箭头标注 `needs: format-check`，表明 Stage 2/3 依赖 Stage 1 输出。
- **底部注释**：灰底小字 `Stage 2 与 Stage 3 无相互依赖，GitHub Actions 自动并行执行`
- **风格**：典型 CI/CD 多阶段流水线可视化，成本标注（$）强化工程决策依据。

#### ▶ 飞书风文字提取：
```
多阶段 AI 流水线

_STAGE 1｜格式检查_  
• Haiku｜$0.05｜3 turns  

_STAGE 2｜安全扫描_  
• Sonnet｜$1.00  
• OWASP Top 10 + json-schema 结构化漏洞报告  
→ 依赖 Stage 1 输出  

_STAGE 3｜覆盖分析_  
• Sonnet｜$0.50  
• 测试覆盖率分析与报告  
→ 依赖 Stage 1 输出  

⚡ 并行执行：  
Stage 2 与 Stage 3 无依赖 → GitHub Actions 自动并行
```

> ✅ 飞书风体现：阶段编号 `_STAGE X_`、成本/模型/能力三要素并列、依赖关系明确标注、并行提示用⚡，飞书“流水线设计卡”标准输出。

---

### **图片 15**

#### ▶ 视觉要素分析：
- **主标题**：`stream-json · 事件协议`，白字；顶部小字 `STREAM-JSON`。
- **垂直时间轴**（左列彩色圆点）：
  1. 🔵 `system` → 初始化：模型版本、可用工具等环境信息  
  2. 🟡 `assistant` → 内容：Claude 生成的文本回复  
  3. 🟢 `result` → 总结：总轮数、费用等统计信息  
  4. 🔴 `compact_boundary` → 警告：上下文压缩中，注意潜在信息丢失  
- 每项含图标+类型标签+说明，颜色与圆点呼应。
- **风格**：事件驱动架构的标准化协议描述，适用于监控系统对接文档。

#### ▶ 飞书风文字提取：
```
stream-json · 事件协议

🔵 system｜初始化  
• 模型版本、可用工具等环境信息  

🟡 assistant｜内容  
• Claude 生成的文本回复  

🟢 result｜总结  
• 总轮数、费用等统计信息  

🔴 compact_boundary｜警告  
• 上下文压缩中 — 注意潜在信息丢失
```

> ✅ 飞书风体现：颜色编码事件类型、短句说明、警告项用🔴强调，飞书“协议规范卡”简洁有力。

---

### **图片 16**

#### ▶ 视觉要素分析：
- **主标题**：`会话管理 · 四种模式`，白粗体；顶部小字 `SESSION MANAGEMENT`。
- **四层纵向流程**，左侧圆点颜色标识模式类型：
  1. ⚪ `default`（无状态）：`每次调用都是全新上下文，不保存会话`
  2. 🔵 `resume`（指定会话续接）：`通过 session_id 载入完整状态继续推理` → 右标 `session_id: abc123`
  3. 🟢 `continue`（自动续接）：`自动连接最近会话，无需手动查 ID` → 右标 `latest session`
  4. 🟣 `fork`（分支）：三向分叉 → `分支 A：性能优化方向` / `分支 B：架构重构方向` / `分支 C：安全加固方向`
- **底部注释**：`原始会话保持不变 · 类似 Git 分支机制`
- **风格**：将 AI 会话类比为 Git 工作流，降低理解门槛，是飞书“概念隐喻卡”的高级应用。

#### ▶ 飞书风文字提取：
```
会话管理 · 四种模式

⚪ default｜无状态  
• 每次调用全新上下文，不保存会话  

🔵 resume｜指定会话续接  
• 通过 session_id 载入完整状态  
→ session_id: abc123  

🟢 continue｜自动续接  
• 自动连接最近会话，无需查 ID  
→ latest session  

🟣 fork｜分支模式  
• 分支 A：性能优化方向  
• 分支 B：架构重构方向  
• 分支 C：安全加固方向  

📌 本质：原始会话不变 · 类似 Git 分支机制
```

> ✅ 飞书风体现：颜色+符号统一标识模式、Git 类比收尾、分支用 • 列举，飞书“会话策略卡”典范。

---

### **图片 17**

#### ▶ 视觉要素分析：
- **主标题**：`CI 环境 · 配置清单`，白粗体；顶部小字 `CI ENVIRONMENT`。
- **左列「必备配置」**（锁图标）：
  - `ANTHROPIC_API_KEY` → API 密钥 – CI Secrets 注入  
  - `CLAUD_E_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1` → 聚合变量，一次性禁用非必要流量  
- **右列「性能调优」**（闪电图标）：
  - `Bash 超时`：`120s → 300s`（命令执行超时延长）  
  - `最大输出 Token`：`32K → 64K`（单次响应上限提升）  
  - `压缩阈值`：`50%`（上下文自动压缩触发点）  
- **底部横幅**：深灰底，带盾牌图标：  
  `--strict-mcp-config` → MCP 服务器环境隔离，确保 CI 与本地配置互不干扰

#### ▶ 飞书风文字提取：
```
CI 环境 · 配置清单

🔒 必备配置  
• ANTHROPIC_API_KEY｜API 密钥 – CI Secrets 注入  
• CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC=1｜聚合变量，禁用非必要流量  

⚡ 性能调优  
• Bash 超时：120s → 300s  
• 最大输出 Token：32K → 64K  
• 压缩阈值：50%  

🛡️ 强制隔离：  
--strict-mcp-config  
→ MCP 服务器环境隔离，CI 与本地配置互不干扰
```

> ✅ 飞书风体现：图标分类（🔒/⚡/🛡️）、参数名原样保留、箭头表示变更、强制项突出，飞书“CI 部署 Checklist”标准格式。

---

### **图片 18**

#### ▶ 视觉要素分析：
- **主标题**：`跨平台 · CI/CD 集成`，白粗体；副标题 `Platform Agnostic – 只要支持 Node.js 即可部署`。
- **三栏并列**，对应三大集成方案：
  1. **左（橙）**：`GitLab CI` → `.gitlab-ci.yml`  
     - `image: node:20`  
     - `script: npm install -g @anthropic-ai/claudе-code && claude --print "Review this MR"`  
     - `rules: if: $CI_PIPELINE_SOURCE == "merge_request_event"`  
  2. **中（蓝）**：`Jenkins` → `Declarative Pipeline`  
     - `agent { docker 'node:20' }`  
     - `environment { ANTHROPIC_API_KEY = credentials('claude-key') }`  
     - 底部标签：`Credentials Provider 安全管理`  
  3. **右（黄）**：`本地脚本` → `Pre-commit 自检`  
     - `#!/bin/bash` + `DIFF=$(git diff --cached)` + `claude --print "Review: $DIFF"`  
     - 底部两点勾选：`提交前自动审查` / `轻量高效的本地自动化`
- **风格**：真正“平台无关”落地示例，覆盖云 CI、企业 Jenkins、本地 DevOps，是飞书“多平台适配卡”终极形态。

#### ▶ 飞书风文字提取：
```
跨平台 · CI/CD 集成  
Platform Agnostic – 只要支持 Node.js 即可部署

🟠 GitLab CI｜.gitlab-ci.yml  
• image: node:20  
• script:  
    npm install -g @anthropic-ai/claudе-code  
    claude --print "Review this MR"  
• rules:  
    if: $CI_PIPELINE_SOURCE == "merge_request_event"  

🔵 Jenkins｜Declarative Pipeline  
• agent { docker 'node:20' }  
• environment:  
    ANTHROPIC_API_KEY = credentials('claude-key')  
→ Credentials Provider 安全管理  

🟡 本地脚本｜Pre-commit 自检  
#!/bin/bash  
DIFF=$(git diff --cached)  
claude --print "Review: $DIFF"  

✅ 优势：  
• 提交前自动审查  
• 轻量高效的本地自动化
```

> ✅ 飞书风体现：平台色标、关键配置精简、✅优势总结，完全匹配飞书“多平台集成指南卡”。

---

### **图片 19**

#### ▶ 视觉要素分析：
- **主标题**：`五层纵深防御体系`，白粗体；副标题 `CI/CD 中运行 Agent 的安全策略`。
- **左侧**：红色警示框 `四大风险`（圆点列表）：
  - 机密泄露  
  - 破坏性操作  
  - 费用失控  
  - 内容污染  
- **右侧**：五层防御卡片（自下而上，绿→蓝→紫→黄→灰）：
  1. `Layer 1 · 最小权限`（绿）：杜绝越权操作 – 安全基石  
  2. `Layer 2 · Secrets 管理`（蓝）：严防凭据泄露  
  3. `Layer 3 · 容器隔离`（紫）：阻断逃逸风险  
  4. `Layer 4 · 成本防护`（黄）：账单失控防线  
  5. `Layer 5 · 审计日志`（灰）：全程可追溯，事后审查  
- **箭头**：从“四大风险”指向五层体系，表“风险→防护”映射。
- **风格**：经典纵深防御（Defense in Depth）模型，用于安全合规汇报。

#### ▶ 飞书风文字提取：
```
五层纵深防御体系  
CI/CD 中运行 Agent 的安全策略

🔴 四大风险  
• 机密泄露  
• 破坏性操作  
• 费用失控  
• 内容污染  

🛡️ 五层防护：  
Layer 5｜审计日志  
• 全程可追溯，事后审查  

Layer 4｜成本防护  
• 账单失控防线  

Layer 3｜容器隔离  
• 阻断逃逸风险  

Layer 2｜Secrets 管理  
• 严防凭据泄露  

Layer 1｜最小权限  
• 杜绝越权操作 – 安全基石
```

> ✅ 飞书风体现：风险→防护映射、层级倒序呈现（从结果回溯根基）、关键词加粗，飞书“安全架构卡”权威表达。

---

### **图片 20**

#### ▶ 视觉要素分析：
- **主标题**：`安全实践 · 四项措施`，白粗体。
- **四宫格布局**：
  1. **左上（紫）**：`容器隔离`  
     - 命令示例：`docker run --read-only --network none claude-runner`  
     - 两点原则：`只读文件系统，禁止写入` / `无网络访问，完全沙箱化`  
  2. **右上（黄）**：`成本防护`  
     - 子项：`路径过滤`（仅关键目录变更触发） + `并发控制`（concurrency group 防重复）  
     - 底部：`从源头避免不必要的触发`  
  3. **左下（红）**：`Secrets 管理`  
     - 警告框：`⚠️ 隐患警告` → `Prompt 要求模型列出环境变量时，模型可能在输出中复述密钥值`  
     - 补充：`GitHub 日志遮蔽机制无法捕获此类泄露`  
  4. **右下（蓝）**：`审计日志`  
     - 展示 JSON 片段：`model`, `cost_usd`, `duration_ms`, `num_turns`  
     - 底部：`JSON 输出自带元数据，全程可追溯`

#### ▶ 飞书风文字提取：
```
安全实践 · 四项措施

🔐 容器隔离  
docker run \  
  --read-only \  
  --network none \  
  claude-runner  
• 只读文件系统，禁止写入  
• 无网络访问，完全沙箱化  

💰 成本防护  
• 路径过滤：仅关键目录变更触发  
• 并发控制：concurrency group 防重复运行  
→ 从源头避免不必要触发  

⚠️ Secrets 管理  
隐患：Prompt 要求模型列出环境变量 → 模型可能在输出中复述密钥值  
→ GitHub 日志遮蔽机制无法捕获此类泄露  

📋 审计日志  
{  
  "model": "claude-sonnet-4-...",  
  "cost_usd": 0.042,  
  "duration_ms": 12350,  
  "num_turns": 3  
}  
• JSON 输出自带元数据，全程可追溯
```

> ✅ 飞书风体现：命令保留换行、隐患用⚠️突出、JSON元数据结构化呈现、措施归类清晰，是飞书“安全落地 checklist”最高标准

---

收到，这是您提供的**新章节最后4页**（即全系列第52–55节），我们将严格按要求：  
✅ 从**第21张起继续编号**（但本次任务明确为“新章节最后4页”，故直接处理这4张）  
✅ 每张图先解析**布局/排版/样式**  
✅ 再提取**完整飞书风文字**（模块化、符号引导、关键词高亮、无冗余）

---

### **图片 21**

#### ▶ 视觉要素分析：
- **主标题**：`CLI vs Agent SDK`，白粗体居中，突出对比主题。
- **双栏结构**：左 `CLI`（蓝调），右 `Agent SDK`（紫调），中间用金色圆形 `VS` 分隔。
  - **左栏（CLI）**：
    - 标题 `CLI` + 终端图标 `>_`，副标题 `命令行接口`
    - 类比句：`如同 curl`（蓝底白字）
    - 三点优势：`轻量便捷` / `简单脚本` / `CI 流水线`
  - **右栏（Agent SDK）**：
    - 标题 `Agent SDK` + 代码括号图标 `<>`，副标题 `智能体开发套件`
    - 类比句：`如同 HTTP 客户端库`（紫底白字）
    - 三点能力：`动态 Prompt` / `精细编排` / `深度集成`
- **底部预告条**：金底黑字 `→ 第八章深入剖析 Agent SDK`，箭头指向下一章，形成强引导。
- **风格**：经典“工具对比卡”，用类比降低认知门槛（curl vs HTTP Client），是飞书技术文档高频模板。

#### ▶ 飞书风文字提取：
```
CLI vs Agent SDK

🔵 CLI｜命令行接口  
• 如同 curl  
• 轻量便捷  
• 简单脚本  
• CI 流水线  

🟣 Agent SDK｜智能体开发套件  
• 如同 HTTP 客户端库  
• 动态 Prompt  
• 精细编排  
• 深度集成  

→ 第八章深入剖析 Agent SDK
```

> ✅ 飞书风体现：颜色分区、类比句前置、三点能力精炼、箭头预告，标准对比卡格式。

---

### **图片 22**

#### ▶ 视觉要素分析：
- **主标题**：`渐进式落地 · 四阶段路径`，白粗体；副标题无，留白强化重点。
- **四阶段横向流程图**，每阶段为圆角卡片，含图标+阶段名+核心动作+时间标注：
  1. **PHASE 1（蓝）**：`观察者` → `只输出报告，建立信任`（图标 👁️）
  2. **PHASE 2（青）**：`顾问` → `CI 监控，标记警告`（图标 💬）
  3. **PHASE 3（黄）**：`门禁` → `合并必要条件，逃生通道`（图标 🔒）
  4. **PHASE 4（绿）**：`主动修复` → `自动修复 + Commit`（图标 🔧）
- **阶段间连接**：虚线箭头 + `2–4 周` 时间标注，强调每个阶段需充分验证。
- **底部提示**：黄色时钟图标 + 文字 `每个阶段至少运行 2–4 周，让团队有时间建立信任`，点明落地节奏关键。

#### ▶ 飞书风文字提取：
```
渐进式落地 · 四阶段路径

👁️ PHASE 1｜观察者  
• 只输出报告  
• 建立信任  

💬 PHASE 2｜顾问  
• CI 监控  
• 标记警告  

🔒 PHASE 3｜门禁  
• 合并必要条件  
• 逃生通道  

🔧 PHASE 4｜主动修复  
• 自动修复  
• + Commit  

⏳ 关键原则：  
每个阶段至少运行 2–4 周，让团队有时间建立信任
```

> ✅ 飞书风体现：阶段编号+图标统一、动作短语无冗余、时间原则独立成条，飞书“落地路线图”典范。

---

### **图片 23**

#### ▶ 视觉要素分析：
- **主标题**：`本章小结`，白粗体居中，字号最大，作为章节收束。
- **四宫格布局**，每格含参数/配置项 + 作用说明：
  1. **左上（蓝）**：`-p 参数` → `Headless 模式入口`  
  2. **右上（黄）**：`output-format` → `输出消费方式`  
  3. **左下（绿）**：`allowedTools` → `安全边界`  
  4. **右下（紫）**：`max-turns / max-budget-usd` → `成本护栏`  
- **底部横幅**：金底黑字流程链：  
  `交互助手 → 可编程组件 → CI/CD 标准环节`  
  用 `→` 表达演进路径，呼应全书主线。
- **风格**：高度凝练的“本章核心要素卡片”，适合作为章节结尾的速查摘要。

#### ▶ 飞书风文字提取：
```
本章小结

🔹 -p 参数  
→ Headless 模式入口  

🔸 output-format  
→ 输出消费方式  

🔹 allowedTools  
→ 安全边界  

🔸 max-turns / max-budget-usd  
→ 成本护栏  

📌 演进路径：  
交互助手 → 可编程组件 → CI/CD 标准环节
```

> ✅ 飞书风体现：🔹/🔸交替标识、箭头归因、结尾路径公式化，飞书“章节小结卡”标准模板。

---

### **图片 24**

#### ▶ 视觉要素分析：
- **顶部标签**：金黄方括号 `[ 下章预告 ]`，与第31张风格一致，形成闭环。
- **主标题**：`Agent SDK 智能体开发`，超大白字，其中 “Agent SDK” 为英文原词，“智能体开发” 为中文，中英混排强化技术感。
- **中部卡片**：深紫半透明圆角框，含：
  - 左上小字：`NEXT · CHAPTER 08`
  - 左侧图标：三层堆叠方块（代表 SDK 层级/模块化）
  - 主标题：`Agent SDK · 智能体开发套件`
  - 两行描述：
    - `从命令行参数迈向编程接口`
    - `TypeScript / Python 灵活控制 Claude 行为逻辑`
  - 右侧：紫色右向箭头 `→`，引导至下一章。
- **整体**：与第31张“Headless 预告”形成对称结构——本章以 CLI 收尾，下章以 SDK 开启，完成从“工具调用”到“智能体构建”的跃迁。

#### ▶ 飞书风文字提取：
```
[ 下章预告 ]

Agent SDK 智能体开发

NEXT · CHAPTER 08  
📦 Agent SDK · 智能体开发套件  
• 从命令行参数迈向编程接口  
• TypeScript / Python 灵活控制 Claude 行为逻辑  

→
```

> ✅ 飞书风体现：方括号预告标签、图标隐喻（📦=模块化套件）、技术栈明确列出、箭头收尾留白，完美复刻飞书“章节终章预告卡”。

---